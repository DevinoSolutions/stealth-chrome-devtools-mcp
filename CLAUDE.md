# CLAUDE.md — navigation map for agents

You are placing a change in `stealth-chrome-devtools-mcp`. This file is your map:
**where** things live, **what** each term means, and the **four conventions** a change
here must follow. It is name-only on purpose — you should be able to route a change to
the right file from this map *without reading function bodies*. The *why* behind the
architecture is in [`DESIGN.md`](./DESIGN.md); how to operate the backend is in
[`RUNBOOK.md`](./RUNBOOK.md); how to build/test/ship is in
[`CONTRIBUTING.md`](./CONTRIBUTING.md).

> This is a **local, single-user tool, 0 external users**. Priorities:
> maintainability, operability, performance.

---

## The four conventions (non-negotiable)

1. **One import form.** Always `from stealth_chrome_devtools_mcp.embedded.X import Y`
   (absolute-from-package; relative imports are banned). **No module under `embedded/`
   imports `server`** — it causes double tool-registration under runpy; pass
   `browser_manager` as an argument instead. See [DESIGN §8](./DESIGN.md#8-the-one-import-convention).
2. **One error convention.** Tools **raise** `tool_errors.ToolError` /
   `InstanceNotFoundError` on failure; success helpers return values. Do not add a
   `{"success": False}` dict — except to join a named KEEP contract
   ([DESIGN §9](./DESIGN.md#9-the-one-error-convention)).
3. **One cloner engine.** All DOM extraction lives in
   `embedded/cdp_element_cloner.py` (`CDPElementCloner`). The file/progressive cloners
   are thin adapters; never add extraction anywhere else, and never resurrect the
   deleted engines ([DESIGN §5](./DESIGN.md#5-the-cloner-subsystem-one-engine-deliberate-per-aspect-transport)).
4. **Golden discipline + "a second way is a defect."** Two-tier goldens: HARD
   invariants never bend; SOFT goldens update *deliberately*, in the same PR that
   changes a schema, with justification (see `CONTRIBUTING.md`). And the binding lens:
   **a change that introduces a second way to do something already done is a defect** —
   prefer extending the one home over adding a parallel path.

---

## Navigation map (the tree as it is)

Package root: `src/stealth_chrome_devtools_mcp/`. **Three console-script NAMES over TWO
mains** (`pyproject.toml` `[project.scripts]`): `stealth-chrome-devtools-mcp` →
`server:main` (the MCP server), and `stealthy` + `stealth-chrome-devtools` → the SAME
`cli:main` (F-891). The second CLI name is an ALIAS and never a second CLI — one
`_DISPATCH`, one parser, one set of verbs; `cli._prog_name()` reads argv[0] against the
closed `cli.SCRIPT_NAMES` tuple so help text names the command you typed and can never
advertise one that is not installed. `tests/test_doc_examples.py` pins the whole
`[project.scripts]` table as an identity, so the two CLI names drifting onto two
different `main`s fails.

### Top-level

| File | Owns |
|---|---|
| `server.py` | thin entrypoint — loads `embedded/server.py` as `__main__` via `runpy` (`main()` shim); its stdio branch is also **THE one place the PROXY process bootstraps its own observability** (`configure_logging("proxy")` + `_start_proxy_error_reporting`, F-827) — in the branch, never at the top of `main()`, or the runpy path would double-init. That branch is taken for the three flags this shim parses AND for the absence of an ANSWER-AND-EXIT request (F-905, `_ANSWER_AND_EXIT`): `-h`/`--help`/`--list-sections` are unknown to its deliberately `add_help=False` parser, so under the default `--transport stdio` they used to reach `ensure_server_running` and asking a question cold-started a backend. Such a request takes the runpy branch instead, whose `build_arg_parser()` can answer it — keyed on the request alone, so no other argv changed. The set's rule is "does that parser print and exit on it", which is why `--minimal`/`--debug`/`--xpool-safe` are NOT members (they configure a backend that then serves), and it is a hand-kept list because this shim may not import the backend's parser — a new printing flag there has to join it. `main()`'s FIRST statement is `backend_env.scrub_process_env()` (F-890 review M5) — at the top precisely because it must cover every branch: the runpy fallthrough imports `fastmcp` in THIS process, and `cli serve --http` delegates here, so this is the one line both reach. Per-branch calls would be the second-way defect waiting for a third branch. It is **the first of the package's two doors onto an `import fastmcp`**, not "the one line every path reaches" — `cli._server()` is the other, and carries the same call (F-889 review N3) |
| `__main__.py` | `python -m stealth_chrome_devtools_mcp` → `server.main()`, and **only there** (F-904): its one call sits under `if __name__ == "__main__":`, because `from ... import main` binds the name at import time and a bare call beside it ran a real backend on nothing more than `importlib.import_module("stealth_chrome_devtools_mcp.__main__")` (measured 2026-09-21 — F-903's own census probe did exactly that, into the operator's live `~/.stealth-mcp`). `tests/test_package_entrypoints.py` pins both directions, and carries two rules that generalise it: the whole-package one that replaced F-903's fence deny-list — **no module body in this package may CALL anything**, with two named allowances, both `tool_runtime`'s and both the same shape — `cdp_transport.install()` and, since F-912, `element_box.install()`: idempotent, no I/O, spawning nothing, once per process ahead of every tool body. They are TWO entries and not a widened pattern, because the allow-list's value is that a third is a decision someone writes down — and F-905's. **`--help` and `--list-sections` used to cold-start a backend through this door** and no longer do: `server.main` parses with `add_help=False` + `parse_known_args` (deliberately — every argument but its three belongs to `embedded/server.py`'s parser, reached through `runpy`), so both were *unknown* to it and the default `--transport stdio` carried them into `ensure_server_running`. They take the `runpy` branch now; `server._ANSWER_AND_EXIT` is the set and a new printing flag on the backend's parser has to join it |
| `cli.py` | **the ops CLI's verb + argparse home**, under both its names — the lifecycle verbs (`status`/`doctor`/`stop`/`restart`/`cleanup`/`kill-orphans`/`serve`) with their bodies here, and F-891's six tool-driving verbs (`tools`/`call`/`ls`/`spawn`/`nav`/`close`) **only as a parser TREE and a dispatch FALLBACK**: `build_parser` makes the one `add_subparsers` and hands it to `cli_call.add_parsers(sub)`, and `main` looks a verb up in this file's `_DISPATCH` (the ops verbs) then in `cli_call.DISPATCH`. Their flags, their shared parent and their bodies are all that module's — see its row. They were declared HERE until the main merge for F-892..895 put this file at 1016 of its 1000-LOC cap: the cut was the tool verbs' surface rather than a raised cap, and it also ended a real split, since this file declared `--profile` while the only explanation of what `--profile` MEANS was in the other one. One lazy `_cli_call()` import still fronts it, and what that protects is now the honest claim: building the parser does import `cli_call`, which costs `argparse` and `sys`, while `backend_client`, `httpx` and the `mcp` SDK stay behind the function that needs them, so an ops verb still never pays for the MCP client. `SCRIPT_NAMES`/`_prog_name()` are the closed set behind the alias. `_server()` is **the package's SECOND door onto an `import fastmcp`** (F-889 review N3) and every verb but `profiles` comes through it, so `backend_env.scrub_process_env()` joins the `STEALTH_MCP_NO_AUTO_RECOVERY` setdefault that was already there, in front of that import — otherwise a stray `FASTMCP_PORT=""` killed the very verbs an operator runs to find out why nothing starts (the pin reproduces the incident's own `ValidationError` through this door). The scrub removes third-party names only, so the read-only guard set one line above survives it. The status block **selects the backend ONCE** (F-868): `_probe_backend_status()` is called by the command, and its `(status, port)` is passed down to every line — `_format_backend_status` (now pure formatting, no I/O), `_recorded_backend_pid(port)` and `_doctor_port_occupant_line(port)` (both read the entry on THAT port via `backend_on_port`, never `first_backend`), and `_other_records_note(port)`, the `others      :` line that names the backends the summary is *not* about — compared on the **(display context, port) PAIR**, and it needs both: context alone stopped working the day F-886 let one context hold two identities (neither entry was an "other", so the summary claimed to be all there is while a second backend served the same desktop), and port alone hides a hand-edited non-int port, which reads as `None` and matches a `None` reported port in exactly the "nothing is running" case. Each other is labelled `context:port`, because two entries under one context are otherwise indistinguishable. Two independent record reads were deleted to get here; do not re-add one — a per-line read is how a live backend's status came to sit above a dead sibling's pid. **`_survey_records()` is the ONE probe pass over every recorded backend** (F-880), binding our record path and the two witnesses (`singleton._probe_port`, `singleton._is_our_backend`) to `backend_liveness.survey`; `_doctor_backend_lines` takes both its per-entry verdict and its `(dead record)` marker from that single pass, so a wedged sibling is probed once per run. It replaced `_probe_recorded_backend`, the adapter that added only the word `no port recorded` — that word is `backend_liveness.NO_PORT` now, and the adapter is DELETED rather than kept beside the survey. `_dead_record_line` and `_cleanup_backend_records` are the two consumers: `doctor` reports and never writes (read-only verb, `STEALTH_MCP_NO_AUTO_RECOVERY=1`), `cleanup --apply` is THE one caller that forgets. The `profiles` verb prints a SECOND line per profile since F-895 — for every row but the two the listing calls `default` and `default-seed` (F-896's words; `_SEED_ROLE` is this file's, `DEFAULT_SESSION` is read from `profile_seed` through `_seed_roles()`'s lazy import so the role column and a spawn's `profile_role` cannot drift), which print none because they ARE the seed and "what seeded them" is a category error (review m6; `_seed_line` answers `""` for those two roles and the caller omits the line rather than printing a blank one; since F-897 that function is the QUESTION only — the PHRASING is `profile_seed.seed_sentence`, because `stealthy spawn` says the same thing about the session it just made). `_collect_profiles` merges `profile_seed.provenance` into every row and `_seed_line` formats it, so the CLI and a spawn read the same three fields out of the same marker and cannot disagree about where a session came from. An unmarked SESSION directory still says unknown — there the answer genuinely is not known. The seed line has THREE endings and not two (F-897 review N1/N4, the full account is on the `profile_seed.py` row): `SEED CHANGED SINCE` only when it is True, nothing at all when it is False, and `(source unreadable)` when it is None — which a DELETED or renamed source guarantees, and which used to render byte-identically to False. A marker that cannot say WHERE it came from is a different question again and reads `seeded from unknown (when: unknown)`, never as up to date. It lists every directory under the clone root UNFILTERED, which is what keeps an existing `sessions/master` visible after F-894 refused the NAME |
| `cli_call.py` | **THE one home for the CLI verbs that drive the backend's TOOL surface** (F-891) — `tools`/`call`/`ls`/`spawn`/`nav`/`close`, plus **the CLOSED exit-code set and the one function that closes it** — `EXIT_OK`/`EXIT_TOOL_ERROR`/`EXIT_USAGE`/`EXIT_NO_BACKEND`/`EXIT_INTERNAL` (70, `EX_SOFTWARE`)/`EXIT_BROKEN_PIPE` (141)/`EXIT_INTERRUPTED` (130), and `_verdict`, which maps EVERY exception out of a tool verb onto one of them so no tool-verb invocation can pair a raw traceback with Python's default exit 1, the code that means "the tool answered and said no" (F-891 review M1; an ops verb's own non-pipe `OSError` still propagates, finding §6.17). 2 is argparse's own, so a usage error this file catches and one argparse catches leave the same trace — and `cli.main`'s "no subcommand" branch returns it too, because printing help is a usage condition and exit 1 there told a script the tool had refused (review M1); a TRANSPORT failure (`httpx.HTTPError`/`OSError`/`TimeoutError`) is `EXIT_NO_BACKEND` and never a tool error, because nothing on the backend saw the request; an `McpError` IS a tool error, because the round trip worked. **"The reader went away" needs its OWN judgement (`_reader_gone`) and it must be asked BEFORE the transport row** (review M2): it is an `OSError`, it comes from OUR OWN `print` after a round trip that worked, and under the transport row `stealthy ls | head -1` answered "could not reach the backend" — a false statement about the backend, made by the one function whose job is to keep transport and tool apart, over the commonest idiom in the shell. It is keyed on the ERRNO and not only the type — `BrokenPipeError` (EPIPE) on every POSIX, a bare `OSError(EINVAL)` on Windows for the same closed pipe — because a type-keyed row passed every hermetic double and still answered 3 on Windows when `tests/test_stealthy_cli_e2e.py` drove the installed launcher into a real closed pipe (round 4); it stands in front of the table because it is the one judgement that reads an attribute, not a type. 141 is `128 + SIGPIPE`, so `set -o pipefail` sees from `stealthy` what it sees from `ls | head`; the message is EMPTY, because the operator's `head` did what they asked. `_abandon_stdout` is the other half and is not optional — whatever `print` buffered is flushed when the interpreter finalises, that write fails again OUTSIDE every handler, and CPython answers with `Exception ignored in: <_io.TextIOWrapper …>` and exit **120**, a code outside the advertised set produced after the set had been honoured; re-pointing fd 1 at the null device makes that flush a no-op, and tolerating a missing `fileno` is the mechanism rather than laziness (a captured or replaced `sys.stdout` has no descriptor, and a stream owning none cannot fail to flush one). **It is reached from BOTH ways out, and the success path is the one a user meets first** (delta review M1, measured): the verdict covers a pipe error raised INSIDE a tool verb, but a verb that SUCCEEDS leaves the tail of any output above the 8 KB `TextIOWrapper` buffer unwritten — `stealthy call get_page_content | head -1`, and equally `stealthy profiles | head -1` — so the ONE explicit `sys.stdout.flush()` is `cli._delivered`'s, in `main`, after whichever of the two dispatch tables answered (round-4 S2: in `_run` it covered six verbs of fourteen) — and `main` guards the DISPATCH around it with the same `_reader_gone` judgement (round-5 M1), because an ops verb has no handler of its own and an answer that crosses the buffer raises mid-`print`, before any flush; the flush is the under-buffer half, the guard the rest, and a verb's genuine `OSError` still propagates. It is a SECOND guarded site, deliberately not a `_verdict` row: `_verdict` would route the measured EINVAL shape to the transport row and answer 3. `_abandon_stdout` itself may never raise — EMFILE on its `os.open` used to escape `main` from inside a handler (round-4 M1). It catches `OSError` and not `BrokenPipeError` because the measured Windows finalisation error is `EINVAL` (errno 22), not a pipe error at all, so a handler keyed on the pipe type would leave the real shape on 120. `_unwrapped` is why any of it reaches the table at all — the transport runs under an anyio task group, so a failure arrives as a singly-nested `ExceptionGroup`, and a group of SEVERAL is deliberately left internal rather than having one of its halves picked to report. `_run`'s `except` names four types for four reasons (`Exception` covers `ExceptionGroup`; `KeyboardInterrupt` is not one; `asyncio.CancelledError` is neither, and is safe to catch THERE because `asyncio.run` has already torn the loop down so nothing is deprived of its cancellation; a `BaseExceptionGroup` that is not an `ExceptionGroup` is what an interrupt becomes through that task group) and deliberately NOT `SystemExit`, which is how argparse's exit 2 already leaves. The one-line report is emitted under `contextlib.suppress(OSError)`, because `... 2>&1 | head -1` closes STDERR too and that print sits inside the handler — its own `BrokenPipeError` left `main` and became the traceback-plus-exit-1 the handler exists to prevent. `--traceback` PRINTS the stack after the one line and deliberately does NOT re-raise (review S1): the stack is never bought with the message, and a re-raise took the exception out of `main` past `cli.sentry_init()`, so `sys.excepthook` shipped a `BackendCallError` carrying the TOOL's own words — exactly what this module's docstring promises it "neither logs nor ships anywhere", and not a `tool_errors.ToolError`, so `expected_events`' convention rule would not have dropped it either. **Not a second CLI** — `cli.py` keeps the one parser TREE, the one `main`, the one dispatch order and both script names; what lives here is the six verbs' own surface, contributed INTO that tree by `add_parsers(sub)` and reached through `DISPATCH`, so there is still exactly one `--help`, one usage line and one set of verbs. Parsers sit beside their bodies because they are one question: the split (declarations in `cli.py`, meaning here) meant adding a flag to `spawn` touched two files and either could drift — convention 4 reached from the side where the second way is a second FILE. A different QUESTION with a different collaborator, too: the ops verbs drive the backend's LIFECYCLE from outside and must work when there is none, these six drive its TOOL surface over MCP and are meaningless without one; the split is also what keeps `cli.py` under a 1000-LOC budget that ratchets down only (`dom_handler`→`text_entry`'s precedent). `backend_url` is the SELECTION binding and it is thin for `singleton`'s reason (the suite patches those names): `singleton._probe_backend_status` — THE one selection `status`, `doctor`, `stop` and `kill-orphans` already make, so a `stealthy` command cannot drive a backend the `status` line is not about (F-868 reaching its last surface) — then `singleton.ensure_server_running`, the proxy's own startup path, so the cold-start lock, F-886's step-aside and F-889's adopt-forward all apply unchanged. **That first step is ALSO why this CLI never evicts a LIVE backend, and it is a property of the walk rather than a rule added here** (F-891 review S1): `backend_liveness.probe_recorded` asks `adoption_candidates` and the liveness ladder and NOTHING about identity, so a backend built from another source tree answers, is adopted, and the one path that can evict is never reached — a fingerprint mismatch is the proxy REUSE GATE's business, since a proxy is about to serve a whole session while a one-shot command only borrows the socket. What the SECOND step still costs is stated rather than hidden, here and in `--no-start`'s help and RUNBOOK: with nothing answering, a WEDGED foreign-identity backend on this context owning no live browser is evicted exactly as a proxy start would evict it. Widening step one to "any recorded backend that answers, whatever its display context" was built and REVERTED — it prevents no eviction (the entry it newly reaches is on another desktop, so the cold start targets another port and terminates nothing) while making `spawn --headed` open a window on a desktop nobody is watching and the CLI disagree with its own `status`. It answers a PAIR `(url, started)`, and the bool is load-bearing: a backend that just answered `responsive` is NOT re-probed, because `_await_backend_http` would spend the whole cold-start deadline re-proving what the selection established one line earlier. `_run` is the ONE place a selection happens and the ONE place an exception becomes a status, so no verb body carries an error policy. **No per-tool argparse mirror**: `call` takes a name and `--arg key=value` pairs whose values are JSON when they parse and strings when they do not (so `headless=false` is a bool while `C:\Users\me\profile` survives), split on the FIRST `=` so a query string does, with `--json` supplying the whole object and `--arg` winning per key — the tool's own schema on the backend is the validation, so a 95th tool is reachable the day it is registered and the tool count stays derived. `spawn` has exactly four sugar flags — `--session`, `--from`, `--headed`/`--headless`, `--url` — and `--session` is `session=<what you typed>`, passed through UNTOUCHED; an unnamed spawn sends no profile argument at all and is therefore whatever `spawn_browser` selects. `--session default` is how the shared profile is named (F-896). `--from` is `seed_from=<what you typed>` (F-897), passed through with the SAME discipline and for a sharper reason: whether that session exists, is open, is the shared one, or names a target that already exists are four facts only the backend can see, and a CLI-side pre-check would be a second answer that goes stale between the check and the spawn. **`--profile` is DEPRECATED and `argparse.SUPPRESS`ed**: accepted for one release, `user_data_dir=<what you typed>`, with one stderr line naming its replacement; the PATH door it used to be is `stealthy call spawn_browser --arg user_data_dir=<path>`, which is what `call` exists for, and a second sugar flag for one tool argument is convention 4's defect. **There is deliberately no `--master`** and never was one shipped: `master` as a bare NAME resolves to `sessions/master`, a different profile (F-894), and the word is retired. **Both profile flags at once are left for the BACKEND to refuse** — that rule is `profile_seed.profile_request`'s and re-deciding it here would be two surfaces that can disagree. What the backend actually selected is PRINTED (`spawn_diagnostics.profile_selection` — role and directory — plus `reattached`), never relabelled, so the CLI reports the resolver's answer instead of holding a second opinion about it. `wants_json` is the one output rule (JSON when asked, and whenever stdout is not a terminal) and it STAYS here, because it is a decision a script depends on. **What a TEXT answer looks like is `cli_render`'s** (F-897) — see that row |
| `cli_render.py` | **THE one home for turning a backend's answer into the lines a human reads** (F-897) — `instance_rows` (`ls`), `spawn_lines` (`spawn`), `tool_lines` (`tools`), the `clip` rule, the three column widths, `LAST_KNOWN_MARK` and `UNKNOWN_SECTION`. It left `cli_call.py` when that file stood at exactly its 1000-LOC budget and `spawn` needed a flag; the line between the two is CONSEQUENCE — an exit code and the JSON-or-text decision are things a script depends on and stay there, while the shape of a table is explicitly not a contract (which is why `wants_json` sends JSON the moment stdout is not a terminal). `instance_rows`/`LAST_KNOWN_MARK` keep F-874 true at the CLI: a `partial` or `stored` record carries no `current_url`, and a table filling that column from `last_navigated_url` would re-commit the exact defect, so the stale value is marked instead — and the table has no headed/headless or profile column at all, because `list_instances` reports neither. `spawn_lines`' seed line is F-897's and is what makes `--from` checkable at the shell; it asks `profile_seed.seed_sentence`, the SAME phrasing `cli._seed_line` prints, and OMITS the line when the marker says `UNKNOWN_SEED` — the shared session, its seed and a directory named by absolute path are nobody's copy, and the two callers gate that question differently on purpose (`profiles` lists DIRECTORIES, so an unmarked one is a finding; a spawn reports a spawn, so it is not). **`_cookie_lines` is its F-898 companion** and it prints the one fact the seed line cannot carry: a `seeded_via` of `cdp-cookies` says how many cookies were handed over from the running source, a `copy` with a `cookie_handoff_error` says they were NOT carried, names the shape-only reason and says in the same breath that the session was created and works without them — and an ABSENT `seeded_via` prints nothing, because an ordinary spawn from a closed source never had a jar to hand over and a line saying so on every spawn is noise. It never prints a cookie's name or value, because the diagnostics it reads never carry one. Every function is pure — records in, strings out — which is what lets them be pinned without a Chrome, a socket or a process. A leaf: stdlib, plus one LAZY `profile_seed` import inside `spawn_lines`, lazy because importing `embedded` fires that package's `sys.path` shim and an `ls` must not pay for it |
| `settings.py` | **the one env home** — pydantic `Settings` + `get_settings()`; every `STEALTH_MCP_*` knob is a typed field here |
| `observability.py` | Sentry error shipping — hardcoded DSN, on by default, never raises (no-op under `STEALTH_MCP_NO_ERROR_REPORTING`); **the one PII scrubber** — `_scrub_event` (Sentry's `before_send`), whose step 0 drops and step 1 scrubs, in that order, which is also why step 0 reads the message as the SDK logged it; **the one non-exception report** — `capture_lifecycle` (F-827: proxy transitions that are decisions, not crashes). It owns the two SHAPES an event can arrive in and nothing about which events are noise: `_hint_exception` (the live exception behind `hint["exc_info"]`, else `hint["log_record"].exc_info`), `_exception_chain` (the SDK's own `__cause__`/`__context__` walk — OUTERMOST first, which is the opposite of the order Sentry serialises `exception.values` in, so `expected_events` normalizes and no rule here may assume either), `_expected_error_base` (the LAZY `tool_errors.ToolError` import — kept lazy because `cli.py` imports this module at startup and firing `embedded/__init__.py`'s `sys.path` shim in an ops-CLI process buys nothing, which is why F-887 resolves it ONLY when there is a live chain to `isinstance` against) and `_expected_event_class`, the ONE binding that hands chain + event to `expected_events.classify`. `_is_expected_tool_failure` is step 0's bool view of that answer — it keeps its name because the pins call it by it, and it is where the recognised class is logged at DEBUG, so a drop is attributable in a RUNNING process and not only in the suite. **What a third-party logger may SHIP is deliberately NOT decided here** (F-906): `LoggingIntegration(event_level=ERROR)` is stated in `sentry_init`, and its breadcrumb handler's INFO default is what let `nodriver.core.connection`'s raw-event line become a breadcrumb the moment a caller enabled root DEBUG — but the fix is a LEVEL in `logging_setup`, not a `before_breadcrumb` here, because the SDK patches `logging.Logger.callHandlers` and `Logger.handle` reaches it only for a record `isEnabledFor` has already admitted. A level therefore closes the Sentry sink and the three non-Sentry ones in one place, while a rule here would close one of four and put log configuration in two homes. **F-907 reaches the same verdict from the other side and for the same reason**: what a third-party logger may ship ABOVE that floor — `Element.__repr__`'s attribute values and page text, at WARNING — is redacted by a `logging` record FACTORY in `logging_setup`, which sits upstream of `callHandlers` and so closes the breadcrumb along with stderr; a `before_breadcrumb` rule here would again be a second home closing one sink of four, and it could only ever matter if the factory were removed. `include_local_variables=False` keeps its second, separate job — `cdp_transport` pins that this module still sets it, because F-902's shape argument rests on it |
| `expected_events.py` | **THE one home for "is this Sentry event the product working as designed?"** (F-887) — the five named classes (`ERROR_CONVENTION` / `CLIENT_DISCONNECT` / `PROACTOR_TEARDOWN` / `NODRIVER_DEAD_BROWSER` / `CALLER_INPUT`), the `RULES` table that enumerates them, and `classify`, which returns the NAME that recognised the event so a drop is attributable — reported, never branched on; there is one drop. Every class is ONE rule read through `Link` (a type NAME + a MODULE + the FRAMES) and `Kind`, so the live path and the serialized path cannot disagree — and **all three spellings are read out of `sentry_sdk/utils.py`, never guessed, because all three differ from the obvious Python answer**: `type` is `get_type_name` = `__qualname__ or __name__` (so a class declared inside a function is `outer.<locals>.Name`, and reading `__name__` let a nested class match a `Kind` the payload could never match — `_type_name_of`); `module` is `get_type_module`, which drops `None`/`builtins`/`__builtins__` and **NOT `__main__`** (`_UNWRITTEN_MODULES` is those three EXACTLY — dropping `__main__` too made a `__main__` class read as a builtin live and as itself serialized, and `embedded/server.py` runs as `__main__` under runpy); and a frame's `module` is `frame.f_globals["__name__"]`, caller-first, which is the traceback's own order. `_links` also normalizes ORDER to **outermost first** — the live `__cause__` walk starts there, Sentry's `values` end there — so `links[0]` is the exception Sentry titles the issue with on either path. `Kind.modules` is a set of module ROOTS matched exactly or on a DOTTED boundary (pydantic arrives as `pydantic_core._pydantic_core`, ours as `…embedded.tool_errors`; the boundary is what keeps `pydanticfoo` out) and `None` means builtins, matched exactly so a third-party class sharing a builtin's name cannot pass. There is exactly ONE `isinstance` (`_is_ours`), because convention 2 is about a CLASS and a subclass declared anywhere must be covered; every other kind is name+module on BOTH paths deliberately — an `isinstance` on the live path would accept subclasses the serialized path could never see. `error-convention` requires the **OUTERMOST** link to be ours and tolerates `TimeoutError`/`asyncio.CancelledError` behind it (what `asyncio.wait_for` is made of) — never as the reported exception, because in every real budget chain the conversion is the LAST raise, so an unconverted outermost timeout is the missing-convention case even with a `ToolError` in its chain. **`caller-input` is decided by the FRAMES, not by its logger**: `fastmcp/tools/tool_manager.py`:220-229 wraps the whole of `tool.run` — argument validation AND the tool body — in ONE `try` with ONE `logger.exception`, so a caller's bad kwarg and a `ValidationError` OUR code raised are byte-identical in logger, message and chain (measured: a logger-only rule dropped the unknown-`STEALTH_MCP_*`-key `Settings()` crash, which fails EVERY spawn, and `browser_manager.py`:429's `BrowserInstance(...)`). `ARG_VALIDATION_FRAMES` is the discriminator and it must be ADJACENT — `fastmcp.tools.tool run` is in every tool failure's traceback, but only directly above `pydantic.type_adapter validate_python` when FastMCP itself validated. The two remaining message rules carry their evidence: `proactor-teardown` names CPython's `asyncio/proactor_events.py`:154 `shutdown(SHUT_RDWR)` callback because a reset our own code saw is our fact, and `nodriver-dead-browser` requires `nodriver` IN the message so an unawaited task of ours — same logger, same complaint, same exception type — still ships. **`client-disconnect`'s message-only arm is WIDER than its name and says so**: it matches "an exception with NO TEXT at `mcp.server.lowlevel.server`", not "a `ClientDisconnect`" — `mcp/server/lowlevel/server.py`:707 is a `case Exception():` catch-all formatting `str(exc)` with no `exc_info`, so a bare `RuntimeError()` of ours is dropped with the 6 500 (pinned, not implicit); it cannot be narrowed, because that event has no values, no frames and no extra, and it is matched by EQUALITY so that line's real protocol faults still ship. `_UNREADABLE` is why "could not be read" is never "recognised". A leaf: stdlib only, the chain and the error base arrive as ARGUMENTS, it does not catch (the never-raises contract is the caller's, so a bug here is visible to the suite instead of becoming "ship it"), and it has exactly ONE consumer |

### `embedded/` — the backend

**Lifecycle & transport**
| File | Owns |
|---|---|
| `server.py` | the real MCP server — **ZERO tool bodies**; all 94 live in `tool_sections/` (plan_SERVERSPLIT, complete at slice 12). What this file owns, and nothing else: the per-execution FastMCP app (`mcp`/`registry`), `app_lifespan`, the four `@mcp.resource` handlers, the xpool-safe gate, `build_arg_parser`, the `__main__` block, **F-889 (b)'s one heartbeat start** (`_SERVE_PORT`, stamped by `__main__` beside `_SERVE_TRANSPORT` and for the same reason, plus the `rt.backend_liveness.start_beating(...)` call inside `app_lifespan`'s once-per-process guard — there and not in `__main__` because that is the first code of ours that runs ON the serving event loop, which is the whole point; http only, since a standalone stdio backend records nothing and has no proxy watching it), and the **binding loop** that drives registration from THIS file's module body, once per execution of it (so the canonical import, the bare-name spec load and the runpy `__main__` load each get a full 94-tool app — a section module that decorated itself would register into the first execution only). **No migration alias block**: every singleton, knob and guard this file drives is read as `rt.<name>` against `tool_runtime`, the one patchable home, exactly as a tool body does. The lone import left is `clone_storage`, which this file never touches — it is the positive delegation handle `tests/test_clone_storage.py`'s F-201 negative-surface pin needs. At 523 LOC it is governed by the 1000-LOC default; its `GRANDFATHER` row is deleted, not merely satisfied |
| `singleton.py` | **backend lifecycle + the stdio proxy** — the liveness PRIMITIVES (`_backend_http_ready`, `_server_is_healthy`) and the two thin bindings that hand them to `backend_liveness` (`_probe_port`, `_probe_backend_status` — see that row for the policy; they are wrappers on purpose, because the suite patches these names and a wrapper resolves them at CALL time). `status`, `doctor`, `stop` and `kill-orphans`'s live-backend guard all consume `_probe_backend_status`, so record selection is fixed in one place; `restart_backend` deliberately does NOT — it reports `_probe_port` for the port it spawned on, because the adoption walk answers "is there a backend for me" and a responsive SIBLING must never speak for a restart's own fresh backend. Port selection (`_select_backend_port`, `DEFAULT_PORT`), the one identity+readiness reuse gate (`_same_identity_backend_ready`, `_source_fingerprint` — a wrapper now; the digest itself is `build_identity`'s — `REUSE_PATIENCE_SECONDS` — which spends its patience through `scheduling_lag.FairWindow`, F-856) and its identity HALF (`_identity_matches`, extracted by F-886 because the eviction guard needs "is this entry a stranger's" and re-spelling it would be a second answer to one question). **"Would I adopt it" and "is it mine" are TWO questions and have two predicates** (F-889 (d) + its review's H1): `_adoptable_identity` is `_identity_matches` OR a recorded version strictly NEWER than ours (through `build_identity.newer`), so a mid-upgrade fleet adopts forward instead of ping-pong-evicting — and it is read by the REUSE GATE and by nothing else. `_identity_matches` still means exactly "mine", because `backend_eviction.protected`, `port_for_context` and `own_or_first_port` all ask it: widening it to cover adoption made a newer stranger read as ours, so the protection rule's condition 2 returned no browsers and the kill site TERMINATED a newer sibling still holding its user's logged-in Chrome, and `restart` was handed the stranger's port back. Adopting forward means step aside, never take the port. Cold-start lock (`_start_backend_holding_lock`), `run_stdio_proxy`. **The bridge's TRANSPORT is not this file's** (F-900): `run_backend` opens the SDK's current `streamable_http_client` over an `httpx.AsyncClient` it asks `backend_client.http_client` for, with `backend_client.BRIDGE_READ_TIMEOUT` — the argument for the policy lives at that constant, and the call site carries a pointer, not a second copy of it. It used to be a bare `streamablehttp_client(url)`, which was both `@deprecated` and a silent inheritance of the SDK's `read=300`. A client we PASS is one the SDK does not close, so it is entered here, and `terminate_on_close=True` is passed explicitly for `backend_client.opened`'s reason. **The F-889 wiring here**: `_watch_backend_liveness` binds the heartbeat witness (`backend_liveness.self_report` against THIS module's `SERVER_STATE_FILE`, a lambda so a `monkeypatch.setattr` still redirects it); `backend_leg`'s two teardown lines are DELETED because `proxy_selfheal.drive` no longer returns; and `_proxy_streams` captures `client_presence.capture()` at start and runs `client_watch` beside `backend_leg` — the SECOND exit, declared BEFORE `backend_leg` so the source pins that read that function's body cannot see it. Both exits are outside the backend leg, which still cannot end a session over a backend. The two liveness probes are thin bindings of `backend_probe` now (`_backend_http_ready`, `_await_backend_http`): this file owns which URL a PORT of ours means and the deadline a cold start gets; the `initialize` payload they used to duplicate is that leaf's. **Eviction moved to `backend_eviction.py` (F-886)** — what stays here is four thin bindings (`_backend_pid_on_port`, `_terminate_backend`, `_protecting_browsers`, `_clear_stale_backend`) that know which record, which state dir and which probes are ours; they are WRAPPERS on purpose, because the suite patches those names and a wrapper resolves its collaborators at call time. Two sites ask the rule: `_select_backend_port` folds `backend_eviction.stepping_aside` into its `force_new`, beside the foreign-occupant and sibling-context clauses, and `_clear_stale_backend` guards the kill itself. **Identity-gating that rule is not what keeps `restart_backend` working** — selection must also CHOOSE its target on identity (`matches=_identity_matches` into `port_for_context`), or on a two-identity desktop it targets the stranger's port, steps aside from it, and restart spawns a third backend while its own wedged one keeps running with no verb left that reaches it — a non-empty answer there means `_start_backend_holding_lock` spawns NOTHING and the next proxy start re-selects. The `server.json` record moved to `backend_registry.py` (path names re-exported here for legacy callers); the proxy's *recovery from a dead backend* moved to `proxy_selfheal.py`; the watchdog LOOP moved to `backend_watchdog.py` — `_watch_backend_liveness` stays here as the wiring that knows which probes are ours; and **the spawn itself moved to `backend_launch.py` (F-867)** — `_start_server_process` still owns WHAT to start (`_server_process_cmd`, `_backend_interpreter`'s F-866 base-interpreter choice, the child env, F-830's boot-log roll) and what to RECORD afterwards (`PORT_FILE`, `_write_server_state`), and makes exactly ONE call, `backend_launch.spawn(cmd, child_env, boot_log) -> Launched(pid, rung)`; it no longer imports `subprocess`. **WHAT the child env may carry is `backend_env.scrub`'s** (F-890) — one call here, which also absorbed the `STEALTH_MCP_NO_AUTO_RECOVERY` pop that used to sit inline |
| `backend_launch.py` | **THE one home for "spawn the backend where no MCP client's Job Object can reach it"** (F-867) — `spawn`, `Launched(pid, rung)` and nothing else in the tree may create a backend. POSIX is one rung (`posix`, `start_new_session=True`, unchanged). Windows climbs three, and the rung that served is logged through the ONE `_log_rung` line on `stealth.proxy` so a post-mortem can read it: (1) `breakaway` — `CREATE_BREAKAWAY_FROM_JOB`, accepted ONLY when `_proven_in_a_job` says the new process is in NO job, because under a nested job chain the flag leaves the innermost job only and a "successful" breakaway can still sit in the client's; `ERROR_ACCESS_DENIED` (what the SDK's job returns) drops a rung while any other `OSError` propagates. A PARTIAL escape is never discarded blindly — rung 2's viability (`_scheduler_plan`) is decided FIRST, and when there is no scheduler rung the child is KEPT as the `breakaway-partial` rung at WARNING, because out of one job beats out of none; conversely, breakaway once proven PERMITTED is re-asked on rung 3. (2) `scheduler` — a one-shot Task Scheduler task through `desktop_launch._schtasks` (the F-810 seam, not a second home) running `pythonw.exe` (no console, so no window can flash) on the BASE interpreter, never a venv's `Scripts\pythonw.exe`, which is F-866's redirector and would rebuild the kill-on-close job; the stdlib-only `_LAUNCHER_SCRIPT` reads argv + the ENTIRE child env + the boot-log path from a JSON spec in `~/.stealth-mcp/backend-launch/` (the command line carries two paths only because **`schtasks` stores at most 253 chars of `/TR` and truncates beyond that with exit 0** — measured, not F-810's documented "~261"; the silent truncation costs a Last-Result-2 task, the whole 20 s pid deadline and a drop to `plain`, which is why the per-attempt token is 12 hex chars, not 32. That cap, that token length and the comparison itself are **not defined here**: they are `desktop_launch.TR_MAX_CHARS` / `TOKEN_CHARS` / `tr_overflow`, read at call time from the module that owns the `schtasks` seam, so this file carries no second 253 that could drift — F-879), re-opens `backend-boot.log` itself and gives the backend stdout AND stderr (F-303 survives the hand-off), and returns the SERVING pid through an `os.replace`d pid file. **Gated on `_same_session_as_console`**: taken only when the spawner is already in the logged-on console session, so the backend lands where it would have anyway and the display context `singleton` records stays true (F-808 — the tool still never PICKS a session). Task and scratch files deleted in a `finally`, and the orphan sweep that runs before a scheduler spawn is a FILESYSTEM sweep, not a `/Query`: any `<token>.json` spec (token = 12 hex) older than 2× the pid deadline gets its `stealth-mcp-backend-<token>` task deleted BY NAME and its files removed. Deliberately not "delete tasks whose spec is absent" — `_cleanup` deletes the task first, so a killed spawner leaves task AND spec, which is the only orphan class there is and the one that predicate missed. A live sibling's spec is younger and untouched; a clean dir costs zero `schtasks` calls. (3) `plain` — today's `DETACHED_PROCESS \| CREATE_NEW_PROCESS_GROUP` (plus the breakaway bit when rung 1 proved it permitted), for a runner with no console session; its line names F-867 and says the backend is inside this client's job. A leaf: `backend_registry` for the state dir, `desktop_launch` lazily for the ONE `schtasks` seam, the ONE pid-file reader, the ONE task teardown and the ONE `/TR` length cap, so none of them gets a second home. Never raises for a rung's own failure — only a genuine `Popen` error reaches the caller |
| `backend_probe.py` | **THE one home for "ask the backend's MCP endpoint whether it is genuinely ready"** (F-889 review) — the ONE `initialize` payload (`_request`), its two headers, the throwaway-session DELETE, and the two SHAPES this tree needs: `ready(url, timeout)` (one synchronous attempt, so a sync caller can call it and the watchdog can drive it off-thread) and `await_ready(url, deadline)` (an async poll, because a cold start is waited out rather than sampled). Why an `initialize` and not a socket connect or "any HTTP response" is argued here once (F-301/F-501 + the 4xx a freshly bound uvicorn answers while FastMCP's session manager starts). Never raises — every failure is False, `_server_is_healthy`'s fail-closed contract, DEBUG because both fire routinely during a cold start. This is the plan_M1 §2.2 #4 debt `singleton._backend_http_ready`'s docstring had carried since M1, paid when the F-889 review needed headroom in a file at exactly its cap: **extract, never raise the cap.** A leaf — `httpx` and `mcp.types`, both imported lazily inside the functions so the proxy's cold start does not pay for them; the URL arrives as an argument, so nothing here decides which backend is being asked about |
| `backend_client.py` | **THE one home for "call a tool on the backend at this URL, and read what it answered"** (F-891) — `opened`, `call_tool`, `list_tools`, `result_value`, `BackendCallError`. Its sibling is `backend_probe` and the boundary is one question each: that one asks *is this backend ready* (a raw `initialize` POST, no client library, no session kept, a bool, never raises), this one says *do this* (a real MCP session through the official `mcp` SDK). Neither may grow the other's question — a liveness answer over a full session costs a handshake per probe, and a tool call over a hand-rolled POST is a second spelling of the protocol. **One session per call and it is always terminated**: `terminate_on_close=True` is passed EXPLICITLY although it is also the SDK's default, because the DELETE is what this exists to guarantee and a default is a value a dependency bump can change silently; F-862's sweep reaps sessions whose client vanished, and a CLI leaking one per invocation would be asking that sweep to clean up after it. A verb making two calls therefore makes two sessions — deliberate, because the alternative is every verb owning a session's lifetime and one of them eventually forgetting. **What is pinned is the DELETE and not the flag** (F-891 review M2): `tests/test_stealthy_cli.py`'s `TestSessionHygiene` drives the REAL `mcp` SDK over an `httpx.MockTransport` and asserts the session id reaches a `DELETE` on all four ways out — success, a tool error, a socket that dies mid-`tools/call`, and cancellation — because the exception path is correct today by ORDERING alone (`call_tool` leaves the `async with` before it raises), which is what a refactor breaks in silence. The node it replaced asserted a keyword argument and its double's `__aenter__` raised, so the exit path never ran. **`streamable_http_client`, never `streamablehttp_client`** (review S2): the latter is `@deprecated` at the pinned mcp 1.27.1 and warns on the CALL, before the context is entered. It does **not** ignore `timeout`/`sse_read_timeout` — this row said so until F-900 and it was wrong; measured, it builds `httpx.Timeout(timeout, read=sse_read_timeout)` and hands the client down to the replacement, and the ignore-with-a-warning belongs to `StreamableHTTPTransport.__init__`'s deprecated overload, which that function never reaches. What the replacement does is take the `httpx.AsyncClient` itself, which is why `http_client(read_seconds)` exists — **THE one transport seam**, the one thing a test substitutes (`scroll_position._now`'s pattern: replace the transport, never the protocol), and the one place the two clocks are decided. Connecting always keeps `CONNECT_TIMEOUT_SECONDS`, so a vanished socket is reported in seconds rather than at the end of a long read budget. A client we PASS is a client the SDK does not close, so it is entered by whoever asked for it. A pin reads `streamable_http_client`'s signature, so a bump that renames it or drops `http_client` is a deliberate decision rather than a silent reversion to the SDK defaults. **Since F-900 the seam has a SECOND consumer and the parameter is `read_seconds`, not `budget_seconds`** — the stdio proxy's BRIDGE asks it with `BRIDGE_READ_TIMEOUT`, which is **`None`**: a bridge is not a call, it holds a standing GET event stream for the life of a session, the backend pushes nothing on it while the session is quiet and sends no SSE keepalive, so ANY read deadline is a deadline on being IDLE — and the SDK does not degrade gracefully past one (`handle_get_stream` retries `MAX_RECONNECTION_ATTEMPTS` = 2 and then RETURNS, at DEBUG), so the inherited `read=300` lost the stream permanently after ~601 s of quiet, which is precisely the discriminator `session_hygiene` reaps a session by (F-862, whose docstring promises a live proxy idle for hours is never touched). A bigger number only moves that clock. The bridge does NOT use `opened` — it is a transparent pipe with its own streams — so what it borrows is the transport and nothing else; what BOUNDS it stays the F-820 watchdog, `proxy_selfheal` and each tool call's own CDP budget, because a transport read timeout is a second answer to "is the backend still there" that cannot tell an idle session from a dead backend. Extending this one function rather than adding a second policy constructor is convention 4 read literally, and `None` is httpx's own spelling of "no deadline" on the clock this parameter always set. `result_value` is THE one reading of an answer and it is PURE, so its three MEASURED shapes are pinned without a backend: `structuredContent` carries the answer while `content` is EMPTY (the common case — a reader reaching for `content[0].text` first raised `IndexError` on every call that worked, measured on the 2026-09-19 prototype this feature replaces), a non-dict return arrives FastMCP-wrapped as `{"result": …}` and is unwrapped only when `result` is the SOLE key, and text content is the fallback, parsed as JSON when it is JSON. A leaf: the `mcp` SDK and stdlib, both imported lazily inside the functions (`backend_probe`'s reason), URL as an argument, so nothing here decides which backend is driven |
| `client_presence.py` | **THE one home for "is the client that started this stdio proxy still there"** (F-889 review M3) — `capture()`, `present(token)`, `await_gone(token)`, `CHECK_SECONDS`, and the ancestor WALK (`_is_shim`, `_LAUNCHER_NAMES`, `_OUR_SCRIPTS`, `_PACKAGE_MARK`, `WALK_LIMIT`). **The direct parent is usually not the client** (review N2, measured): above a proxy sits `python.exe` (the venv trampoline, byte-identical argv) → `uv.exe` (waiting on it) → the shell → `claude.exe`, and on POSIX `uvx` waits in the trampoline's place. Every one of those shims outlives us BY CONSTRUCTION, so a token naming one can never become "gone" while a session is stranded — which is exactly how this exit came to miss the 116 stale proxies it exists for. So `capture` climbs to the first ancestor that is nobody's shim (a named launcher, one of our console-script redirectors, an argv identical to ours, or any argv token carrying the package name) and stops THERE — climbing further would name a terminal that outlives the client, i.e. the same miss by a longer route. **Walking too far is the safe direction**: every ancestor lives at least as long as the one below it, so an over-eager skip only ever delays the exit, while naming a shim short of the client makes it never fire. Which is why ambiguity — a walk that does not settle inside `WALK_LIMIT` (8), an ancestor psutil will not describe, no parent at all — answers `None`: presence unknown, and this proxy never exits on that ground. F-889 (c) deleted the proxy's exit because that exit IS the outage; the same outage's cleanup found **116 stale proxies**, so "never exits on its own decision about the backend" has to keep being different from "never exits". The identity is the PAIR `(pid, create_time)` and never a bare pid — a recycled pid would make this module cause the disconnect it exists to prevent — the same discipline `browser_pid_registry` stamps on every browser. Captured ONCE, at proxy start: asked later it would name whatever we were reparented to, which on POSIX is init and is immortal. **Fail-open uniformly** (no parent, psutil refusing, a partial answer all read as PRESENT): a false "gone" disconnects a live session, a false "present" costs one idle process the next EOF collects. A poll and not an event, because no portable notification for "my parent died" exists to await. A leaf: `psutil` + stdlib, and it never cancels or exits anything — `singleton._proxy_streams` owns what to do with the answer, exactly as it does for `pump_client` returning |
| `backend_env.py` | **THE one home for "what the environment we hand the backend must NOT carry"** (F-890) — `scrub(env)`, `scrub_process_env()`, `FASTMCP_PREFIX`, `NO_AUTO_RECOVERY`. **Three callers, two tables, one shared half** (review M5 + F-889 review N1/N3/N4): the child-env composer, `server.main()`'s first statement, and `cli._server()` — because the composer is not every path by which this package imports `fastmcp`. The `runpy` fallthrough runs `embedded/server.py` IN THIS PROCESS (that is what `--transport http` does) and `cli serve --http` reaches the same line through `_cmd_serve`, so an operator with a stray `FASTMCP_PORT=""` in their shell got the identical import-time `ValidationError` with the identical absence of a log line. `scrub_process_env` READS `os.environ`, which this repo otherwise confines to `settings.py` — a NAMED exception, argued in the module docstring: that rule is about CONFIGURATION (one typed home per `STEALTH_MCP_*` knob), while this reads nothing and produces no value, it deletes a third party's names before a library parses them. Putting it in `settings.py` would move a `FASTMCP_` prefix into the `STEALTH_MCP_*` home and make `settings` import from `embedded/`. The backend is a child of the stdio proxy and `_start_server_process` copies `os.environ` whole, so the MCP CLIENT's environment reaches it — including names that belong to a THIRD party. `fastmcp` (2.11.2) builds a `pydantic_settings.BaseSettings` AT IMPORT whose `env_prefixes` are `["FASTMCP_", "FASTMCP_SERVER_"]`, so an inherited `FASTMCP_PORT=""` is parsed into `port: int` before `build_arg_parser` exists as a concept and `int("")` kills the process before `configure_logging` has installed anything — sixty-six minutes of launches on 2026-09-18 died in `backend-boot.log` alone while the proxy above them retried and gave up (measured; the bare `port`/`PORT` names the incident brief named are NOT read — `env_prefix` is `''`, which is what makes them look plausible, but `ExtendedEnvSettingsSource` walks `env_prefixes` instead, so a fix written against them would have shipped as a fix for a bug it did not touch). Scrubs by PREFIX and drops the WHOLE family, never "override the field that crashed us": our backend's configuration comes from the argv `_server_process_cmd` builds, so every `FASTMCP_*` name is a second input to a decision already made (`stateless_http` would not even crash — it would silently give the bridge a transport it is not written against). Deliberately NOT derived from `Settings.model_fields`: that needs an `import fastmcp` in every proxy cold start, and **the stdio proxy must never import the MCP server stack** (`desktop_launch` carries the same paragraph about its lazy nodriver import). The constant is OURS; that it still COVERS the library is pinned against `fastmcp`'s own `model_config`. Case-folded on both sides, because `case_sensitive` is False AND Windows folds env-var case in the OS. It also **absorbed M8-2's `STEALTH_MCP_NO_AUTO_RECOVERY` pop** — same sentence ("a name in the parent's environment must not reach a decision the backend makes for itself"), so one removal site and not two lines apart. **That pop is the CHILD table's and ONLY the child table's** (F-889 review N1): applied to our OWN environment it is a defect, because that flag is the operator's own answer — `cli.py` sets it with `os.environ.setdefault` so `doctor` and `status` stay read-only — and deleting it silently re-enables the orphan reaping a read-only verb exists not to do (measured: `scrub_process_env` removed `['FASTMCP_PORT', 'STEALTH_MCP_NO_AUTO_RECOVERY']`). So `scrub_process_env` applies `_inherited_fastmcp` ONLY and **never removes a `STEALTH_MCP_*` name from this process**; the prefix still has one home, and what differs between the two tables is one clause and the party each environment belongs to. `_remove` is the shared act, and the LEVEL is the caller's (review N4): the composer reports at INFO into a configured proxy log, `scrub_process_env` at WARNING because it deliberately runs BEFORE `configure_logging` and Python's last-resort handler emits at WARNING and above — an INFO line there is written to nothing. A leaf: stdlib only, `scrub` never touches `os.environ` (it mutates the dict copy the composer owns), and the removed NAMES are logged, never their values — an environment is where tokens live and that line reaches the durable log |
| `backend_eviction.py` | **THE one home for taking a backend's port away from it — the DECISION and the ACT** (F-886). The rule: a backend is PROTECTED when it is one of ours, RUNNING, of an identity we would NOT adopt, and still owns at least one LIVE BROWSER — and a protected backend is never terminated and never bound over, so the arriving client spawns beside it and both sessions keep their browsers. Four conditions in cheapest-first order (`protected`), the record read behind the last of them (`owned_browsers`, through `browser_pid_registry.read_entries` and its `RECORD_NAME`), the composition a cold start calls (`clear_stale`: reusable → nothing to do; protected → the WARNING refusal naming the spared count; else evict), the bind-site half with its INFO line (`stepping_aside`), and the ACT itself (`pid_on_port`, `terminate`, `TERMINATE_GRACE_SECONDS`/`PORT_RELEASE_SECONDS`) — which moved here from `singleton` because a rule in one file and the kill it gates in another is how the rule came to be missing. **The act is deliberately UNGATED**: `stop_backend`/`restart_backend` also call it, and an operator asking IS the authority the rule otherwise supplies. Deliberately NOT "is it answering `initialize`" — a wedged foreign backend still holds its user's tabs, and whether a backend is USABLE is `backend_liveness`'. Deliberately "owns a live browser" and not "is alive": protecting every live backend accumulates one per source edit forever, and browsers are the only state reconnecting cannot rebuild. An unreadable record is no browsers, which resolves toward EVICTING, because refusing on one would brick every cold start. A leaf: `browser_pid_registry` + psutil; the identity verdict, both liveness probes and the record entry all arrive as arguments, so `singleton`'s four thin bindings stay the patch surface the suite targets (`terminate` even asks the CALLER's `pid_on_port`, so a patch of `singleton._backend_pid_on_port` still decides what dies) |
| `build_identity.py` | **THE one home for "which build is THIS process running"** (F-886 review) — `version` (the installed distribution version, or `UNKNOWN_VERSION` = `"0.0.0"`, which matches no real release so the gate cold-starts), **`newer`** (F-889 (d): is a RECORDED version strictly newer than ours — a plain numeric-segment compare, zero-extended so `2.2` and `2.2.0` are one build, and deliberately NOT `packaging.version`, which is not a declared dependency; anything unparseable and `UNKNOWN_VERSION` on EITHER side are "not newer", so every uncomparable case fails closed onto today's cold start. It exists because `fingerprint_mismatch` answers "these two digests differ" and never "mine is older": two identities on one desktop each read the other as stale and each evicted the other on every proxy start, and a fleet MID-UPGRADE is exactly the population F-886's browser-protection rule does not cover) and `source_fingerprint` (SHA-256 over every `*.py` under a root, the relative path hashed beside each file's bytes so a MOVED module changes the digest; `__pycache__` skipped, `ATTEMPTS`×`RETRY_SECONDS` to outlast a transient read error, `None` — never `""` — when it still cannot be read). Paired deliberately with `backend_registry.fingerprint_mismatch`, the one READER of a recorded digest, so producer and reader sit one import apart and a third spelling of either half cannot appear. Extracted from `singleton`, which keeps two WRAPPERS because the suite patches those names AND the two globals they read (`SERVER_NAME`, `SOURCE_ROOT`), so both must resolve at CALL time. A leaf: stdlib only, the package name and the source root arrive as arguments |
| `backend_watchdog.py` | **THE one home for the proxy's mid-session liveness watchdog** — the SLOW witness (`watch_liveness`): F-820's strikes plus the confirmation phase. A leaf: both probes arrive as arguments, so it never imports `singleton` and the dead-vs-busy policy stays single-homed in the reuse gate. **F-889 (a)+(b) put TWO gates in front of the verdict, and both are optional collaborators with inert defaults where the existing suite drives this.** (a) A strike run may not conclude until a `scheduling_lag.FairWindow` of `interval * (failures_before_teardown - 1)` has been SPENT — `fair_window` builds it, the default is that module's class (consumed, never modified, `MAX_STRETCH` included), and `expired()` is asked EVERY tick once a run is open so each tick is charged at the lag its OWN nap measured. On an idle machine nothing changes: the per-tick charge is `elapsed_tick / factor` = `interval * (1 + probe / nap_actual)`, which is **≥ `interval` always**, so the remaining ticks always spend the window and the human-pinned ~12 s hard-down detection stands. The pause IS the measurement (`FairWindow.nap` waits and observes its own lag in one call), so the DEFAULT tick runs it on a worker thread — it sleeps, and run inline it would freeze the stdio pump; a caller that injects its own `sleep` owns the pause and the window then charges wall seconds, which is what `interval=0` unit tests require. (b) A condemnation needs a SECOND WITNESS: `_vetoed` asks `heartbeat` how long ago the backend said its own event loop turned (`backend_liveness.self_report`), and a fresh report against a failed client probe RESETS the run — that is "I am starved", never "it is dead". It is called INLINE and never through `_ask`/`to_thread` (a small local JSON read; `tests/test_watchdog_busy_vs_dead.py`'s exact-call-list assertion is what states that), and tested `is not None` and never for truthiness, because a stamp written this instant is `0.0`. A stale or ABSENT report falls through to the confirmation exactly as before, which is what makes a mixed fleet — where the backend is 2.1.9 and stamps nothing — behave precisely as it does today. **The veto is BOUNDED at `HEARTBEAT_VETOES` = 10 completed strike runs** (F-889 review M1): the heartbeat proves the event LOOP is turning and NOT that the HTTP listener is reachable, and those can come apart, so an unconditional veto traded the outage for a hang. A veto costs a whole run and a run cannot conclude until its `FairWindow` is spent, so the budget is counted in FAIR-time rounds — spent slowly by a starved proxy, at the nominal ~12 s rate by a fairly scheduled one (≈132 s idle to reach the confirmation gate, ≤ ≈242 s under maximal starvation). It refills on any healthy tick or successful confirmation, so it bounds one continuous failure episode and not the session. `_defer` is the other extracted half and exists for the same kind of reason: the "not being scheduled" WARNING is written once per RUN, never per tick, because a starved proxy sits in that branch for as long as the starvation lasts (review L3) |
| `backend_liveness.py` | **THE one home for "is the backend on this port alive, which recorded backend would THIS client be served by, and which recorded backends are DEAD"** (F-868, F-880) — `probe_port`, the socket→`initialize`→`down`/`wedged`/`responsive` ladder (never copy those four lines again), plus `NO_PORT`, the one word the ladder cannot reach for an entry naming nothing usable as a port (it lived in `cli._probe_recorded_backend`, which F-880 deleted); `survey`/`Surveyed`/`dead_entries`/`forget_dead` — **THE one deadness rule, and it needs BOTH witnesses**: `down` AND the recorded pid not being a backend of ours, because a backend is recorded at Popen time and is `down` for its whole cold start, and because "pid gone" alone says nothing about whether the PORT is free (F-868 §6's objection, answered by requiring `down`). A `wedged` entry is NEVER dead. `survey` takes ENTRIES, not a path, so ONE probe pass serves both `doctor`'s `window_capable_first` listing and `cleanup`'s whole-record sweep; it never consults `adoption_candidates`, so a dead entry for a FOREIGN display context is forgotten exactly like our own — death is not a property of a display context. The WRITE is `backend_registry.forget_entries`', never this module's. And `probe_recorded`, the ADOPTION-order walk that reports the first candidate which answers — wedged over down when none does, `("none", None)` when no adoptable entry names a port at all — never `first_backend`'s "whichever entry the record lists first". A leaf on `backend_watchdog`'s pattern: the two primitives arrive as ARGUMENTS (`is_healthy`/`http_ready`) and the record as a PATH, so it never imports `singleton` and the order itself stays `backend_registry`'s policy. `probe_recorded` takes the per-port probe as an argument too, so `singleton._probe_port` — the name the suite patches — is what the walk actually asks. **Also THE one home for the BACKEND'S OWN liveness claim** (F-889 (b)) — `HEARTBEAT_INTERVAL_SECONDS` (3.0) and `HEARTBEAT_STALE_SECONDS` (30.0, stated beside it because their RELATION is the decision: ten missed loop iterations), `self_report` (the reader), `stamp` (the never-raising writer over `backend_registry.stamp_heartbeat`), `beat` and `start_beating`. Every OTHER liveness answer here is a claim by the PROBER, and a timeout is a fact about the backend only while we were awake to hear an answer — on 2026-09-18, with 2.4 GB free of 125.7 and 114 proxies paged out to ~0 MB, we were not, and the backend those probes condemned answered `initialize` in 227 ms. `self_report` is the one witness a starved prober cannot fake, and it costs no HTTP, no socket and no thread. It reads the per-port SIDECAR `backend_registry` owns, and it starts from the ENTRY — so a stamp for a port the record no longer names is not evidence about anything, which is what makes resurrection structurally impossible rather than guarded against. **`beat` must run on the EVENT LOOP and that is the entire design**: the failure the watchdog exists for (F-501) is a backend whose dispatch loop is dead while its socket stays open, and a heartbeat on an OS thread or in a signal handler would keep stamping straight through it and defend the one backend that deserves condemning. The WRITE goes to a worker thread (`_commit`'s Windows sharing retry can sleep 0.1 s and the loop being demonstrated must not be the thing blocked to demonstrate it), but the loop still has to schedule the hand-off and be resumed after it. `time.time()` and never `monotonic` — writer and reader are different PROCESSES — so the staleness bound is SYMMETRIC: a stamp from the future is a clock that jumped, not a backend that is alive. `None` (no evidence, falling back to today's behaviour) for: no entry, no stamp, a non-numeric stamp, and a `heartbeat_pid` disagreeing with the entry's `pid` — which means the stamp is a predecessor's leftover on that port, not a claim by the process recorded there. `start_beating` is idempotent per process on `session_hygiene.install()`'s precedent, and a no-op with no running loop |
| `scheduling_lag.py` | **THE one home for "was this process scheduled fairly, and what does a time budget owe it when it was not"** (F-856) — `FairWindow`, whose budget is charged in fair seconds (elapsed ÷ the lag its own naps measured), plus `MAX_STRETCH`, `REPORT_FACTOR` and the `proxy: patience extended under starvation` lifecycle report. It never decides alive-or-dead: only "has this window been spent", so `proxy_selfheal`'s ONE heal path is untouched. A leaf; the `_now`/`_wait` module functions are its single timing seam |
| `session_hygiene.py` | **THE one home for "this MCP session was abandoned by its client — reap it"** (F-862) — `HygienicSessionManager`, the MCP streamable-HTTP session manager plus a sweep: a session with NO standing GET event stream (a live proxy always holds one) and no request for `ABANDONED_AFTER_SECONDS` is terminated through the transport's own `terminate()`, so a liveness probe whose DELETE was lost or a proxy that died no longer costs the backend ~0.11 MB forever. `install()` binds the class to the name FastMCP constructs by module attribute — the one seam, called from `server.py`'s http branch as `rt.session_hygiene.install()` BEFORE `mcp.run()`. A leaf: imports no other embedded module |
| `serve_startup.py` | **THE one home for "startup work that must not delay the backend's first serve"** (F-856) — `after_serving`, which runs one idempotent, best-effort startup job on a daemon thread. Its docstring carries the safety argument for reaping orphans CONCURRENTLY with serving. Deliberately not a general background-task runner: `clone_storage.spawn_background_sweep` keeps its own asyncio task, dedupe and trigger-time root capture |
| `proxy_selfheal.py` | **THE one home for "the backend under this proxy died — heal in place instead of exiting"** (F-838) — the backend-generation loop (`drive`, `_one_generation`), the bounded recovery (`heal_backend`, `HEAL_ATTEMPTS`, `MAX_CONSECUTIVE_HEALS`) and the in-flight-call failure report (`PendingCalls`). Also **THE one home for "which witness saw this death"** (F-843) — the generation-end causes (`WATCHDOG_CAUSE` / `CONNECTION_LOST_CAUSE` / `CONNECTION_RESET_CAUSE`) and `_confirm_bridge_verdict`, which settles dead-vs-busy for a bridge-first death with the SAME identity+readiness gate the watchdog uses (handed in as `confirm_alive`); the cause is reported, never branched on — there is one heal path. Also the home for **three of F-827's four proxy lifecycle reports** (`CONDEMNED_EVENT` / `HEALED_EVENT` / `UNREACHABLE_EVENT` via `_report`, which only ever calls `observability.capture_lifecycle`); the fourth (eviction) is reported from its own site in `singleton.py`, `_report_eviction_decision`, which runs AFTER the kill site has answered and names which way it went — a refusal used to ship as an eviction. A leaf: it imports no other embedded module and takes the startup path (`ensure_server_running`) as an argument, so the reuse gate, adoption order and cold-start lock — including the herd's serialization — stay single-homed in `singleton.py`. Never raises. **Since F-889 (c) `drive` NEVER RETURNS** — the exit is deleted, not lengthened. Where it used to return (`heal_backend` gave up, `MAX_CONSECUTIVE_HEALS` deaths back to back, or a generation that never became READY) it now enters bounded backoff — `_retry_delay`, `RETRY_BASE_SECONDS` 2.0 doubling to `RETRY_MAX_SECONDS` 60.0, jittered ±`RETRY_JITTER` because the population this exists for is a FLEET and 114 proxies orphaned by one death would otherwise re-enter `ensure_running` in the same second — and keeps asking for as long as the client's stdio pipe is open. `MAX_CONSECUTIVE_HEALS` is the trigger for that WAIT rather than for an exit; the wait IS the flap remedy, so the streak resets after it. `singleton._proxy_streams` read the old return as "tear down for reconnect", so the proxy process EXITED and Claude Code rendered it as "Connection closed" — for a backend answering in 227 ms, 114 times in one window. **The ONE exit is the client's**: `pump_client`'s EOF cancels the task group from OUTSIDE and that unwinds through `_one_generation`'s `async with`, which is also why `backend_leg`'s two teardown lines are DELETED rather than left unreachable. `NEVER_READY_CAUSE` names the ending that used to be "not an incident" and was therefore the last door the exit had; it is reported, never branched on, like every other cause. `TEARDOWN_EVENT` is GONE (not aliased) — it described a thing that no longer happens, and `UNREACHABLE_EVENT` answers the same question ("how often does stealth still disconnect" → how often a proxy enters retry) with the same two `reason` values plus the first `delay`. **Once per outage EPISODE and never per retry** (F-889 review M2): the series climbs to the 60 s cap and then runs for as long as the client is attached, so one proxy left overnight is ~1400 ERROR events and the population this exists for is 114 of them — that is not a louder signal, it is the quota spent by the machine least able to say anything new. `REACHABLE_EVENT` (INFO) closes the episode with `attempts` and `outage_seconds`; without it every outage that recovered is indistinguishable in the data from one still running. The durable proxy log still records every attempt — it is a local file nobody pays for, and a series climbing toward the cap is what a post-mortem reads. **The proxy's exit is still not this module's**, and since the review there are TWO of them, both outside `drive`: the client's EOF, and `client_presence` noticing that the process which started this proxy is gone |
| `backend_registry.py` | **THE one home for the `server.json` record** — schema **v3** (`SCHEMA_VERSION` = 3, `backends` is a LIST so one display context can hold one backend per IDENTITY, F-886; v2's keyed shape and v1's flat shape still READ, v2's key still authoritative for the context, so an upgrade adopts rather than evicts — `LEGACY_KEYED_SCHEMA` names the one we no longer write), read/write/clear (`read_backends`, `record_backend` — which supersedes by PORT **and** by (display context, IDENTITY), so our own respawn on a new port replaces our own entry while a stranger's is KEPT; the digests are compared through `fingerprint_mismatch`, never `==`, so F-829's unreadable sentinel keeps its one meaning; `forget_entries` — F-880's "drop THESE entries", which RE-READS the record and matches on display context + port + pid so a context re-recorded since the caller probed is kept, and which writes nothing when nothing matched; whether an entry deserves forgetting is `backend_liveness`', never decided here. **`forget_backend` is DELETED** (F-886): once a context can hold two clients' backends, "drop this whole context" is no longer a statement anyone means, and `stop_backend` — its only caller — stops ONE port and now forgets exactly that entry through `forget_entries` — `clear_record`), the adoption order (`adoption_candidates`, `window_capable_first`), per-context port lookup (`port_for_context` / `own_or_first_port` — OUR OWN entry on that context when it holds one, its first entry otherwise; both take a REQUIRED `matches` predicate, which both callers fill with `singleton._identity_matches` so the restart SEED and the port SELECTION cannot ask different questions. Ours-first is not a nicety: since v3 the first entry on a two-identity desktop is the STRANGER's by construction — it was recorded first, which is why we stepped aside from it — and targeting it made `restart` walk away from its own backend, spawn a third on an OS-assigned port and leave the wedged one unreachable by every verb. The first-entry fallback is still the SEED it always was, since selection re-tests it; `port_conflict`), the entry normalizers (`read_record`, `first_backend`, `backend_on_port`, `recorded_int`, `fingerprint_mismatch` — the one reading of a recorded source fingerprint, F-829), plus `STATE_DIR`/`SERVER_STATE_FILE`/`PORT_FILE` — **which four other modules FROM-import, so a new global derived from them has a second home to update** (F-903): `singleton`, `process_cleanup` and `response_handler` each bind `STATE_DIR` again at THEIR import, `singleton` derives `LOCK_FILE` from it, and `settings` recomputes the path a second time because it is a leaf that may not import the package — ten bindings in all, which is why a `setattr` on the name here fences nothing. The suite redirects all ten through `tests/operator_fence.py` (THE one home for keeping a test run out of the operator's REAL directories — BOTH `~/.stealth-mcp` and the browser-session root, one module because they share one filesystem tripwire; installed from `tests/conftest.py` at import time, which is ahead of every fixture of every scope); its enumeration is MEASURED by importing the package and is pinned, so a new derived global fails `tests/test_operator_fence.py` rather than reaching the operator's live record. **The heartbeat SIDECAR is also this module's** (F-889 (b) + review H3) — `HEARTBEAT_NAME` (`heartbeat-<port>.json`, a basename pattern on `browser_pid_registry.RECORD_NAME`'s precedent, so nothing binds a state dir at import time), `heartbeat_path`, `stamp_heartbeat` (the one writer) and `read_heartbeat` (the one reader). **It is a separate FILE and never a field on an entry, and that is the whole design.** The first pass put two optional fields on the backend's own entry and wrote them back through the same whole-record read-modify-write every other writer here uses. Every other writer runs under singleton's cold-start lock; this one cannot, because a backend taking that lock every 3 s would serialise itself against every proxy start on the machine. Unlocked, that read-modify-write is a lost update by construction, and both harms were MEASURED: a sibling's `record_backend` landing in the window written back out of existence, and a `forget_entries` undone from the pre-forget snapshot. A per-port file has exactly ONE writer — the backend on that port — so there is no merge to get wrong and no snapshot to hold; `stamp_heartbeat` never opens `server.json` at all (pinned by making every door onto it raise) and is unconditional, because resurrection is prevented at the READ instead (`self_report` starts from the entry and requires the recorded pid to match). `forget_entries` unlinks a forgotten entry's sidecar and `record_backend` unlinks a SUPERSEDED entry's (F-889 review N5) — both doors out of the record, so a sidecar always leaves with its entry; the port being recorded is spared, because a re-record on the same port describes the same listener and dropping its stamp would blind the watchdog for a whole staleness window. `SCHEMA_VERSION` does NOT move and there is no new field anywhere: the record is byte-for-byte what 2.1.9 reads and writes, and bumping it would make an upgrade evict where it should adopt. `_commit_json` is the ONE atomic write both the record and the sidecar go through |
| `display_context.py` | **THE one home for "can a window launched by THIS process be seen, and on which desktop"** — the opaque token (`display_context()`: `headless` / `unverified` / `win-session-N` / `wayland-…` / `x11-…` / `aqua-<uid>`) and `can_show_windows()`. Observational only: it never picks or enters another session (F-808) |
| `desktop_launch.py` | **THE one home for "hand a launch to the logged-on user's desktop"** (F-810, Windows only) — the availability probe (`available`, `_active_console_session_id`), the two predicates the guard and the spawn pipeline consult (`can_deliver_headed_window`, `should_delegate`), the Task Scheduler round trip (`launch_and_attach`, `_schtasks`) and the attached-browser process shim (`pid_shim`). The OS places the process; the tool still never picks a session. **The host-and-port pair that makes nodriver connect instead of spawn is no longer spelled here** (F-888): it is `cdp_attach`'s, and this file is one of its two consumers, so a delegated headed launch and a backend adopting a browser after a restart cannot drift on how a running browser is entered. What stays is the fact only this module needs — on the attach path `browser_args`/`user_data_dir` in the config are IGNORED, so they must ride on the launcher command line. Being THE `schtasks` seam it is also **THE one home for what `schtasks` will STORE of a `/TR`** (F-879): `TR_MAX_CHARS` (253, measured — not the documented "~261"), `TOKEN_CHARS` (12) and `tr_overflow`, the one home for the COMPARISON as well as the number, so neither composer in the tree can drift on the cap or on its inclusive boundary. `_tr_command` is this module's ONE composition site and cannot hand back a command that would truncate — it raises `ToolError` naming the cap, the actual length and the state dir that is long, and it is called BEFORE the launch dir is created, so an impossible layout costs no directory, no task and no 20 s deadline. It RAISES where `backend_launch` drops a rung, because a delegated headed launch has no fallback and a backend spawn does. What rides in `/TR` is the interpreter plus ONE path; Chrome's own argv lives in the launcher SCRIPT, which is why a pathological profile path costs the budget nothing. **It now has a second consumer**: `backend_launch` reaches in for `_schtasks`, `_cleanup`, `_read_pid`, `_active_console_session_id`, `_NO_CONSOLE_SESSION` and the `/TR` budget on its scheduler rung (F-867/F-879), so a change to any of them moves the backend's cold start as well as a headed browser spawn. **nodriver and `requests` are imported LAZILY, inside the two delegation functions that use them**, precisely so those seams stay cheap for `backend_launch`: the stdio proxy never imports `browser_manager`, so a module-level nodriver import here landed in every proxy's cold start to serve a browser path the proxy never takes — ≈ 175 ms warm to reach a seam now, down from ≈ 470 ms (`-X importtime`) |
| `browser_pid_registry.py` | **THE one home for `browser_pids.json`** — its schema, the owner stamp on every entry (`owner_pid`, `owner_create_time`), the record's FILE NAME (`RECORD_NAME`, just the basename — every public function still takes the whole path, so nothing binds a state dir at import time; it is here because `process_cleanup` and `backend_eviction` both join it to one and a literal in both is a second place to rename it from), and the read-merge-write protocol every writer shares — and, since F-888, **the cross-process CLAIM** (`claim_browser`/`release_claim`/`Claimed`, plus `owner_identity`, the stamp's one source now that two writers need it). A claim is the decide-and-stamp an adoption takes BEFORE it opens a CDP connection, inside ONE `update_entries` mutate so the check and the write share the record's file lock; the decision made in there is RETURNED rather than re-read afterwards, because a re-read once the lock is released is strictly weaker — a third backend's legitimate later write could make our own successful claim read as a failure. Keyed on the PID and never on the instance id: the id is not stable across `browser_reattach`'s two entry points, so two backends racing for one Chrome would mint two different ids and both "win". Since F-888 the entry also carries **`cdp_port`**, the browser's DevTools port — the one fact a LATER backend needs to reach a browser this one spawned, and the one that existed nowhere but `browser.config.port` in the process that dies; `new_entry` writes it and `normalize_entries` copies it in the SAME commit (the two halves this module's docstring says must never drift), and an entry without the key reads as `None`, which means "ask the other two witnesses", never a lie. Also **THE one home for "does this entry's profile directory outlive its browser"** (`on_persistent_profile`: `uses_custom_data_dir is True and not auto_clone`) — it was written out by hand in three places in `process_cleanup` and the three have to agree, because a directory spared from deletion whose entry is dropped anyway is a live Chrome nothing can find again, and a browser spared at shutdown whose directory is then deleted is F-888's incident with an extra step; a legacy entry carrying neither key reads as NOT persistent, which is the safe direction (a temp profile reclaimed, never a named profile deleted). And **THE one home for "is this a usable TCP port"** (`valid_port`, `recorded_port`) — three witnesses ask it about three shapes (this record, Chrome's `DevToolsActivePort`, a `--remote-debugging-port` argument) and a second copy of the range is a second place it drifts; `bool` is excluded EXPLICITLY, because it is an `int` subclass and a hand-edited `true` would otherwise read as port 1 |
| `tool_registry.py` | `SECTION_TOOLS` + `ToolRegistry.section_tool` (registration, section gating, correlation-id stamping, the surrogate-safe return wrap) + the canonical **verb taxonomy** (module docstring) |
| `tool_runtime.py` | **THE one home for what a tool body reaches for beyond its own arguments** (plan_SERVERSPLIT slice 0) — the four constructed singletons (`browser_manager`, `network_interceptor`, `dom_handler`, `cdp_function_executor`), the re-exported module singletons (`profile_seed` joined them at F-896, because a tool body now asks it for the shared session's NAME rather than spelling `"default"` a second time), the four tuned knobs (`CDP_OPERATION_TIMEOUT`, `MAX_TIMEOUT_MS`, `EXECUTE_SCRIPT_TIMEOUT`, `MAX_USER_SCRIPT_BYTES`) and the guards that enforce them (`_BLOCKING_SCRIPT_PATTERNS`, `_script_rejection_reason`, `_clamp_timeout`, `_with_cdp_timeout`). **`_with_cdp_timeout` CANCELS the operation it bounds, and must keep doing so** — the caller gave up, so the rest of a multi-step body has to stop; that contract is pinned on the wire (`tests/test_wire_semantics.py`). It is deliberately NOT shielded: F-883 B1 shipped a shield here briefly (`529cec0`, reverted) and it protected the in-flight send by detaching the whole operation, so a cancelled `navigate` navigated anyway. Listener safety is one layer down, in `cdp_transport` — see that row. This module is also **THE one call site of `cdp_transport.install()`** and, since F-912, of **`element_box.install()`** — two seams, deliberately not one, because one is about the CONNECTION and the other about an ELEMENT (see those rows) — placed with the singletons because they have their shape (once per process, before any tool body runs) and because this module is loaded once where `server.py` is executed three times. A leaf — it imports no `mcp`, no `registry`, no `server`. Being a normal module it is loaded ONCE, unlike `server.py`, so a body that resolves `rt.<name>` at call time has exactly ONE patchable home (`tests/conftest.py`'s `patched_server`); `__all__` states that surface. **This is now the ONLY home**: all 94 tool bodies reach these as `rt.<name>`, and so do `server.py`'s own non-tool readers (`app_lifespan`, the four `@mcp.resource` handlers, the `__main__` block). The migration alias block that re-exported them on `server` is gone, and `tests/test_tool_sections_contract.py` fails if one comes back — an import alias binds the object at import time, which is how a `setattr` on `tool_runtime` would silently stop reaching half its readers |
| `cdp_transport.py` | **THE one home for "DELIVERING a CDP reply must not be able to kill the listener"** (F-883 B1, with F-788/F-794; widened by F-902) — `install()`, `installed()`, `CdpReplyError`, and nothing else. One sentence holds it: the listener task is the only thing that resolves every future and dispatches every event on a connection, so handing it a reply must not end it. **The scope word is DELIVERY and it is exact, not modest** (F-902 review S1): what is covered is everything from the moment `_listener` holds a `Transaction` — `tx(**message)` and the `await`. TWO raises remain on that same result path and still end the listener (measured): `json.loads` (`connection.py`:440) and `mapper.pop` (:443). They are named rather than fixed because **this seam cannot reach them** — both run before a `Transaction` exists, and there is no seam on `Connection` at all, since `CantTouchThis` refuses every class-level assignment (the same fact that put half 1 on `Transaction.__await__`); guarding them would mean replacing `_listener`, i.e. a double of the library's dispatch loop in the hot path of every message. Neither is reachable from a real Chrome: a `json.loads` failure is a malformed frame rather than a reply, and a `pop` failure is a reply for an id we never sent. **Two ways in were found and both land here, as three halves of one idempotent `install()`** — deliberately not a second module for THIS question, the CONNECTION's, which would be two answers to "handing the listener a reply must not end it" one import apart. **The rule is one home per QUESTION, not one nodriver patch in the tree**, and F-912 is the worked example of where the line falls: `element_box.install()` is a second nodriver patch called from the same `tool_runtime` line, and it belongs beside this one rather than inside it because its subject is an ELEMENT's box and nothing about a reply, a `Transaction` or the listener — fold it in and this file's headline stops being true, which is the narrowing F-902 review S1 already made once. `browser_connect` is the third, installed at its own site for its own question. What must NOT happen is a fourth module about the connection. **Half 1 (F-883)** — the reply's own AWAIT owned it: cancelling a Python await never un-sends a command Chrome already has, so the await must not own the reply. `nodriver`'s `Connection.send` registers a `Transaction` (a bare `asyncio.Future`) in `Connection.mapper` and then `await tx` with NO `finally`; cancel the coroutine suspended there and the future is cancelled while still registered, so when Chrome answers, `_listener`'s unguarded `tx = self.mapper.pop(id); tx(**message)` reaches `set_result` on a cancelled future and the `InvalidStateError` **ends the listener task** — the one task that resolves every future and dispatches every event on that connection. Every later call on that tab then hangs to its own timeout and the operator is told the browser may have crashed, about a browser that is fine. `install()` wraps `Transaction.__await__` in `asyncio.shield`: the cancellation lands on a throwaway outer future, the registered one stays pending exactly as the listener expects, and the `CancelledError` still arrives AT that await so the BODY still stops. **`Transaction.__await__`, never `Connection.send`**: `Connection`'s metaclass (`CantTouchThis`) raises `SettingClassVarNotAllowedException` for any class-level assignment, wrapping `send` costs a task per command, and a dunder is looked up on the TYPE — so one assignment covers nodriver's own sends (`Tab.evaluate`, `Element.apply`, `Browser.update_targets`, `Tab.close`), none of which pass through a seam of ours. Deliberately NOT: popping our `mapper` entry (the listener's `pop` is unguarded too, so that is a `KeyError` one line earlier), guarding `set_result`, or replacing `Transaction` (a double of a library object in the hot path of every command). It adds no deadline. It costs one `mapper` entry and one pending future per abandoned reply until Chrome answers — the residue 2.1.8 already had, minus the dead listener. **Half 2 (F-902)** — the reply's own PARSE was unguarded: `Transaction.__call__` drives the generated CDP generator and `_listener` calls it BARE, in the same `else:` branch, so a `from_json` reading a field Chrome had retired raised inside the listener and the popped transaction was never completed either (the call HANGS rather than raising). Measured on Chrome 153: one `get_cookies` on a page with one cookie killed the tab. So `__call__` is wrapped and a parse failure completes THAT transaction with `CdpReplyError` — the one unreadable command fails, every other pending future still resolves. A transaction already `done()` is LEFT ALONE, because `set_exception` on one raises the `InvalidStateError` half 1 exists to stop; half 2 therefore also absorbs half 1's crash, and neither is redundant (the guard alone DROPS the reply, half 1 leaves it deliverable — pinned, and said so in the sensitivity node). **Its report is SHAPE ONLY and that is a PII rule**: nodriver's re-raise interpolates `response['result']` — the WHOLE reply — so the string that killed the listener carried every cookie NAME and VALUE on the page and escaped as an unretrieved task exception, i.e. the asyncio handler, the durable log and Sentry at once. The replacement names the CDP method, the exception TYPE, the reply's field COUNT, and a missing FIELD only when `_missing_field` proves the failure named nothing else; constructed OUTSIDE the `except` so not even `__context__` carries the payload out, and `CdpReplyError` is deliberately NOT a `ToolError`, because that class is what `expected_events` DROPS from Sentry and a reply we cannot read must ship. **Three claims there are PREMISES, and each is pinned rather than asserted** (F-902 review S2/S3): the field name is only proven IDENTIFIER-shaped — a cookie name is frequently that shape too — so the step to "protocol field" rests on every generated `from_json` indexing with a LITERAL, measured true across nodriver 0.47's 810 parsers by an AST scan that fails on a computed lookup; the raw reply is a LOCAL of the guarded frame, kept out of a serialised event because the error is STORED and never RAISED there (a stored exception contributes no traceback frame at all — measured, and pinned as an absence, since a refactor to `raise` would end it silently) and because `response` is `del`eted before the error is built; and the outermost layer is `observability.py`'s `include_local_variables=False`, which belongs to ANOTHER module, so flipping it now fails a pin in THIS file rather than quietly widening what a parse failure can ship. **Half 3 (F-902)** — `_RETIRED_COOKIE_FIELDS` (`{"sameParty": False}`), supplied at `Cookie.from_json`: half 2 makes `get_cookies` fail honestly, half 3 makes it WORK. A NAMED tolerance carrying its measurement, never "fill in whatever is missing" — an invented `sourcePort` is a lie a caller could act on where a DELETED field has one meaning left — and patched at the classmethod rather than the three product call sites because that one method is also what `Storage.getCookies` and the four cookie-carrying Network EVENTS reach (the event path was already guarded, so those were silently DROPPED rather than fatal). Its one named cost: `to_json` still writes the field, so a Chrome 153 cookie reports a synthesised `sameParty: false`. Each half states its own DELETE condition — a nodriver bump that removes the registration in a `finally`, guards the result path, or makes `same_party` optional. Idempotent per half (`server.py` is executed three times under runpy) on `session_hygiene.install()`'s precedent, and called from exactly one place, `tool_runtime`'s module body. A leaf: it imports `nodriver` and nothing else of ours |
| `element_box.py` | **THE one home for "an element with no box is reported without quoting the page"** (F-912) — `install()`, `installed()`, `ElementBoxError`, and nothing else. `nodriver/core/element.py`:499 is the only raise in the library that interpolates a live `Element` (AST census of all 37 raise sites), and `Element.__repr__` renders the tag, **every attribute as `name="value"` and the whole recursive descendant TEXT** — so `could not find position for <input type="password" value="SECRET">` LEFT THE PROCESS — into the backend log at DEBUG through `click_element`'s relay (`dom_handler.py`:276) and into **Sentry**, which does NOT drop it, because `get_element_state` (:606) raises its `ToolError` from inside an `except` and `expected_events`' error-convention rule tolerates only a timeout or a cancellation behind ours. The CLIENT leg is a change of SHAPE and not of disclosure — `get_element_state` returns `attributes` (with `value`), `text` and `text_all` on the SUCCESS path by design (:578-586) — which is why the two sinks nobody asked for are the finding. **Reachable, unlike F-907's three WARNINGs**: measured on Chrome 153, `DOM.getContentQuads` answers an EMPTY LIST rather than an error for `display:none`, for an `<option>` inside a `<select>` and for a detached node, while `visibility:hidden`, a zero-size box, an empty inline and `content-visibility:hidden` all answer one quad and never reach it. The guard wraps `Element.get_position` and is **keyed on `type(exc) is Exception`** — the bare builtin, exactly — never on the message, because a library may reword its own sentences (F-907 §3) and a text key fails OPEN; everything else propagates UNCHANGED, since a `ProtocolException`'s text is Chrome's own diagnostic and an `AttributeError` is what `mouse_click` catches by design. Fixed **at the raise** because that is the one moment the element is an OBJECT rather than a string — F-907's residual 9 names this home in so many words ("shape-only at the source", on F-902's `cdp_transport.CdpReplyError` precedent) — and that is also why `click_element` could not be fixed at its own site: it never calls `get_position`, nodriver does, from inside `mouse_click`. The replacement is built by `logging_setup._shape` and by **no second shaper** (`backend_launch`'s reach into `desktop_launch`'s `schtasks` seam; one way only, since `logging_setup` runs in the stdio proxy and `_shape` is duck-typed so it never imports nodriver), and it is raised **OUTSIDE the `except` block** rather than with `raise … from None`: every reader measured (stdlib `traceback`, `sentry_sdk/utils.py`:798-801 and :880-916, `observability._exception_chain`) DOES honour `__suppress_context__`, so `from None` would have closed all three — leaving the handler first makes the `__context__` ABSENT rather than suppressed, which nothing reading the attribute directly can opt out of, and it costs one line's placement. Cost, measured: +0.113 µs per call (+5.4 % in-process), against 430.9 µs median for the same call over real CDP. `ElementBoxError` is deliberately NOT a `ToolError` — that class is what `expected_events` DROPS, and this is a library-level condition each of the three call sites already converts under its own policy. Its own module rather than a fourth half of `cdp_transport`, whose question is the CONNECTION and whose headline was narrowed deliberately; `browser_connect` is the precedent for a second nodriver patch. A leaf: `nodriver` + `logging_setup`. **DELETE it** on a nodriver release whose `get_position` reports a missing box without interpolating `self` |
| `tool_sections/` | **THE one home for the 94 tool bodies**, one module per `SECTION_TOOLS` section. `__init__.py` owns the contract (a section module exports only `SECTION` + `TOOLS`, **never** decorates, **never** imports `mcp`/`registry`/`server`, resolves everything as `rt.<name>` at call time) and `SECTION_MODULES`, the one enumeration `server.py`'s binding loop and `tests/source_scan.py` both walk. Adding a tool means adding a function to a section module and to its `TOOLS` tuple — never a decorator here, and never a body back in `server.py`. **All ELEVEN sections: `browser_management.py` (8 tools), `element_interaction.py` (12), `network_debugging.py` (10), `cookies_storage.py` (3), `debugging.py` (5), `tabs.py` (5), `element_extraction.py` (9), `progressive_cloning.py` (10), `file_extraction.py` (9), `cdp_functions.py` (13), `dynamic_hooks.py` (10) — 94.** A module owns a WHOLE section, which is why `debugging.py` also holds `validate_browser_environment_tool` — filed among the element-extraction bodies in `server.py`, registered into `debugging` all along — and why `element_extraction.py` holds `extract_complete_element_cdp`, filed among the file-extraction bodies and registered into `element-extraction` all along. `dynamic_hooks.py` is the only module with SYNCHRONOUS bodies, which is what proves `tool_registry._surrogate_safe_returns`' sync branch survives the move. A section module also owns any constant only its own bodies read, which is why `_CAPTURE_OFF_NOTE` lives in `network_debugging.py`. `cdp_functions.py` is the one section a runtime GATE switches off (`--xpool-safe` / `--disable-cdp-functions` / `XPOOL_SAFE_MODE`): both gate sites stay in `server.py` and both still run AFTER the binding loop, because `apply_disabled_sections` removes tools that are already registered. `browser_management.py` owns the browser's own lifecycle — `spawn_browser` with THREE pre-flight guards outside the try, in this order and not another: **`rt.clone_storage.require_allowed_user_data_dir` FIRST (which since F-896 is also where `session` and `user_data_dir` become ONE request and where its ANSWER — the anchored directory — is bound back onto `user_data_dir`, so every line below sees one value), then `require_allowed_seed_from` against that answer (F-897 — it has to sit AHEAD of the re-attach below, because a session whose browser is still running is a session that EXISTS, so `--from` would otherwise be silently dropped by an adoption in exactly the case a caller wants to be told about; since F-898 this body takes ONE `cookie_handoff.driven_profiles` snapshot just above that guard and passes its `holds` predicate to BOTH the gate and the resolver, so the pre-flight refusal and the copy cannot disagree about whether the source is ours), then the F-808/F-810 headed-visibility guard** (F-894 review M1 + m9 + its round-3 CI red — the re-attach below matches the requested directory against live browsers, so a reserved PATH had to be refused in front of it rather than in the resolver behind it; a caller-input refusal raised inside the try would be re-labelled `Failed to spawn browser: …`, which is the wrong word for a request we declined to act on; and the CALLER-shaped refusal outranks the HOST-shaped one, because a reserved path is refused on every machine while "no desktop here" is a fact about this backend — with F-808 first, every headless cell answered the display-context message for a reserved path and the reservation was unreachable there. Neither guard has a side effect, so the order decides only which message is sent, and it is pinned), the **F-888 re-attach branch placed BEFORE profile selection** (selection is where F-871's walk to `<name>-2` happens, so a live browser already holding the requested profile has to be caught in front of it; **since F-931 it is asked about the directory the SELECTION WILL LAND ON — `user_data_dir or clone_storage.master_profile_dir()` — and never about the string the caller typed**, because `require_allowed_user_data_dir` answers None when nothing was named and gating the re-attach on that answer meant `spawn_browser()` could not adopt the browser `spawn_browser(session="default")` adopts on the very same directory: two spellings of one profile with two outcomes, convention 4's second way, and the one that lost is the one the owner and every integration test makes. The `master_profile_dir()` call is a READ of `clone_storage`'s one home, never a second decision about where the shared session lives, and what it costs is one process-table walk on the unnamed path — exactly what the named path has always paid; the whole decision is `browser_reattach.adopt_held_profile`, which never raises and answers None for every ordinary spawn, and `_adopted_instance_record` composes the SAME five keys a spawn returns — plus interception setup, because an adopted tab carries none of this backend's handlers and a caller's `block_resources` would otherwise be silently dropped), its retry budget (`_SPAWN_ATTEMPTS`, named because the LAST attempt is a DECISION: it must not ask `clone_storage._fallback_profile_selection` for a re-selection nothing will ever drive, which for a cloning role copied a whole profile tree and left it `_protect_clone_dir`-ed for the process lifetime — this handler had already run for it and `close_instance`, the only other release, never would; the handler `continue`s so the loop's `else` stays THE one exhaustion raise and the joined error text is unchanged), **`_seed_cookies_over_cdp`, the F-898 hand-off site** — it runs AFTER the browser is up (there is no target connection before that), reads `clone_storage.LIVE_SEED_KEY` off the selection dict and does nothing at all when it is absent, bounds the whole exchange with the SAME `rt._with_cdp_timeout` every other CDP body uses, and **reports rather than raises**: any failure becomes `seeded_via: "copy"` + `cookie_handoff_error: <shape-only reason>` in `spawn_diagnostics.profile_selection` plus one WARNING line, because the session was created and works — it is only missing the source's cookies, and a `ToolError` there would discard a spawn that succeeded. The warning deliberately does NOT forward `error=`: `exc_info` on a failed `Storage.setCookies` would put Chrome's answer to a command whose parameters were the jar into the durable log. Its counts (`cookies_carried`/`cookies_read`/`cookies_session`/`cookies_partitioned`/`cookies_in_target`) are merged over `_public_profile_selection`'s dict at the ONE site that composes that field, so there is still one shape, and `close_instance`, the other end of the on-disk profile/clone lifecycle — which since F-910 answers a RECORD rather than a bare bool (`closed`, plus `seed_refreshed`: True refreshed, False refused with `seed_error` in `clone_storage`'s own words, **None = not asked** because this close was not of the `default` session, on `profile_seed.seed_changed_since`'s three-value precedent, so "nothing to report" cannot read as "nothing reported" — while `seed_error` ITSELF is conditional and deliberately so, present exactly when there is a refusal to quote, which is a fact about that refusal and not a third value of `seed_refreshed`). It is the one place a caller can learn that the SEED every later session is copied from failed to move: the refusal used to go into a dict this body discarded one line after building it, and the lead's F-910 M3 ruling is that a silent refusal is how a defect stays hidden. Its return schema IS declared in `tests/goldens/tool_surface.json`, so that SOFT golden moved with it (the `_WrappedResult` wrapper goes away — a dict return is not wrapped — which is also the WIRE change); it also owns `list_instances`' THREE record shapes (F-874), which `_live_instance_record` decides one entry at a time: `active` + `partial: false` carries the LIVE `current_url`/`title` from `tab_identity`, `active` + `partial: true` carries `detail_error` plus `last_navigated_url`/`last_navigated_title` and deliberately NO `current_url` key, and `stored` — an instance no longer in memory, so nothing live to read — carries only the `last_navigated_*` pair. **Since F-899 the `stored` branch has no routine producer left in a real backend and it is KEPT anyway**: every path that takes an instance out of `_instances` now drops the store entry in the same breath (`_discard_instance_unlocked`, the spawn-failure pops, and `close_instance`'s Phase 1), and adoption writes `_instances` first and the store last — so a `stored` row means a path popped one and forgot the other, which is the class of bug F-899 was. It is the safety net, not dead code; do not delete the branch because nothing reaches it today. The bound is exactly ONE CDP budget per entry and it sits on the one call that reaches Chrome — `tab_identity.refreshed`'s `Target.getTargets` round trip; the two manager lookups in front of it (`get_active_tab`, `get_browser`) are lock-guarded dict reads and are deliberately NOT wrapped, because a `_with_cdp_timeout` there would claim a CDP bound over something that never speaks CDP and charge the entry three budgets for one round trip. The entries are gathered CONCURRENTLY, so one wedged browser costs one CDP budget for the listing and not one per instance, and its degradation is also written to the durable log (`rt.debug_logger.log_warning(..., error=exc)`, shape-only message + F-869's `exc_info` forwarding) so a real bug inside `tab_identity` is visible rather than a quiet partial row. `tests/source_scan.py`'s floor is now at its FINAL value 13 (`server.py` + `tool_runtime.py` + the eleven), so a module dropped from `SECTION_MODULES` reads as a collapsed set rather than a smaller one |
| `tool_errors.py` | the error convention — `ToolError`, `InstanceNotFoundError`, `_require_tab`, `_require_browser`. `_require_js_value` is THE one place a thrown script becomes the convention, and since F-883 a REJECTED promise arrives there too (`awaitPromise` reports a rejection as the exception it is) — so the detail is CLAMPED (`JS_ERROR_CHARS` = 200, `JS_ERROR_TRUNCATION` = `…`): `Promise.reject(new Error(<anything>))` is page-authored and unbounded, and the message reaches the client, the debug ring and Sentry at once. That 200 is a THIRD copy beside `js_aspect_answer.MAX_ERROR_CHARS` and `page_storage.BLOCKED_REASON_CHARS` on purpose — this file states three times over that it imports nothing from `embedded` (it is why `_require_landing_ok` takes its timeout as a parameter), and the three bounds answer three different questions at three different homes |
| `logging_setup.py` | the observability spine — `resolve_log_dir`, `configure_logging`, `with_correlation_id`, `CorrelationIdFilter`; `with_correlation_id` is also **THE one place a failed tool call is recorded** (`_record_tool_failure` → `debug_logger.log_tool_failure`, F-835) — never add a per-tool `except` that logs; **the one home for log-file retention** — `roll_boot_log` (the launcher-side boot-log rotation, F-830) and `prune_old_logs` + its dead-backend post-mortem exemption (F-840); plus `backend_uvicorn_config` — the one home for the backend's uvicorn run-config (access logging off, graceful-shutdown timeout). Owning log-WRITING means owning what the DEPENDENCIES write, so it is also **THE one home for a third-party logger's LEVEL** (F-906, F-908) — `PAYLOAD_LOG_FAMILIES` (`nodriver`, `websockets`, `sse_starlette`, `mcp.client`), `PAYLOAD_LOG_FLOOR` (WARNING) and `apply_payload_log_floor`, the FIRST statement of `configure_logging`, ahead of the idempotency guard and of everything that can raise `OSError` (a process whose log dir would not open still has stderr and Sentry). **The list is the extension point and a second floor function is the defect** — F-908 added two families and nothing else, because the mechanism is one sentence and it covers any library: an explicit level on the family ROOT is upstream of every sink. The first two libraries interpolate a RAW CDP message into their own text — `connection.py`:445 DEBUG-logs the whole reply, :451 INFO-logs the whole event, `browser.py`:824/:869 DEBUG-log a cookie's name and value outright, `websockets/protocol.py`:609 logs the frame and prints a short one WHOLE — so a page's cookies were one enabled DEBUG record from our sinks. **F-908 is the same door from the OTHER END of one request**: `sse_starlette/sse.py`:362 DEBUG-logs the SSE chunk, and for this backend that chunk is the whole serialised answer to a `tools/call` — `get_cookies`' jar, `get_page_content`'s HTML — because the SDK and FastMCP both default `json_response` to False, nothing here passes it, and `backend_env.scrub` stops an inherited `FASTMCP_JSON_RESPONSE` (F-890) reaching fastmcp; F-906's lines quote what CHROME said, that one quotes what WE said back. Its floor costs even less: `sse_starlette` has no call at WARNING or above anywhere in the package, so nothing is standing behind it. **And one answer has TWO ends**: the backend logs the SSE chunk it SENDS, the STDIO PROXY re-parses the identical bytes and logs the MESSAGE (`mcp/client/streamable_http.py`:218, `:547` the argument-side twin), and both processes call `configure_logging` — so `mcp.client` is capped too, or the same jar still reaches a root handler one process along (measured, F-908 review M1). The entry is `mcp.client` and not `mcp.client.streamable_http` because a full census of the client package found a SECOND renderer of the same shape, `client/sse.py`:114/:137 — loaded transitively by importing the streamable transport, so the logger exists even though nothing here drives it **Which families are OUT is MEASURED and PINNED, not a preference** (`TestTheFamiliesDeliberatelyLeftOut`) — `starlette`/`anyio` log nothing below WARNING at all (AST census, never a grep, whose false negative would read as proof a library is silent); `uvicorn`'s whole-ASGI-message logger placeholders `body`/`bytes`/`text`/`headers` BY CONSTRUCTION and logs at TRACE, below what `basicConfig(DEBUG)` admits, and its access log is already off (F-830); `httpcore`'s two BODY traces set no `return_value`, so their message is the trace NAME (response HEADERS can appear, a tool result cannot); `fastmcp`'s real tool-ARGUMENT line (`server/server.py`:672) is shielded by the library itself — every name is prefixed `FastMCP.` and that root carries its own level plus `propagate=False`, so the BARE `fastmcp` family that does inherit root holds no payload line and capping it would be the rejected `uc` entry spelled differently; and the `mcp` **SERVER** tree renders nothing for a REQUEST — `server/lowlevel/server.py`:676 IS admitted at DEBUG, but for a request `%s` is handed a `RequestResponder`, which defines neither `__repr__` nor `__str__`. **That line's NOTIFICATION arm DOES render its model in full**, so the verdict rests on what a client notification can carry, enumerated from the SDK's own union and pinned (five arms at mcp 1.27.1; the free text in `notifications/progress` and `notifications/tasks/status` is the CALLER's status string, never page content) — a sixth that carries page data goes RED. So capping the `mcp` SERVER family or `fastmcp` would silence those SDKs' own diagnostics and close no door `mcp.client` does not already close. **The first round ruled the whole `mcp` family out on `server/lowlevel` alone and shipped that as a positive claim; it was wrong, and the proxy leak is what it cost** — census the tree a logger actually lives in, not the one you expected the payload in. F-902 measured that no shipped configuration reaches them and that is still true (`--debug` is `debug_logger.enable()`, the in-memory ring, and touches no stdlib level; `STEALTH_MCP_LOG_LEVEL` is applied to `stealth.<role>`, never root) — but the protection was the LEVEL and those loggers carry none, so it was ROOT's to give away, and one caller-side `logging.basicConfig(level=DEBUG)` gave it away: MEASURED in BOTH orders, all SIX pinned payload lines reached a root handler (hence stderr, hence `backend-boot.log`) and the INFO one reached Sentry, whose `LoggingIntegration` breadcrumb handler sits at INFO. Six is the count the matrix actually drives (`PAYLOAD_MARKS`) and it is derived from that dict, not typed twice — F-906's four plus F-908's two ends. The floor is set EXPLICITLY on the family ROOT because `basicConfig` only ever sets root's level and `getEffectiveLevel` stops at the first ancestor holding one — so ours wins either way round, and that is ALSO why this is the whole fix: Sentry patches `logging.Logger.callHandlers`, which `Logger.handle` reaches only for a record `isEnabledFor` already admitted, so a level is upstream of all four sinks at once and a `before_breadcrumb` rule beside it would be a second home for one decision. Deliberately not a `logging.Filter` — `connection.py`:451 pre-interpolates with `%`, so the payload is in `record.msg` with `record.args` EMPTY and a filter could only pattern-match text nodriver may reword. WARNING is not a new policy but the effective level every shipped config already had, which is why nothing an operator sees changes and why nodriver's real diagnostics (`connection.py`:483 names the callback and the event CLASS, never the message) still pass; `websockets` is capped too because capping `nodriver` alone leaves the same payload reachable one layer down; and there is deliberately NO `uc` entry — that is only this codebase's import alias, both libraries build loggers from `__name__`, and capping a name no logger has would be a claim the evidence does not support. The residual is named: a caller who writes `logging.getLogger("nodriver").setLevel(DEBUG)` still gets DEBUG, because asking for one library's payloads BY NAME is a different act from turning DEBUG on globally. **The floor is NOT a completeness claim, and the one line it sits BELOW is named rather than implied**: `element.py`:537/:624/:633 interpolate an element at WARNING, and `Element.__repr__` renders descendant TEXT (a text node returns `node_value` bare) — reachable with NO `basicConfig`, through `lastResort` into `backend-boot.log` and a Sentry breadcrumb. That is F-907's, not this row's, because raising the floor over it would silence nodriver's real diagnostics; and F-907 has a tool F-906 did not — those three pass the element through `record.args`, so an args-side rule is available where `connection.py`:451's `%` pre-interpolation made one impossible. **And the half ABOVE that floor is this file's too** (F-907) — `PAYLOAD_ARG_PACKAGE` (`nodriver`), `SHAPE_MAX_ATTRS`/`SHAPE_MAX_NAME_CHARS`/`SHAPE_OVERFLOW`, `_shape` and `install_payload_arg_redaction`, the SECOND statement of `configure_logging`, beside the floor and for the same placement reason. `element.py`:537/:624/:633 log `"could not calculate box model for %s"` with a live `Element` at **WARNING**, and `Element.__repr__` renders the tag, **every attribute as `name="value"` AND the element's whole recursive TEXT CONTENT** (F-906 §5 said "tag and attributes"; the text half is larger and was not known then), while `Tab.__repr__` renders `target.url` with its query string — so unlike F-906, which needed a caller's `basicConfig`, such a record reaches stderr (hence `backend-boot.log`) AND a Sentry breadcrumb **in the plain shipped backend and proxy**, with a password field's `value=`, a `data-*` session token and the page's text in it — and it needs NO handler of anyone's, because production root carries none and the stdlib's own `logging.lastResort` (an `_StderrHandler` at WARNING) carries it instead. A pin measures that configuration with the root handlers STRIPPED, because a capture handler on root SUPPRESSES `lastResort` and a harness that installs one measures the caller-has-a-handler world exclusively; it is pinnable at all only because `lastResort.stream` is a property read at EMIT time, where `basicConfig`'s `StreamHandler` binds `sys.stderr` at CREATION time and a `redirect_stderr` around the emit would read a real leak as absent. **What those three sites are NOT is reachable in nodriver 0.47** — measured, and the correction moves this finding's severity DOWN: `Position.center` is a 2-tuple and so always truthy (`(0.0, 0.0)` for a zero-size box at the origin), so `if not center:` cannot open, and the other two ways out of `get_position()` — `element.py`:499's raised `Exception` and the `except IndexError` branch's `None` — both leave `mouse_click` before the warning line, so this ships as insurance and as correctness for any FUTURE nodriver WARNING that renders an object; the reachability premises are pinned, so a bump that makes them live is a RED test rather than a Sentry event. What IS live on that path is `element.py`:499's `Exception("could not find position for %s " % self)`, measured to carry the whole repr — an exception, not a `LogRecord`, so no logging mechanism reaches it; it is named as a residual, not quietly folded in. **A record FACTORY and not a `logging.Filter`, because both alternatives were MEASURED and neither reaches**: `Logger.handle` consults only the filters of the logger the call was made ON and `callHandlers` inherits HANDLERS, never filters, so a filter on the family root never fires for a `nodriver.core.element` record (pinned, because a fix written that way passes a test that emits on the root and leaks every real line); enumerating descendants covers NOTHING, since at `configure_logging` time no `nodriver.*` logger exists yet (the proxy never imports nodriver, the backend imports it later) and it would need a second install site after the import; and a HANDLER filter is no use in the configuration that leaks, because there we do not own the handler (production root carries none — `lastResort` — and under a caller's `basicConfig` it is theirs). A factory runs inside `makeRecord`, upstream of filters, handlers, stderr and Sentry's `callHandlers` patch at once, covers a logger created LATER (a module a future nodriver adds) and survives `dictConfig(disable_existing_loggers=True)`, which nothing attached to a logger does. **The key is `_carries_payload` and it has TWO clauses, both about the ARGUMENT and neither about the record**: an EXCEPTION is never shaped, whatever package defined it, and otherwise it is `type(arg).__module__` — never the message (nodriver may reword these lines; it may not stop passing an `Element`) and never via `isinstance`, which would need an `import nodriver` in every stdio proxy. The exception clause is not politeness: `connection.py`:483's third argument is whatever the user CALLBACK raised, so its type is the caller's and "all three args are builtins" was never measurable once — a `nodriver.core.connection.ProtocolException`, the commonest thing a CDP-touching handler raises, rendered as its bare type and lost Chrome's `Inspected target navigated [code: -32000]` from the breadcrumb; and that site passes `exc_info=True`, so every sink that formats a traceback renders the text ANYWAY and shaping the `%s` withholds nothing while costing the one sink that formats none. Its named cost is a residual: `ProtocolException.__init__`'s `hasattr(args[0], "to_json")` branch serialises the whole object into `.message` and `tab.py`:1020 raises one from a `cdp.runtime.ExceptionDetails`, i.e. the page's own thrown error — closable only at the RAISE, on F-902's `cdp_transport.CdpReplyError` precedent. **And never gated on `record.name`**: the first write was, so one of OUR records carrying an `Element` leaked the whole repr (measured) — whether a rendering carries page content is a property of the object and the logger it was handed to is not evidence about it, so a `stealth.*` line is redacted exactly as nodriver's is and our own sites' PII discipline (F-869/F-873/F-876/F-877) has this as its FLOOR rather than its substitute; the pin that asserted the exemption is INVERTED, not deleted. Cost, measured: ~350 ns per record for the chained factory CALL (the price of installing any factory) plus ~80 ns per argument for the scan, against ~1.7 us to build a record; a record carrying nothing payload-shaped gets its OWN args tuple back rather than an equal copy, pinned by identity. Redacting is NOT silencing: an `Element` keeps its tag, its attribute NAMES and its child COUNT (`child_node_count`, an `int` a page cannot author into a string) and loses every VALUE and all text — the count is deliberately the ONLY thing said about the children, because `__repr__`'s `content` half renders each descendant text node's `node_value` BARE, which is page text and the larger of the two surfaces — deliberately STRICTER than `click_target.Shape`, which reports id and classes by value, because that module reads a known element through our own code while this is handed an arbitrary object on a third-party line. Everything else keeps its TYPE alone. The rewrite is EAGER and lands a `str`, which costs nothing because a `LogRecord` exists only once `isEnabledFor` admitted it — so the floor takes everything below WARNING and the redaction takes what the floor lets through, and **the two findings compose exactly along that line**. `websockets` is deliberately absent from the redaction (measured: its every WARNING+ site logs a static message or a `str`), and `_redacted` maps the TOP LEVEL of `record.args` only — a nodriver object nested in a list argument is a named residual, not live on any measured line, and recursing an arbitrary argument inside `makeRecord` was declined. `_shape`'s `except Exception` is TOTAL on purpose — both reads run library code inside `makeRecord`, so anything escaping breaks every log call in the process; a narrow tuple was written first and a property's `RuntimeError` walked through it — and it is not a swallow: it names the exception's TYPE in the rendered shape, never `str(exc)`, which on a page-derived object is page-authored. **And the payload lines NO family cap can ever reach are this file's THIRD mechanism** (F-911) — a SECOND RULE inside F-907's one record factory and deliberately not a second install (a factory chain is ordered by install time, so two installs make the outcome depend on call order). What lives HERE is only the rewrite; the table and the two helpers are `payload_log_sites`' — see that row. `mcp/shared/session.py`:383/:384/:430 log with the module-level `logging.warning`/`logging.debug` FUNCTIONS, so the records are the ROOT logger's: no entry in `PAYLOAD_LOG_FAMILIES` can reach them however the list grows (there is no family to name, and capping ROOT is not the missing entry — root's level is the CALLER's), and every line is an f-string, so `record.msg` is finished and `record.args` is EMPTY, which is what F-907 reads. :383 carries pydantic's middle-truncated `input_value=` echo of the caller's arguments and :430 the WHOLE message UNtruncated (the f-string renders the model, not pydantic's error formatter) — **both at WARNING, so this needed no `basicConfig` from anybody**, and the SDK arranges its own sink: `logging.warning` module-level calls `logging.basicConfig()` when root has no handlers, so the first such line installs a stderr `StreamHandler` on OUR root (measured, `[]` → one handler) and the backend's stderr is `backend-boot.log`. The rule WITHHOLDS the rendered text and keeps everything else — same logger, same level, `<mcp/repo-relative/module.py:LINE message withheld: N chars>` — because an operator who knows WHICH line fired can read the SDK's source, while a pre-rendered string has no safe half the way an `Element`'s tag and attribute NAMES are (F-907's asymmetry, deliberate). **The unit is the MODULE and the key is `record.pathname`**: not the text, which the library may reword (F-906's rule); not the line number, the least stable thing in a dependency, where a rule keyed on 383/384/430 goes silently inert on the next release; not `record.name`, which is `root`. A path is what a family root is one level finer, and when the module stops existing the entry is inert and VISIBLE, because `tests/test_root_logger_payload.py` reads the INSTALLED SDK's source by AST. `record.args` is cleared WITH the message or `getMessage()`'s `msg % args` raises `TypeError` in every formatting handler — `session.py`:422 is that shape — and `_withheld` never runs the `%` itself, because interpolating a third party's format pair inside `makeRecord` is the failure `_shape`'s handler exists for. **A root `logging.Filter` WOULD have worked here** — unlike F-907's case the call is ON root, so `Logger.handle` consults root's own filters — and is declined on convention 4 alone: a second mechanism for one question, one import away, covering strictly less. No `before_breadcrumb` either, MEASURED not argued: `makeRecord` is upstream of `Logger.callHandlers`, the one method `LoggingIntegration` patches, so the breadcrumb is built from an already-withheld record — and BOTH halves of that premise are pinned. Scope is that ONE module, from an AST census of all 206 logging calls in `mcp` (11 on root): `client/session_group.py`'s three root WARNINGs are real but nothing here constructs a `ClientSessionGroup` (the `uc`-entry rule), and `server/sse.py`:193 carries a session id. **And what a record carries in its EXCEPTION is this file's FOURTH mechanism** (F-913) — a THIRD RULE inside the same one factory, never a second install, and again only the REWRITE lives here: the two site tables, the structural test and the restatement are `payload_log_sites`'. It is a separate `if` and deliberately NOT part of the site/args `elif` chain, because it replaces a different HALF of the record and a module needing both rules would otherwise silently get one. `mcp/client/streamable_http.py`:240/:394/:574 log a STATIC message with no args at ERROR, so F-907's rule sees nothing to shape, F-911's would withhold the one part that is SAFE, and F-908's WARNING floor on `mcp.client` sits BELOW it — while `exc_info` carries a pydantic `ValidationError` whose `str()` quotes the SSE data, i.e. a tool RESULT on the proxy leg, and `expected_events` recognises none of its five classes, so `LoggingIntegration` ships it as a full Sentry EVENT (the harshest of the four sinks, and the only one of this family still open). **MEASURED, and the stub finding's own sentence was wrong**: pydantic caps each echo by RENDERING length — above 50 it keeps the first 25, `...` and the last 24, i.e. **52 of the rendering** whatever the input type, which for a `str` is 50 characters of the INPUT (24 + `...` + 23; truncation begins at 49), so a whole frame's head is ALWAYS the JSON-RPC envelope and never "a cookie jar's first entries" — what leaks is the frame's LAST 23 characters plus **any value shorter than the cap, echoed WHOLE**, once per union arm (9 errors and **276** echoed characters for one 273-byte frame — 216 was a pre-rewrite measurement and survived into three files). That moves the severity DOWN and the sharpness UP, because a cookie value or a session id is frequently under 50. The rule REPLACES what the record's `exc_info` renders as and never mutates the live exception, because `:241` sends that very object downstream into `BaseSession._receive_loop`; the original TRACEBACK is handed through, so every frame survives — measured byte-identical, and the FRAME LIST is the whole of what that measures: the exception's TYPE and VALUE both change by construction, so Sentry's default stacktrace-first grouping does not move but a strategy or fingerprint rule keyed on type or message does (finding §6.1 residual 5). Keyed on the SITE **and** the exception's structure, never on message text, and never on the TYPE alone: `expected_events.CALLER_VALIDATION` reads that same type off FastMCP's own records, so an ungated rule would stop `caller-input` classifying and re-open the class that was 466 events a week when F-887 measured it. pydantic's own `hide_input_in_errors` was measured FIRST, works, and is rejected — it changes what `:241` sends downstream and covers only the models we enumerate — and NOT on cost, a third ground that was offered and is WITHDRAWN, because the rebuild measures 0.298 ms median cold and a ground that cannot carry its own number does not belong beside two structural ones. `:196`/`:198`'s `Raw result:` pair is adjacent and deliberately OUT of scope: both are f-strings, so their payload is in `record.msg` where a rule about `exc_info` cannot see it, and what they render is the server's own capabilities rather than a tool answer **`_shape` has a SECOND consumer since F-912**: `element_box` reaches in for it to build the replacement text for nodriver's one repr-carrying EXCEPTION, so a change to `_shape` or to its three bounds moves what an operator is told about a boxless element as well as what a log line says. One way only — nothing here may import `element_box`, since this module runs in the stdio proxy and `_shape` is duck-typed precisely so it never needs nodriver |
| `payload_log_sites.py` | **THE one home for "which third-party SITES render a payload, and what a record from one is allowed to say"** (F-911, F-913) — TWO tables and ONE matching rule. F-911's half: `PAYLOAD_LOG_SITES` (today `mcp/shared/session.py` alone), `_SITE_STEMS` (DERIVED from that table, never typed beside it — the cheap first gate, one frozenset lookup per record in the process, against a `record.module` the stdlib already computed), `WITHHELD_TEMPLATE`, `site_of` and `withheld`. It is the SITE half of a TRIO and the trio is the point: `logging_setup` holds a third party's log line down by **LEVEL** (`PAYLOAD_LOG_FAMILIES`, when the library names its own logger), by **ARGUMENT** (`PAYLOAD_ARG_PACKAGE`, when the payload rides in `record.args` as an object), and by **SITE** — here, when neither can, which is exactly what `mcp/shared/session.py` presents by logging through the module-level `logging.warning`/`logging.debug` FUNCTIONS with f-strings. `site_of` makes TWO reads and no third (the stem, then the PATH — several packages ship a `session.py`, and withholding a stranger's records because their file shares a name would be silencing) and has deliberately **no `except`**, which is a CLAIM: unlike `logging_setup._shape` it calls no library code, only attributes `LogRecord.__init__` computed itself, so `record.module` cannot raise and `pathname` is tested for `str` rather than coerced. `withheld` never runs the `%` — interpolating a third party's format pair inside `Logger.makeRecord` is the failure `_shape`'s handler exists for, avoided by not doing it — and it names the exception TYPE, never `str(exc)`. A leaf that decides NOTHING about when it is asked: the record arrives as an argument, there is no install and no state, and `logging_setup`'s ONE factory owns the rewrite (including clearing `record.args`, without which `getMessage()`'s `msg % args` raises `TypeError` in every formatting handler) — `backend_liveness`/`backend_eviction`'s pattern. Stdlib only, so the stdio proxy pays nothing. It is a module rather than three more screens of `logging_setup` because this is the one of the three rules that is a REGISTRY, and its timing was the 1000-LOC budget F-911's addition crossed — which **ratchets down only**, so the cut is this question's surface rather than a raised cap (`cli_call`→`cli_render`'s precedent). If `logging_setup` grows again the honest next cut is the other two rules joining here, making it THE one home for "what a third party's log line may carry" entire. **F-913's half is the SECOND table** — `PAYLOAD_EXCEPTION_SITES` (today `mcp/client/streamable_http.py` alone), `_EXCEPTION_SITE_FILENAMES`, `INPUT_QUOTING_NAMES`/`INPUT_QUOTING_MODULE_ROOTS`, `WithheldInputError`, `restated_exc_info`, `RESTATED_TEMPLATE`, `MAX_RESTATED_ERRORS`, `MAX_LOC_CHARS` — and it is a second TABLE rather than a second entry in the first because the two replace different halves of one record: F-911's withholds `record.msg` and clears `record.args`, F-913's replaces what `record.exc_info` RENDERS AS and leaves the message alone. Adding `streamable_http.py` to `PAYLOAD_LOG_SITES` would delete that file's static, SAFE message and leave the payload untouched in `exc_info` — a change that reads as a fix and closes nothing, which is worse than an absent entry because the next reader believes it is handled (pinned). They are disjoint today and nothing requires them to be; the factory applies them independently. `_site_in` is now THE one matching rule, parameterised over the table, so the separator-BOUNDARY decision (F-911 review S1) has one home and two copies of it cannot drift. `restated_exc_info` is the ONE entry point the factory calls: it answers `None` for everything but a record MADE BY a table site whose exception CHAIN contains a link that quotes its own input, so an ordinary `ConnectionError` out of `:574` keeps its text (F-907's exception clause still stands for every exception that quotes nobody) — and it hands the ORIGINAL traceback through, so the live object the SDK sends downstream at `:241` is untouched and Sentry's default stacktrace-first grouping does not move (the type and value DO change — residual 5). The restatement is built from the library's OWN accessor, `errors(include_input=False, include_url=False, include_context=False)`, never by cutting pydantic's rendered sentence, and keeps the exception's TYPE and MODULE, the MODEL, the error COUNT, the length it replaced and **up to `MAX_RESTATED_ERRORS` distinct `type`/`loc` pairs, with a count of how many more** — a `loc` is a field NAME, F-907's measured safe half — and it is "up to" because the 4-arm union this finding is about OVERFLOWS the cap: 9 errors, 9 DISTINCT pairs, cap 8, so the shipped restatement ends `…+1`. The cap is deliberately NOT raised to fit it (`control_state.MAX_OPTIONS`' precedent — keep the bound, give what falls beyond it its own message). Meanwhile `msg` and `ctx` go, because for a `value_error`/`assertion_error` `msg` IS `"Value error, " + str(the validator's own exception)`. The type is matched by NAME and module ROOT and the accessors are reached through `getattr`, because this leaf DUCK-TYPES rather than imports: `import pydantic` in the stdio proxy is the cost the whole module exists to avoid |
| `process_cleanup.py` | orphan reaping — side-effect-free `__init__`, `activate()` at serve boundary (handlers armed synchronously; the REAP is handed to `serve_startup.after_serving`, F-856), `recover_orphans()` seam. **It no longer kills every browser whose backend is gone** (F-888): at SHUTDOWN (`_cleanup_all_tracked`) and at STARTUP RECOVERY (`_recover_orphaned_processes`) a browser on a persistent profile is left RUNNING and left TRACKED, because those two sites are where a human's logged-in Chrome died when its backend stopped, restarted, healed or crashed. Both ask ONE predicate, `browser_pid_registry.on_persistent_profile`; recovery classifies the whole record in ONE pass before the loop (`browser_reattach.adoptable_for`) so the reaper and the adopter cannot disagree about a browser, and it deliberately does NOT drop a spared entry — the record is the only thing that still names that instance. `force` skips the classification entirely (`kill-orphans --force` has always meant "yes, including a live backend's", and it is now the ONE verb that can still end a human's logged-in browser — RUNBOOK says so at the `--force` paragraph, and so does `cli.py`'s docstring, which counts THREE gates). The classification answers TWO sets, not one: `Classified.adoptable` is "attach to this" and `Classified.spare` adds the entries it could not reason about at all — recovery consumes `.spare`, because conflating the two is how an unclassifiable entry would be handed to the adopter as though we understood it. **Since F-916/F-917/F-918 the three ways this path could kill what it had not established all resolve the other way, and `reap_guard` is where that rule lives**: `.spare` now also holds the entries `_adoptable_entry` answers `reap_guard.UNDECIDED` for; the kill set — which is built by scanning the entry's `user_data_dir` while the spare is matched by `instance_id`, so on a SHARED profile one stale entry's reap reached a browser another entry had just been spared for — is filtered by `reap_guard.spared_pids(saved_processes, spare)`, subtracted at the ONE place `_kill_processes_for_metadata` finally spends it (`protected_pids`), never once per source; and `_kill_process_by_pid` asks `reap_guard.killable` for the verdict it used to reach by falling through a blanket `except` — an unreadable `.name()` (a Windows `AccessDenied`) now answers False instead of logging "Could not verify" and terminating anyway. **F-922 (owner ruling) then narrowed WHICH pids that set may contain at all**: the `user_data_dir` scan is for DISPOSABLE profiles only — a clone directory is ours by construction, a human never opens one by hand — while on a NAMED or shared profile a reap may end only what the RECORD names, because that is exactly what a human DOES open and since F-888 a browser on one outlives its backend. The predicate is `browser_pid_registry.on_persistent_profile` read the other way round (the same one F-888 and `_cleanup_profile_for_metadata` ask; there is no second notion of "ours"), and the recorded pid then supplies the kill through the identity check and start-time fence that were already there. It binds the CLOSE path too, not just recovery, because a named profile is a named profile whichever caller arrived. What it costs is the owner's accepted trade: a leaked Chrome on a named profile whose record entry was lost is never reaped automatically, stays visible in `stealthy profiles` and is cleared deliberately. It also shrinks F-917's surface to the disposable case — do NOT write a `protected_pids` pin on a persistent entry, it passes vacuously (measured). `_skip_note` is the ONE line for a pid a reap declined, four spellings collapsed to pay for these lines (1007 → 1006). `_KILL_RUNGS` is the escalation as ONE table — `process_exit._rung` is the CLOSE path's twin and the two stay apart for that module's stated reason — and collapsing it is what paid for these lines in a file at its cap (1009 → 1007, row ratcheted). What is NOT here: the adoption rule, the endpoint ladder, the browser-side liveness witness and the reap a failed adoption falls back to are all `browser_reattach`'s, which takes THIS object as an argument — this file imports that one, so the seam only works in that direction. `track_browser_process` gained `cdp_port` and is the SECOND write an adoption makes — the first is `browser_pid_registry.claim_browser`, taken before the door |
| `reap_guard.py` | **THE one home for the rule startup orphan recovery kept breaking: an answer we could not ESTABLISH must resolve toward NOT KILLING** (F-916, F-917, F-918) — `UNDECIDED`/`Undecided`, `spared_pids`, `killable`/`Verdict`. Recovery runs on every backend cold start and what it decides about may be a human's logged-in Chrome, so every witness on that path has two ways of answering "no" — the process is provably not ours, which is a DECISION, and the process could not be read at all, which is not one. Three places collapsed those into one value and all three resolved toward the kill. `UNDECIDED` is a distinct TYPE with one instance rather than a second `None`, because one value standing for two statements WAS the defect; `spared_pids` is the bridge between a spare matched by `instance_id` and a kill set built by `user_data_dir`, which cannot be fixed by widening the spare instead (two entries may legitimately share one directory, and a directory-wide spare would stop the record ever shrinking — **which is still true, and F-922 is where the protection it was reaching for actually belongs**: narrow the KILL by profile KIND, not the SPARE by directory, because a record-only reap still ends the entry's own browser and still lets the entry leave); `killable` answers FOUR things where the old code answered two — gone (`NoSuchProcess`, which a zombie IS, measured), ours, someone else's, and **unreadable**, the last of which now refuses instead of logging and terminating anyway. The refusal WORDING rides on the `Verdict` so the message and the decision have one home, and nothing here writes a log line. What the rule costs is named in the module docstring rather than hidden — a browser we can neither adopt nor reap is left running and left recorded — and **how long that lasts depends on WHICH witness could not be read, which this row claimed wrongly until it was measured**: the two shapes decided AFTER `browser_alive` (no recoverable endpoint; persistence the record never stated) are re-classified on every cold start and reaped the moment their Chrome exits, while the two decided BEFORE it (an unreadable `pid`/`user_data_dir`, a missing `create_time`) can never become an established negative, so **those record entries are PERMANENT**. That is the owner's ruling read literally rather than a defect — what we can never establish we can never reap — and bounding it would need a bare-pid liveness check, which `(pid, create_time)` exists to make unnecessary. `browser_pids.json` has no age prune at all (F-928). `kill-orphans --force` skips the classification by design. It is the same trade `profile_lock._browser_pids` (unreadable → HELD; `None` for "could not be asked" is deliberately distinct from `()` for "asked, nothing running"), `backend_eviction` (refuses to evict what it cannot prove idle) and `spawn_leak._started_after` (a pid whose start time it cannot read is spared) already make. `killable` is also what F-919's reap reaches its kill THROUGH, so the two interlock: that fence decides WHICH process a failed spawn may end, this one whether a pid may be ended at all. A leaf: psutil + stdlib; the Chromium-family name test arrives as an ARGUMENT (`process_cleanup._is_browser_process_name`), so there is no second answer to what counts as a browser we launch |
| `models.py` | pydantic data models (`BrowserInstance`, `BrowserState`, `NetworkRequest`, …). `BrowserInstance.last_navigated_url` / `last_navigated_title` are a CACHE and the names say so (F-874): the spawn and `BrowserManager.update_instance_state` (i.e. the `navigate` tool) are their only two writers in the tree, so a page that moved on its own, a `switch_tab` and a late `document.title` all leave them behind. They were `current_url`/`title`, which is how `list_instances` came to report a login page for an instance on a feed — what an instance is showing NOW is `tab_identity`'s answer, never this pair |
| `platform_utils.py` | OS-specific helpers |

**Browser & interaction**
| File | Owns |
|---|---|
| `process_exit.py` | **THE one home for ending a browser's OS process — wait for its own exit, then escalate** (F-910) — `EXIT_GRACE_SECONDS`, `browser_pid`, `wait_for_exit` / `wait_for_exit_async`, `settle`, `terminate`, `report`. `Browser.close` is the START of Chrome's shutdown and that shutdown is what COMMITS the cookie store, so the kill may not be the first thing that happens to a browser we asked to leave: measured, Chrome's process was still `running` at the kill site on **20 closes out of 20** and exited unaided 0.129–0.165 s later, and when the terminate won the race the user's login was gone (~1 close in 30 locally, 2 of 2 on Windows CI). `browser_pid` answers the BROWSER and never one of its children — a profile is held by a whole tree and only the member with no `--type` is the browser (`browser_cmdline.browser_process`' rule from the other side); a pid carrying `--type=` answers `None`, i.e. do not wait, because waiting on a renderer would report "exited" while the browser was still flushing, which is this finding's own defect wearing a fix's clothes. `EXIT_GRACE_SECONDS` = 5.0 is ~30× the slowest measured exit and is a CEILING, not a wait; what it costs is named rather than hidden — a WEDGED Chrome makes one close up to 5 s slower and is then killed exactly as before. `wait_for_exit_async` is bounded TWICE because a worker thread cannot be cancelled, so a close always reaches the kill path. **The wait POLLS and must never call `wait()`, and that is POSIX and not taste**: a browser we launched is a CHILD asyncio is already blocked on (`ThreadedChildWatcher._do_waitpid`'s `os.waitpid(pid, 0)`, CPython 3.13 `asyncio/unix_events.py`:1443, or `PidfdChildWatcher._do_wait` at :990), a child's status can be collected ONCE, and the loser of that race reports **`returncode 255`** with a WARNING — so `psutil.Process.wait()`, which on POSIX reaps through `os.waitpid(pid, WNOHANG)` polling (`psutil/_psposix.py`:62-155), would have made a clean exit read as 255 in `terminate`'s own guard. `_has_exited` asks `is_running()` (which is ALSO the pid-reuse guard: psutil compares the `(pid, create_time)` pair, `psutil/__init__.py`:616-641) and treats `STATUS_ZOMBIE` as exited, because a zombie has committed its cookie store and is waiting only for asyncio's reap. An AST pin forbids `.wait()` in this module, because Windows — where this was written — cannot observe the defect at all, and **the gate's Linux/macOS cells are this module's first execution**. `settle` is the whole of Phase 2b as ONE call (grace, diagnostics, and the kill a CANCELLED close would otherwise skip — Phase 2b sits inside an `except Exception` that a `BaseException` walks past, and the grace widens that window to the ceiling for a wedged browser, so the kill runs synchronously before the cancellation is re-raised). `terminate` is the terminate → kill → SIGTERM ladder, moved here WHOLE from `browser_manager`: a wait in one file and the kill it gates in another is how F-886's rule came to be missing. **Two other files still end browser processes without coming through here, deliberately** — `spawn_leak` (a failed spawn's leftovers) and `process_cleanup` (orphans at startup and shutdown): neither browser was ever asked to leave, so there is no shutdown in flight to wait for, and a grace there would buy a wedged orphan 5 s of a startup for nothing. At SHUTDOWN the cost is named instead of hidden: `close_all` closes SEQUENTIALLY, so N wedged browsers add N × ~6 s to a backend's exit — which does NOT collide with uvicorn's 2.0 s `timeout_graceful_shutdown`, because that budget wraps `_wait_tasks_to_complete()` alone and `lifespan.shutdown()` runs after it with no deadline (`uvicorn/server.py`:279-293, read from source); the closes stay serial because parallel `_blocking_teardown`s mutate the one `ProcessCleanup.browser_processes` dict `_save_tracked_pids` iterates, i.e. a faster exit bought with a corrupted `browser_pids.json`. `report` is the ONE close-diagnostics line (`process_exit.close_exit_wait`: did Chrome leave unaided, and after how long) and it is PINNED rather than trusted, because it is written under a never-raises contract that hid this leaf's own first-draft defect — it imported the debug_logger MODULE where every other file in the tree imports the SINGLETON, so every line it wrote was an `AttributeError`: swallowed in `report`, so the diagnostics did not exist and nothing said so, and NOT swallowed in the ladder, where it would have escaped immediately after a successful `terminate()`. A leaf: `psutil` + stdlib + `debug_logger`; every input is a primitive or nodriver's `_process`, it knows nothing about instances, records or profiles, and it never raises — an unreadable process is one we did not wait for, which resolves onto the behaviour that shipped before F-910 |
| `browser_manager.py` | `BrowserManager` — spawn/list/close instances; `close_instance` offloaded teardown, now FOUR steps — Phase 1 (claim), Phase 2 (graceful CDP), **Phase 2b (F-910: wait, bounded, for Chrome's OWN exit before anything kills it — ONE `process_exit.settle` call, which owns the grace, the diagnostics line and what a cancelled close does about it; it is its own await, so Phase 3 keeps its whole `CLOSE_KILL_TIMEOUT`)** and Phase 3 (blocking kill, whose ladder is `process_exit.terminate`'s now). The three phases as they were: **the in-memory-storage entry is dropped in Phase 1, under the same lock and in the same statement block as the `_instances` pop** (F-899) — that store is a cross-check of `_instances`, so no `await` may separate the two. It used to be Phase 4 bookkeeping six awaits away, inside the `try` whose handler is `except Exception`, which an `asyncio.CancelledError` walks past: a client disconnecting mid-close left the manager without the instance and the store with its entry, i.e. a `source: "stored"` row in `list_instances` for the life of the backend. `close_instance` has exactly ONE removal site; the other `in_memory_storage.remove_instance` in this file is the unrelated stale-instance discard. `_launch_browser` is the ONE function in the tree that launches a browser and it opens with `browser_connect.install()`, ahead of the F-810 delegation branch, because BOTH starts below it are nodriver's `Browser.start` and whose connect budget decides has to be settled before either (F-834 stage 2). `get_page_state` owns the page-state COMPOSITION and its error POLICY, not the storage read (F-869): `page_storage.StorageBlockedError` is the page refusing (INFO, empty dicts, still `partial: false`), and every other exception propagates so `get_instance_state` can answer its `partial: True` + `detail_error` record — the one degradation shape, reached by raising, exactly as this method's docstring always said. `navigate` owns the retry budget (one stale-tab recovery, F-824 — taken only for a `Page.navigate` Chrome never answered; a timeout AFTER acceptance, read off `navigation_milestone.Progress.accepted`, is the page's own and is reported, never retried on a replaced tab, F-881) and the POLICY around the navigation, not its reads: `wait_until` is validated once via `navigation_milestone.require` BEFORE the loop (a typo costs no CDP send), the navigation itself and the wait `wait_until` names are ONE call into `navigation_milestone` under the budget it always had (F-881) — never `tab.get` (nodriver's `Tab.get` is `Page.navigate` plus a `Tab.wait()` that, with the `Page` domain unenabled, is a flat 0.5 s sleep) and never `tab.wait(<event class>)`, which nodriver 0.47 reads as a duration and skips — and where it LANDED is one more call, `navigation_milestone.landing`, under the remaining budget. That read used to be two `tab.evaluate` round trips here (`window.location.href` then `document.title`) and a `meta refresh` fired between them, so the tool answered with the FIRST document's url and the SECOND document's title (F-882, measured); one round trip is what makes the pair a snapshot of one document. The failed-attempt WARNING is also this file's, and it carries `type(error).__name__` plus `Progress.describe()` — accepted / committed / how many later documents superseded ours — because `str(TimeoutError())` is `""` and the shipped line ended at its colon |
| `browser_reattach.py` | **THE one home for "a browser outlived the backend that spawned it — may we adopt it, where is its CDP endpoint, the door back in, and the pass that walks through it"** (F-888). Four parts, one module, because each exists only to serve the first. (1) **The rule** (`adoptable`): four conditions, cheapest first, and each one alone refuses — the recorded OWNER is not a live backend of ours (`browser_pid_registry.is_reapable`; two backends driving one Chrome is F-886's harm reached from the other side), the profile is PERSISTENT (`on_persistent_profile` — a disposable auto-clone is never adopted, its whole contract is to die with its browser; but an entry recording NEITHER key never SAID, and since F-916's named residual that absence answers UNDECIDED rather than "reap it" — measured killing a LIVE Chrome on the shared profile through the recorded-pid fallback, which is not the directory scan F-922 narrowed. The discriminator is `browser_pid_registry.persistence_recorded`, i.e. `uses_custom_data_dir is None`, and that one key is MEASURED rather than chosen: `normalize_entries` preserves ITS absence as None while collapsing `auto_clone`'s to False, and it is also the only one of the pair that can make an entry persistent. `False` stays an ANSWER and is still reaped, and the spare is bought with a POSITIVE liveness witness — both halves of the pid's identity — so a keyless entry whose Chrome is gone still leaves the record), the recorded Chrome pid is still that Chrome, and a CDP endpoint is recoverable. **Failing a condition is two different statements and since F-916 they are two different VALUES**: `None` is a DECISION — a live sibling owns it, its profile is disposable, or its pid is provably not the Chrome recorded — and the reaper may have it; `reap_guard.UNDECIDED` is the absence of one, and the reaper may not. The three that answer UNDECIDED are the ones a PERSISTENT entry can fail because a witness could not be READ (unreadable `pid`/dir, no `create_time`, no recoverable endpoint), and the last of those is the measured population: 2.1.8/2.1.9 recorded no `cdp_port`, so exactly the records holding the stranded logins were being reaped on every backend cold start. "The Chrome this entry names is gone" stays a DECISION on purpose — sparing it too would mean `browser_pids.json` could never shrink — and the psutil `AccessDenied` that predicate folds into its False is caught one layer down by F-918's guard at the kill itself. What sparing costs is a browser we can neither adopt nor reap; `reap_guard`'s docstring names it and why it is the cheaper error. (2) **The endpoint ladder is `cdp_endpoint`'s** since F-916 — its OWN leaf, extracted because this file was at its 1000-LOC cap when the fix needed a third answer in it (999 → 991). Deliberately NOT folded into `cdp_attach`: that module never calls it (both callers are this file's two entry points), and a ladder whose witnesses are a recorded field, a live process's argv and a file inside a profile is about a RECORD ENTRY, not about a websocket. See that row for the three witnesses and why their order is what it is. (2b) **The holder path** (`held_by`, `adopt_held_profile`) — the SECOND entry point into the same rule, and **the record is not its input**, because the browser it exists for has no record entry at all: the owner backend died and the SUCCESSOR rewrote `browser_pids.json` without it, so `run` walks straight past it forever. The witness is Chrome's own process singleton through `profile_lock.profile_hold` (F-871's one home) — for "is this held", never for WHICH process to enter, which is `browser_cmdline.browser_process`' — and the record is consulted only to REFUSE — via `is_reapable`, the same rule `run` asks, so the two entry points cannot disagree — and, when a DEAD owner's entry does name that pid, to donate its instance id. The record read is LAZY (`read_entries` is a callable) because the overwhelmingly common spawn is onto a directory nobody holds and must not pay for it. Deliberately NOT gated on `on_persistent_profile`, which reads a record that is not there: a caller naming a `user_data_dir` by hand IS that declaration. It answers THREE outcomes and they are deliberately distinguishable: an `Adoptable`; `None`, which means nothing holds the directory and is the ordinary spawn with nothing to report; and `Refused`, which means a browser IS there and we did not take it, for one of three named reasons — a live backend of ours owns it, none of its processes is identifiable as the browser, or no CDP endpoint could be recovered. That last one used to be a bare `None`, indistinguishable from an empty directory, which made `spawn_diagnostics.reattach_declined` silent in exactly the case an operator needs it. **A failure here never reaps** — that is the one place it differs from `run`, and the difference is intent: `run` is startup recovery where an unreachable orphan must end somewhere, while here a CLIENT asked for that browser and killing it because we could not attach would be this finding's own harm committed by the fix; every failure returns None and the spawn proceeds exactly as today. (3) **The door is `cdp_attach`'s**, not this module's: entering a browser that is already running is a question `desktop_launch` had too, so it is one leaf with two consumers (see its row). What this module owns is WHEN to knock and what to do with what answers. (4) **The seam and the pass** (`recorded_browser_alive`, `adoptable_for`, `reap_recorded`, `start`, `run`): `run` is driven fire-and-forget from `app_lifespan` on `clone_storage.spawn_background_sweep`'s precedent — it reaches a browser over CDP, a wedged one costs a whole `ATTACH_BUDGET_SECONDS` (15 s), and nothing about readiness depends on it, which is F-856's argument at the exact code path F-856 was about. Adoption registers under the RECORDED instance id, so a client holding an id from before the restart reaches the same browser, and reads the live url/title through `tab_identity` (F-874) because an adopted instance's cached pair is empty. A failed attach falls back to `reap_recorded` — exactly what 2.1.9 did to that entry, with the two keys that spare the DIRECTORY — so every adoptable entry still reaches one of the two ends 2.1.9 had; it hands that reap the **SPARED** pids (`reap_guard.spared_pids`, the same function `process_cleanup` asks) over the SAME entries dict this pass classified — never a second read, and **never `.adoptable`**, which is what it was: built from the adoptable set the subtraction protected the candidates and left every entry F-916 had just spared exposed, so one candidate's failed attach could end a browser this pass had refused to reap, under the OTHER entry's instance id (B1). Two subtraction sites disagreeing is F-917's own shape, re-made on the path F-916 added. **Both collaborators arrive as ARGUMENTS** (`ProcessCleanup`, `BrowserManager`), on `spawn_leak.reap_launched_browsers`'s precedent, which reaches the same private helpers: that is what keeps the graph acyclic, since `process_cleanup` imports THIS module for its two decisions. **The CLAIM is what makes any of this safe between PROCESSES** (`claim`, over `browser_pid_registry.claim_browser`): both entry points stamp this backend as the entry's owner inside ONE locked read-merge-write BEFORE the door, keyed on the PID rather than the instance id (the id is not stable across the two entry points, so two backends racing for one Chrome would mint two ids and both "win"). What this file owns is the DECISION to claim and what the entry says; HOLDING that claim across the attach — the shield, the kept task and the hand-back on every failure path including the budget's cancellation — is `browser_claim.held`, the `async with` `_adopt_one` runs inside, and the two refusals it raises (`Refused` / `Undecided`) are that module's, re-exported here because this is where callers meet them. A lost claim and ANY failure to decide both resolve toward SPARE: `run`'s `except Refused` leaves the entry and the browser alone at WARNING, and the reap is reserved for a browser we CLAIMED and then could not attach. That is also what answers two concurrent spawns onto one held directory: the second reads a LIVE owner and is `Refused`. `_directory_lock` is only the in-process cheap path (per requested DIRECTORY, so an unrelated spawn never waits out a wedged browser's `ATTACH_BUDGET_SECONDS`); `_pass_lock` is the record pass's, and it is NOT about two MCP sessions — `app_lifespan` runs `run` once per process already. `nodriver` is imported LAZILY inside the door, for `desktop_launch`'s reason — the classification half runs on every backend startup and usually has nothing to adopt |
| `browser_cmdline.py` | **THE one home for what a RUNNING browser's own command line says about it** (F-888) — `browser_process` (**which member of a profile's process TREE is the browser**: the one with no `--type`, and nothing else — not the parent pid, since the browser's own parent is a trampoline that has already exited, and not the port, since a renderer carries that too. `profile_lock.profile_hold` answers "is this directory held, and by whom" from a SET, so the pid it names is whichever member iterated first: measured on one real Chrome 153 spawn, eleven processes on one profile — the browser, six renderers, two utilities, a gpu-process and a crashpad-handler — of which only the browser and the renderers carry `--remote-debugging-port` at all. Adopting the named member was therefore a coin flip: a `utility` gave no port and the adoption declined SILENTLY, while a `renderer` would have been adopted and stamped onto `Browser._process_pid`, so the manager would discard the instance the moment that renderer recycled and `close_instance` would kill a renderer and leave the browser running), `debug_port` (`--remote-debugging-port`, and it JOINS the port to the profile by comparing the process's own `--user-data-dir` against the one we recorded: conditions 3 and 4 of the adoption rule are otherwise independent, so a RECYCLED pid landing on some other chrome.exe satisfies "alive + Chromium-family" and would have donated a STRANGER's debugging port — which adoption stamps our ownership over and `close_instance` later kills), `is_headless` (measured, because `BrowserInstance.headless` defaults to False and an adopted instance used to report that default about a browser this backend never launched) and `dead_local_proxy` (a loopback `--proxy-server` nothing answers on — almost always this tool's own authenticated forwarder, which lives INSIDE the backend and dies with it while the launch arg lives on; the test is a CONNECT, never the presence of the flag, because a caller may run their own local proxy that is still up). It exists because a browser that outlived its backend has nothing else describing it: the process that knew these three has exited and the record may never have named it. `psutil` gives a LIST, so quoting is a non-issue, and both flag spellings are accepted (`merge_browser_args` uses one, nodriver the other). **Since F-931 `browser_process` is a two-line reader of `browser_members`**, which answers WHICH of a set are browsers plus the third fact that function cannot report because it answers one pid or None: whether any member's argv could not be READ (`Members.unreadable`, `_still_running` behind it — a pid MISSING from the table contributes nothing, because the scan and this read are two moments and a process that exited between them is an established negative, while one we were REFUSED is not). That third fact exists for `profile_lock._tree_hold`, this module's SECOND consumer: an argv it could not read is not evidence a browser is absent, and a directory shown free on that evidence is two browsers on one profile. The `browsers`/`unreadable` pair is deliberately two fields and not one collapsed answer — `reap_guard`'s distinction at a different witness. A leaf: psutil, stdlib `socket`, and `browser_pid_registry` for what a usable port is and how two paths compare. It decides NOTHING — every judgement is `browser_reattach`'s and `profile_lock`'s |
| `browser_claim.py` | **THE one home for HOLDING the record's claim on a browser across an attach** (F-888 re-review) — `held`, the `async with` an adoption runs inside, plus the two named refusals it is the source of (`Refused` / `Undecided`) and `SETTLE_SECONDS`. `browser_pid_registry.claim_browser` owns the WRITE; what is here is the half that exists only because the thing the claim guards is `await`ed. A claim is taken before a single byte reaches Chrome, the attach can fail, raise or be CANCELLED by its enclosing budget, and on every one of those paths the record must not be left naming this backend as the owner of a browser it does not hold — that state IS F-888's own harm reached from the other side, since our own classification reads a live owner, recovery spares the entry, every other backend is refused, and nothing heals it until this process exits. Three things make it correct and they are here rather than re-spelled at each call site: the claim is taken INSIDE the block (one taken before its handler exists is one nothing releases), it is SHIELDED (cancelling an `await asyncio.to_thread(...)` does not stop the worker thread — it still takes the record lock and still writes), and the TASK is kept rather than just its result, so a teardown can hand back a claim that LANDED after the budget had given up on it (`_landed`, bounded by `SETTLE_SECONDS` = 6.0, one more than the registry's own 5 s lock deadline). `Undecided` is a SUBCLASS of `Refused`, not a sibling, because every caller's handling is identical: a lost claim and a failure to reach a verdict are the same instruction — leave it alone, never reap — and one `except Refused` has to cover both. A leaf: `browser_pid_registry` + `tool_errors`; BOTH record writes arrive as CALLABLES on `backend_eviction.terminate`'s precedent, so `browser_reattach.claim` stays the name the suite patches and this module never learns what an adoption candidate is |
| `cdp_attach.py` | **THE one home for entering a browser that is ALREADY RUNNING, over CDP** (F-888) — `config_for`, `attach`, `attach_reclaiming`, `close` and `CDP_HOST`. One door with two consumers that arrived from opposite directions: `desktop_launch.launch_and_attach`, which must connect to what the Task Scheduler started (F-810), and `browser_reattach`, which connects to a browser whose backend died. **The gate is nodriver's**: setting BOTH `host` and `port` on a `Config` is what makes `uc.start` CONNECT instead of spawn (`browser.py` 371-375 — `connect_existing`, and `create_subprocess_exec` is behind `if not connect_existing`); a pin reads BOTH consumers' source and fails if `config.host =` or `uc.start(` comes back into either. `CDP_HOST` is a constant, not an argument, because both launch paths already fix `127.0.0.1`. `attach_reclaiming` runs the attach as its own task behind a SHIELD: cancelling an await never un-opens a websocket, so a plain `wait_for` around the door leaves a live connection AND a live listener task on a `Browser` nobody references any more — the cancellation lands on the shield, the task finishes, and a done-callback closes what it produced (the closer is held in a module-level set, because a bare `ensure_future` is only weakly referenced by the loop). `close` is **`Connection.disconnect` and never `Browser.stop()`**, which would also TERMINATE the process — and never `aclose`, which does not exist on a nodriver `Connection`: since `Connection.__getattr__` delegates unknown attributes to `self.target`, a guarded `aclose()` is a cleanup that silently does nothing, which is exactly what shipped until a pin asked. Every TAB is a `Connection` too, so it disconnects those as well. A leaf: `nodriver` (imported LAZILY inside each function, for `desktop_launch`'s measured cold-start reason) and `debug_logger` |
| `cdp_endpoint.py` | **THE one home for "where is the CDP endpoint of the browser this RECORD entry describes"** (F-888, extracted by F-916) — `endpoint`, `_port_from_profile`, `DEVTOOLS_PORT_FILE`. The ADDRESS, never the door: going THROUGH a door is `cdp_attach`'s and deciding WHETHER to knock is `browser_reattach`'s; this sits between them. Its own leaf rather than part of either, because `cdp_attach` never calls it — both callers are `browser_reattach`'s two entry points — and because its subject is a RECORD ENTRY where the door's is a websocket. **Three witnesses, in order of trust**: the port the record CARRIES (`cdp_port`, written at track time since 2.1.10); `--remote-debugging-port=` on that pid's command line, which is what nodriver passed it (`Config.__call__` appends the flag from `config.port`) — read through `browser_cmdline.debug_port`, which also JOINS the port to the profile so a RECYCLED pid on a stranger's chrome.exe cannot donate its debugging port; then `<user_data_dir>/DevToolsActivePort`'s first line. **Why that order**: the recorded port leads because a record WE wrote is about THIS instance; the COMMAND LINE outranks the file because it is definitionally the live process's while `DevToolsActivePort` OUTLIVES the browser that wrote it — **measured on the real stranded Seller Central Chrome (pid 115652, `--remote-debugging-port=9223`): the file was ABSENT while the browser ran**, so a file-first ladder would have found nothing. The file still earns its rung: `--remote-debugging-port=0` gives a command line `valid_port` rejects as "not bound yet", and the file is where Chrome wrote the port it resolved that to. The last two rungs are not belt-and-braces — 2.1.8/2.1.9 recorded no port at all, and those are precisely the browsers carrying the stranded logins F-888 exists for and F-916 measured being reaped. Each witness is re-checked as an int in range rather than trusted: the record tolerates a hand edit, a command line is whatever the process says it is, and the file survives its writer. It decides NOTHING about adoption and never raises: an unrecoverable endpoint is `None`, and what that MEANS is the caller's (for `browser_reattach._adoptable_entry` since F-916 it is `reap_guard.UNDECIDED` — spare the browser, do not adopt it). A leaf: `browser_pid_registry` + `browser_cmdline` + stdlib |
| `navigation_milestone.py` | **THE one home for "this navigation has reached the milestone the caller asked for, and where it landed"** (F-881, F-882) — `MILESTONES` (`wait_until` → the `Page.lifecycleEvent` name: `load` → `load`, `domcontentloaded` → `DOMContentLoaded`, `networkidle` → `init`, i.e. the commit, followed by F-787's fixed `NETWORKIDLE_SLEEP_SECONDS`, moved here so `wait_until` has one home and still not a quiescence wait), `require(wait_until)` (the validation, callable before any CDP), `Progress` (`accepted`: `Page.navigate` answered — the line between a stale tab's timeout and the page's own; plus F-882's `committed` and `superseded`, and `describe()`, the ONE phrasing of a failed attempt's reason, which exists because `str(TimeoutError())` is `""`), `navigate(...)` and `landing(tab)`. **Keyed on the FRAME's loader CHAIN, never on one `loaderId`** (F-882): F-881 waited under exactly the id `Page.navigate` answered with, which is right until the document Chrome committed for it is REPLACED before it gets there — a head-script `location.replace`, a JS challenge — and the replacement commits under a NEW id, fires its own `load`, and the wait burned the whole budget on three of ten ordinary sites. So `_Chain` keeps the loaders that committed in OUR frame (a same-origin subframe's events carry the SUBFRAME's `frameId` — measured — and are dropped), extends on a commit for that frame under a new loader **after ours committed**, and is satisfied when `loaders[-1]` — the document the tab is actually showing — reaches the milestone, re-checked after EVERY event so the answer is the first instant it is true rather than a function of event batching. A commit is the name `init` and **never `commit`**: `Page.setLifecycleEventsEnabled(true)` REPLAYS the current document's whole lifecycle under that name (measured, Chrome 152), and it is sent immediately before `Page.navigate`, so the replay always describes the page being LEFT. The response and the new document's events are still NOT ordered (measured: `DOMContentLoaded` 0.3 ms before the response, `load` 0.3 ms after), so the listener is armed BEFORE the send and every event is BUFFERED; an older document's `load` belongs to no chain member and does not count. A failed navigation (`errorText`) commits its error page under the SAME `loaderId` and fires `load` for it, so F-802/F-833's `chrome-error://` detector reads the landing unchanged; a same-document navigation answers `loaderId: null` and fires nothing, so it returns at the response. **`net::ERR_ABORTED` is the one answer where ours never commits, and it has TWO measured meanings** — a download (`Content-Disposition: attachment`: nothing commits, nothing fires, the tab does not move) and a page that navigated itself away while we were pending (its own document commits 11.6–14.1 ms later, or just BEFORE the abort response in 2 of 6 runs) — so it seeds an EMPTY chain, adopts the first commit in that frame as the head, and waits `ABORTED_GRACE_SECONDS` (1.0 s, ~70× the worst measured gap) measured FROM THE RESPONSE (the abort arrives a second or more in; a grace anchored at the call is already spent) and clipped to HALF the remaining budget (one that expires at the caller's deadline loses the named answer to the enclosing `wait_for`). Only a grace that passes with nothing in it raises, naming the abort. `landing(tab)`/`LANDING_JS` is the post-navigation read and it is ONE `JSON.stringify` round trip for url AND title, same discipline and same reason as `page_storage.READ_JS` — two `tab.evaluate` calls straddled a `meta refresh` and answered with one document's url and another's title. **And that read may have its document taken away UNDER it** (F-882e): the milestone and the read are two round trips, so a document scheduled to replace itself AT the milestone commits in the gap and Chrome answers the in-flight `Runtime.evaluate` with `-32000 Inspected target navigated or closed` — which reached the caller as a failed `navigate` about a page that was fine (twice on `integration (macOS/ARM64)`, both `[accepted, committed]`). So `landing` RE-READS, up to `LANDING_SWAP_RETRIES` (2) times, keyed by `document_swapped` on `SWAPPED_CODE` **and** `SWAPPED_MESSAGE` — both, because `-32000` is Chrome's generic server error and also carries `DOM agent hasn't been enabled`; every other `ProtocolException` is raised from the FIRST read, and the LAST read sits outside the loop so its failure is the one the caller sees. Deliberately NOT a second wait for the new head of the chain to reach the milestone: the milestone belongs to the navigation and was reached, the listener is gone by here, and re-arming one would put a second "when is a document ready" home one function away from `navigate` — a committed-but-still-parsing landing is one document at one instant and is already a named truthful state (F-882d). `browser_manager.navigate` is deliberately unchanged: a swap error that survives the re-reads is already not retried (`_is_recoverable_navigation_error` does not match it — `target closed` is not a substring of `target navigated or closed`), which is a pin now rather than a coincidence of substrings. An unknown `wait_until` raises `ToolError` naming the three. The listener is removed in a `finally` through nodriver's `remove_handler` — `del self.handlers[evt]`, F-824's race — tolerating the `KeyError` a concurrent `Tab.wait()` leaves. No deadline of its own beyond the abort grace: the caller's `wait_for` is the bound, and `budget_seconds` is handed in to clip F-787's sleep and that grace. Cost: two idempotent Page-domain commands per navigation (~1 ms together) — `setLifecycleEventsEnabled`, and `Page.enable`, which nodriver re-sends EVERY time because removing the listener makes its next send forget `cdp.page` from `enabled_domains`. A leaf: `nodriver` + `tool_errors` + stdlib `json`, tab as an argument. `tests/fakes.py`'s `FakeTab` models `Page.navigate` + lifecycle delivery (`lifecycle="before"/"after"/"never"`, `title_at_load`, `stale_load_for`, `last_milestone`), F-882's supersession (`supersede_after`/`supersede_count`/`supersede_last_milestone`/`supersede_url`/`title_after_supersede`, `iframe_loader`, `navigate_error`; `supersede_held` + `deliver_supersession()` for a node that must read the page BEFORE the replacement lands, whatever the host's loop scheduling), F-882e's swap UNDER the read (`landing_swaps` + `TARGET_SWAPPED_ERROR`, whose text is copied from the CI traceback and never imported from the product, so a pin measures the key against CHROME's wording) and the enable-time replay on EVERY tab, plus `Tab.wait(t)` as nodriver 0.47 has it, so the CI shape is reproducible without Chrome |
| `tab_identity.py` | **THE one home for "what page is this tab showing RIGHT NOW"** (F-874) — `record(tab)`, the `{tab_id, url, title, type}` record (a pure attribute read: `getattr`, because `Browser.tabs` yields a raw `Connection` for a rediscovered target and `Connection.__getattr__` delegates to `self.target`), `refreshed(browser, tab)`, which puts ONE `Browser.update_targets()` in front of it, and `UNTITLED`. Its three consumers are `browser_manager.list_tabs`, `tool_sections/tabs.get_active_tab` and `tool_sections/browser_management.list_instances` — the first two had the same four expressions written out by hand and the third read `BrowserInstance.current_url` instead, which is the finding. The refresh is a real `Target.getTargets` and not a read of `tab.target` as it stands, because that metadata is only as fresh as whatever `Target.targetInfoChanged` nodriver has already processed; it costs one round trip per BROWSER, which is why a listing pays it once rather than awaiting each tab (`Tab.wait()` has a 0.5 s floor and a rediscovered `Connection` cannot be awaited at all, F-771 — that `await tab` is gone from `get_active_tab`). A leaf: imports no other embedded module, takes the tab as an argument, and does NO error handling — bounding (`rt._with_cdp_timeout`) and degrading are the caller's, because only the caller knows whether one wedged browser costs the whole answer or one row |
| `page_storage.py` | **THE one home for "read a page's localStorage/sessionStorage"** (F-869) — the ONE `JSON.stringify` round trip that reads both stores (`READ_JS`, `read`), and the two outcomes it distinguishes: `StorageBlockedError` (Chrome threw *inside the page* on an opaque origin / `data:` URL / policy-blocked storage — the expected answer) and `StorageReadError` (the evaluate did not answer with the promised JSON, so empty storage would be a lie). It is `JSON.stringify` for the same reason the viewport read next to it is (F-844): `tab.evaluate` always sends deep `SerializationOptions` and returns `deep_serialized_value.value` RAW, so `Object.keys(localStorage)` arrives as `[{'type':'string','value':'k'}, …]` — measured on Chrome 152 — and the old per-key loop hashed a `dict`. One round trip also retires the `f"localStorage.getItem('{key}')"` interpolation (a quote in a key was a syntax error; `');…` was injection) and 2N+2 CDP calls. **No message here ever carries a stored VALUE** — a page's localStorage is where its session tokens live and a `StorageReadError` reaches the durable log, `get_instance_state`'s `detail_error` AND a Sentry breadcrumb at once, so every message reports shape and count only (type name, entry index, field count, character count). The one page-supplied string repeated at all is the refusal text, kept because Chrome's wording is the diagnostic and **bounded** by `BLOCKED_REASON_CHARS` because `window.localStorage` is an own accessor with `configurable: true` (measured, Chrome 152) — a page can install its own throwing getter and author it. A leaf: imports no other embedded module, takes the tab as an argument |
| `dom_handler.py` | DOM manipulation + element interaction. It no longer owns the EVAL SEAM either (F-883): `execute_script` is a one-line delegation to `script_evaluation.run`, and `_json_value`/`_script_value`/`_evaluate_by_value`/`_evaluate_as_function_body` moved out whole when that seam grew a fourth decision to carry and this file sat at 997 of its 1000-LOC budget (997 -> 853; the cap ratchets down only). It no longer owns a key press, a "did it work" or a "where did the click go" (F-873, F-876): `type_text`, `paste_text` and `click_element` keep only the ORDER, and in all three the order is load-bearing. `type_text` verifies a line's characters BEFORE that line's Enter, never after (an Enter that submits may navigate, and a read against the detached element would report a failure the page had accepted), and skips an empty line entirely, which is what keeps the common `"query\n"` from reading back across its own navigation. `paste_text` reads its baseline AFTER the clear (before it, the baseline would be the preset value and a control that refused everything would still look like it had moved) and skips the guard for empty text. `click_element` reads the aim ONCE, after `scroll_into_view` and BEFORE the click, because afterwards would describe a page the click may already have changed and a `display:none` target has no box left to ask about; its synthetic fallback is KEPT — for such a target it is the only thing that reaches the element — and is now labelled `dispatch: "synthetic"` rather than silent. `select_option` and `upload_file` keep only the order too (F-877) — read before write, read back after the events; guards before CDP, `FileList` after `send_file` — and the argument for why each half is load-bearing lives in `control_state`'s module docstring next to the code that makes it matter, with a one-line pointer here (it was moved out when four leaves' orchestration put this file over the 1000-LOC cap; that cap ratchets down only). `upload_file` keeps ONE pre-flight guard in its own body, every-path-must-exist, and its missing-path message reports position, count, length and suffix, never the path, which named the operating user; the other guard, is-this-an-`<input type="file">`, is `control_state.require_file_input`. `scroll_page` owns the scroll RECORD and its budget, not the reading and not the PICK (F-875, F-878): it asks `scroll_position` which element is the page's scroller (the FIRST round trip, so it is also where "a bad request costs no round trip" is enforced), for the position before, runs the one script that module hands it — which arms the end-of-scroll latch and scrolls in the same round trip — settles on the page's own end-of-scroll signal, and composes `scrolled`/`at_edge`/`settled`/`settle_seconds` + the six offsets + `scroller`/`scroller_is_document`. Those last two come from the FINAL READ and never from the pick, so a path that went stale mid-scroll names the document it actually drove rather than the div it hoped for. `scrolled` is `Position.moved_from` — the ONE home for that comparison: offsets only, NEVER the whole reading (a lazy-loading page grows its extent while standing still, and a whole-`Position` compare reported that growth as a scroll with identical before/after offsets in the same record), and only when both readings are about the SAME element (across a stale-path fallback the before is the div's and the after the document's, and the difference between two elements' offsets is not a distance anything travelled). `max_scroll_y == 0` is an ANSWER, not a raise — the same policy `page_storage`'s blocked read has, for the same reason. Its `except ToolError: raise` sits in front of the blanket handler because `scroll_position` already speaks the convention; re-wrapping doubled the message and dropped the cause |
| `script_evaluation.py` | **THE one home for "run a caller's JS in the page and read its answer"** (F-883) — `run`, `evaluate`, `as_async_function_body`, `script_value`, `json_value`, the two compile complaints (`_FUNCTION_BODY_COMPLAINTS`: `ILLEGAL_RETURN`, `TOP_LEVEL_AWAIT`) and the two record witnesses (`_COMPILE_CLASS`, `_STACK_FRAME`). It carries four findings welded together and none may be re-derived from taste. **F-795**: the `exceptionDetails` are read BEFORE the result, and the record goes to `tool_errors._require_js_value`, never a raise here. **F-832**: a raw `Runtime.evaluate` with `return_by_value=True` and NO `serialization_options` (CDP documents it as OVERRIDING `returnByValue`), read by a None-vs-ABSENT test rather than a truthiness one. **F-812**: the source is evaluated AS WRITTEN first — a top-level `var`/`function` must land on the PAGE, not become a local of a wrapper that is thrown away — and only Chrome's own complaint sends it round again. **F-883**: `await_promise=True` on THE one send (without it Chrome answers with the Promise OBJECT and `returnByValue` serializes that to `{}`, which is indistinguishable from a script that returned `{}` — and a REJECTION answered the same `{}` with `success: true`); an ASYNC wrapper on both wrap paths (the retry's `(async () => {…})()` and the `args` path's `(async function() {…})(args)`, so `await` is legal on both call shapes rather than one); and a retry keyed on TWO complaints, each PAIRED with the phrase its message uses, because a caller told "top-level 'return'" about an `await` goes fixing the wrong line — keyed on the RECORD, never the message: `exception.class_name == "SyntaxError"` AND no stack frame in the description (measured: a compile complaint's is bare, every thrown Error's is `error.stack`; `stackTrace` itself is NOT sent without `Runtime.enable`), so `throw new Error("Illegal return statement")` is a throw and is never run twice — a page that also overwrites `.stack` still can, §6 says so. The retry is taken on the bare path only; the args wrapper cannot produce either complaint and would lose its args. It owns NO deadline: a Promise that never settles blocks the send exactly as `while(true)` blocks the renderer, and the bound for both is `tool_runtime._clamp_timeout` + `_with_cdp_timeout` at the tool body — CDP's own `Runtime.evaluate` `timeout` is deliberately NOT sent as well, because a second deadline is a second answer to "how long may a script run"; and a Promise that settles AFTER that bound must not kill the tab, which is `cdp_transport`'s shield around the reply, not this module's and not the timeout wrapper's. A leaf: `nodriver` + `tool_errors`, tab as an argument. THE seam for `execute_script`; `cdp_function_executor` still evaluates caller source through its own KEEP-contract path, deliberately not re-routed here |
| `text_entry.py` | **THE one home for pressing a key in a page, and for proving the text landed** (F-873) — `press_key` (the ONE way a key reaches the page: exactly two events, a `keyDown` carrying `text` so Chrome synthesises the keypress, and a `keyUp` that does not — never a third, because a separate `char` on top fires a SECOND keypress and submits a form twice, measured Chrome 152), `press_enter` (`text="\r"`, vk 13; Shift is bit 8 and buys the page's `shiftKey` branch, not a suppressed submit — Blink's implicit submission does not consult the modifier), `type_characters` (per character, full lifecycle, re-focusing before each one), `clear_via_keyboard` (the ONE select-all + Delete, shared with `paste_text`; `type_text` used to send WebDriver's private-use codepoints through `send_keys` — U+E009 for Ctrl, U+E017 for Delete — which CDP never spoke, so all THREE characters landed verbatim and nothing was cleared: measured `"<U+E009>a<U+E017>preset-value"`, a fallback that corrupted the field it was asked to empty), `READ_JS`/`entered_text` (the ONE read-back, a JSON **string** because `Element.apply` returns `result[0].value` and a thrown script lands there as `None` — a non-`str` answer is "could not be read" and raises, never an empty field; `contentEditable` reads `textContent`) and `verify_received`. The check is **"did anything change"**, never "does it contain what I typed": a mask, an autocomplete that rewrites or a `number` field that normalises all DID receive the input, and a stricter test is the same defect with the opposite sign — the cost, named in the finding's §6, is that a control whose value is legitimately identical afterwards now raises. **`entered_text`/`verify_received` have a SECOND consumer** — `paste_text` (F-876), which is why the refusal message says "entered" rather than "typed" and no longer names key events: the same five controls refuse one `Input.insertText` exactly as they refuse a key per character, and a message about keys would be about the wrong mechanism half the time. `date` is deliberately absent from that message's hint and from this row's list: the local Chrome 152 build refused digits into an `<input type="date">` but PR #110's gate measured all three CI cells ACCEPTING them, so it is build-dependent and naming it as a known refusal is a claim the evidence does not support. **No message here ever carries the typed text** — the selector and the counts only, because a raised `ToolError` reaches the debug ring through `log_tool_failure` (ring only, F-782/F-835 — the durable line is deliberately withheld there) and reaches the client and Sentry as the exception itself, and the field may be a password box (F-869's discipline). A leaf: `nodriver` + `tool_errors` only; tab and element arrive as arguments (the element typed under `TYPE_CHECKING`, as `element_resolution` does) |
| `click_target.py` | **THE one home for "where was this click aimed, and what was under that point"** (F-876) — `AIM_JS`/`aim` (the ONE read: a single `Element.apply` answering a JSON **string**, same shape discipline and same reason as `text_entry.READ_JS`), the `Shape` descriptor (tag/id/classes and **never text content** — an overlay is frequently a consent banner and its words are the page's; the class list is bounded by `MAX_CLASSES`), the `COORDINATE`/`SYNTHETIC` dispatch kinds, the closed `reason` code set (`not-rendered` → `off-viewport` → `zero-size` → `not-visible` → `pointer-events-none` → `covered` → `disabled` → `None`, in that order because `pointer-events:none` makes BOTH it and `covered` true and only one is the cause, and `disabled` is the one shape where the hit-test names the target and the click is still inert) and `record`, the composition. The point is `getClientRects()[0]`'s centre, NOT `getBoundingClientRect()`'s: `Element.mouse_click` clicks `Position(quads[0]).center`, and the two are byte-equal on every shape measured except an `<a>` wrapped over four line boxes, where the bounding box is 28.5 px off — a record built on it would name a point the click never used. Deliberately NOT a "did the page react" oracle: navigation, DOM mutation and network are unbounded and the absence of any of them is not evidence, so this module reports the two facts that ARE decidable and `click_element` raises for neither. A leaf: `tool_errors` only, element as an argument, and no error policy — the one thing it raises is "the page did not answer with the promised JSON" |
| `control_state.py` | **THE one home for "what does this form control hold now, and did it take what was asked"** (F-877), for the two controls whose state is a SET of selected things — `READ_SELECT_JS`/`read_select` and `apply_js`/`apply_selection` (two `Element.apply` calls, each ONE `JSON.stringify` round trip, same shape discipline and same reason as `text_entry.READ_JS`), `READ_FILES_JS`/`read_files`, the `MAX_OPTIONS` bound, the ONE matching rule (`resolve_option`), the three verdicts (`verify_matched`, `verify_selected`, `verify_attached`) plus the two records, and `require_file_input` — the file-input twin of `verify_matched`'s `is_select` gate (what the control IS is this module's question as much as what it holds; it moved out of the handler when four leaves' orchestration crossed the 1000-LOC cap). **The module docstring is also where "why the handler's ORDER is load-bearing" is argued** for both tools — read-before-write and read-back-after-the-events for `select_option`, guards-before-CDP and `FileList`-after-`send_file` for `upload_file` — so the handler's two docstrings are one-line pointers. The matching rule is in PYTHON on purpose: `text=` used to be `send_keys`, so its real consumer was Chrome's `<select>` typeahead — a buffer with a ~1 s timeout SHARED with the previous call, searching from the option AFTER the current one, and on a `<select disabled>`, which cannot take focus, sending the characters to whichever control had it (measured: a request for the disabled select moved a DIFFERENT one, trusted, and the tool answered `True`). The rule is exact `option.text`, then exact `option.label`, then a case-insensitive PREFIX over either, skipping `disabled` options — the prefix tier is kept because the typeahead had it. The write re-checks the option's `value` against the index it was given (a dependent dropdown can repopulate between the two calls — its residual, named in the docstring, is a replaced list whose option at that index happens to carry the SAME value) and fires `input` then `change`, and only when the selected-index **SET** moved, never when `selectedIndex` moved: on a `<select multiple>` holding `[0, 2]`, assigning `selectedIndex = 0` drops option 2 while that one number does not budge, so a comparison of it would fire nothing and leave the page believing it still holds two — and the shipped `value` arm fired `change` unconditionally, so that would be a REGRESSION. **The events are UNTRUSTED** (`new Event(...)`, `isTrusted: false`) where the typeahead's were trusted; there is no trusted alternative that is not the typeahead this module removes, the other two arms were already untrusted, and the loss is named here, in the tool docstring and in the finding's §6 on `click_target`'s `synthetic`-labelling precedent. `MAX_OPTIONS` costs one thing and it is named rather than hidden: an `index=` genuinely inside a control larger than the cap gets its OWN message ("beyond the read cap"), never "no option matches". **No message and no record field here carries an option's text or value, a file path or a file name** — a `<select>` is frequently a list of account numbers and an absolute path names the operating user; selectors, counts and indices only. A leaf: `tool_errors` only, element as an argument |
| `scroll_position.py` | **THE one home for "which element is the page's scroller, where is it scrolled, and has it stopped"** (F-875, F-878) — `SCROLLER_JS`/`scroller`/`Scroller`, the ONE pick: axis-aware (the axis comes from `_DIRECTIONS`, so `right` can find a horizontal-only strip), **document-first** (if `document.scrollingElement` can move on that axis it IS the scroller — a PRECEDENCE, not a tie-break, which is why a plain page, quirks mode, document scroll-snap and a nested box inside a scrolling document are all unchanged and the SCROLL CALL generated for them is byte-identical to F-875's and its `scrollend` listener is still armed on `window`; the script around that call is not — `READ_JS` moved to the element's `scrollLeft`/`scrollTop` and `SCROLL_JS` binds the target to a name), else the largest VIEWPORT-CLIPPED area among elements that can move on that axis with `overflow` `auto`/`scroll`, near-ties (within `AREA_SLACK`) broken by the larger extent, else the document anyway. Measured 12/12 on twelve real-Chrome fixtures against 4/12 for `document.scrollingElement` alone, 9/12 for largest-area-with-a-floor and 8/12 for the element under the viewport centre (which walks INWARD to a page's own grid and is blind behind a `position:fixed` scrim) — the matrix is `audit/stage2/finding_F878_scroll_page_nested_scroller.md`, and **do not re-derive the rule from taste**. Picked ONCE per call and addressed afterwards by an index path from `documentElement` (`_RESOLVE_JS`), not a stashed `window.__x` reference: the pick costs 2.59 ms on a 6007-element page and a full settle is ~200 polls. `READ_JS`/`read` (the chosen element's offsets AND extent AND identity in ONE `JSON.stringify` round trip; `scrollLeft`/`scrollTop`, which for the document scroller is `window.scrollX`/`scrollY` by definition — one expression, no branch; `Math.round`ed, because sub-pixel positions are real and a settle comparing floats would never see two reads agree). The identity lives on `Position`, never on `Scroller`, so the record cannot name an element it did not read; it is SHAPE only and bounded in the page itself (`SCROLLER_ID_CHARS`/`SCROLLER_CLASS_CHARS`/`SCROLLER_CLASSES`). `_json_answer` is the one "did the evaluate answer with the JSON we asked for" ladder both round trips take. `Position.offset` (the `(x, y)` pair — **the only half that means "moved"**; offsets and extent are never compared together, because a page that only GREW is not a page that scrolled and a page still filling in must still be able to settle), `EDGE_TOLERANCE_PX` (one pixel of slack on the FAR edge only: the offset and the extent round independently, so under fractional zoom a page at its true bottom can read one short — `scrollY` is never below 0, so the near edge needs none), `SCROLL_JS`/`start` (the scroll and the arming of a one-shot `scrollend` latch in ONE round trip, answering `moves` — will this change the SELECTED scroller's offset at all, from the clamped target, synchronously, on either axis — and `supported`, `'onscrollend' in T`, the feature test asked OF THE TARGET because `'scrollend' in window` is FALSE on Chrome 152), `settle` (wait for the PAGE to say the scroll ended, extent taken from the FINAL read, `SETTLE_BUDGET_SECONDS` = 10 s — ~3× the finding's worst measured smooth scroll and a THIRD of `CDP_OPERATION_TIMEOUT`, so a slow scroll is REPORTED rather than raised). **Never "two consecutive reads agree"**: a document smooth scroll runs on the COMPOSITOR while the offset is read on MAIN, so a blocked main thread holds a mid-flight value for as long as the long task (measured 120 ms→121, 250→250, 400→400) and CI gate run 35046780659 (macOS/ARM64) reported `scroll_y_after: 3498` about a page that read 4898; `scrollend` LATCHES, so jank can only delay when the end is SEEN. **And the latch is armed on the element that scrolls, never on `window`** — measured on Chrome 152 across the twelve fixtures: an ELEMENT scroll's `scrollend` fires at that element (`#shell`, `#main`, `#deck`, the horizontal `#strip`), smooth AND instant, on both axes, and does NOT bubble to `window`/`document`; a DOCUMENT scroll's fires at `document`/`window` and NEVER at `document.scrollingElement`. One right target per scroller kind, and BOTH wrong choices fail silently in the same way — the listener never fires, the settle burns its whole 10 s budget and then reports `settled: false` about a scroll that finished in a second — so the armed target is literally the expression that received the scroll (`var T=…`), which is why no element case needs a degraded path and why a pin reads that target back out of the generated script rather than trusting it. Read agreement survives as the fallback for a browser with no `onscrollend`, which is the one path where `START_GRACE_SECONDS` still matters (a smooth scroll that has not begun reads exactly like one that never will); a scroll whose `moves` is false never waits for an event that will not fire — a no-op scroll latches nowhere, measured — which is what keeps an instant scroll and a one-viewport page on the fast path. Also `_DIRECTIONS`/`script`/`Position.at_edge` — ONE table for what a direction means (axis, edge, JS), never a script table beside an edge table; its six scripts carry a `{target}`/`{extent}` placeholder, which is `window` for a document scroller and `_el([…])` for a nested one. `_validate` REJECTS a negative `amount` and an unknown direction from the request ALONE, and both `script` and `scroller` call it, because since F-878 the FIRST round trip is the pick and "a bad request costs no round trip" has to stay true of it: `amount` is a distance and `direction` is the only thing carrying a sign, so the pre-2.1.7 pair of wrong readings (`down, -500` silently scrolled UP; `up, -500` interpolated `top: --500` and died as a JS syntax error) becomes one refusal rather than one silence. It never decides whether a scroll SUCCEEDED; that is the caller's record. A leaf: `tool_errors` only, takes the tab as an argument, `_now`/`_sleep` its single timing seam (the `scheduling_lag` pattern) |
| `element_resolution.py` | selector resolution that survives CDP document-node invalidation (route ALL selector resolution through here — never `tab.select`/`find` directly). Also **THE one home for "only one `DOM.getDocument` on this tab at a time"** (F-884) — `_document_lock`, one `asyncio.Lock` per tab, taken by `_resolve_with_recovery` around each resolution ATTEMPT (never around the retry loop, so the settle sleep releases it), and **THE one home for the WAIT** — `_wait_for`, which polls a locked SINGLE-SHOT query with the lock RELEASED between tries, bounded by the caller's own deadline. Never `tab.select`/`find`/`select_all`/`xpath`: each bundles a poll loop into the query, so the lock spanned the wait and one waiter froze every other DOM call on that tab for nodriver's 10 s default regardless of its budget — measured, a 1 s `wait_for_element` held the tab 10.5 s, a sibling `query_elements` went 0.25 s → 10.56 s, four concurrent absent resolutions blew `CDP_OPERATION_TIMEOUT`, and a waiter answered `False` about an element the concurrent click it was starving would have created. Production calls only `query_selector` / `query_selector_all` / `find_element_by_text` / `find_elements_by_text` (the last is what `Tab.xpath` is built on — `DOM.performSearch` takes an XPath — minus its `enable` prologue, NOT its `disable`: `find_elements_by_text` sends `dom.disable()` bare as its LAST statement, after its answer is built, so a failure there discards a resolution that SUCCEEDED. `Tab.xpath` swallows exactly that call in a `finally` and says in a comment that it "sometimes raises"; calling `find_elements_by_text` directly loses the swallow, so `_xpath_matches` re-makes it — named, keyed to the disabled-agent wording alone, and a single RE-RUN rather than a `pass`, because the answer went with the exception and the search is an idempotent read whose own `getDocument` re-enables the agent. `_is_disabled_agent_error` is that key and it is deliberately NOT a `_STALE_NODE_MARKERS` entry and NOT a `recoverable_race`: it is the MASK, and re-resolving on it would restore the pre-fix behaviour by the other door), and **every tab double in the suite offers ONLY those names** so a regression is an `AttributeError` rather than a silent 10 s freeze — including the bare-`MagicMock` ones, which cannot fail that way on their own (a `MagicMock` auto-creates every attribute, so stubbing a bundled name leaves the real one answering a non-awaitable mock that dies at the `await`, inside whatever `except` the node was pinning). `tests/test_silent_excepts_log.py::_single_shot_tab` is how those four get it. All four resolvers (`resolve_element`, `resolve_by_text`, `resolve_elements`, and `_wait_for` under them) take the SAME `timeout` and spend it in the same place — `resolve_elements` could not offer one before F-884, because the wait was `select_all`'s and bundled into the query. `_DEFAULT_WAIT_SECONDS`/`_POLL_SECONDS` are nodriver's own 10 s/0.5 s, so a caller passing no timeout waits as it always did; `timeout=0` is one query, which is what `dom_handler.wait_for_element` passes because its own 0.5 s loop IS the wait (it used to nest nodriver's 10 s default inside a caller's 1 s budget). One budget for CSS and XPath, so an absent XPath now waits 10 s where `Tab.xpath` waited 2.5 s. `_now`/`_sleep` are its single timing seam (the `scroll_position` pattern). `getDocument` does not merely read the document, it RESETS this CDP session's node-id bindings, and every resolution here is `getDocument` followed by a query using the id it returned — so two overlapping resolutions on one tab always lose, and what surfaces is not the recoverable stale-node text: nodriver's `query_selector` sends `DOM.disable()` from inside its `except` before re-raising, and that send fails with `-32000 "DOM agent hasn't been enabled"` once a sibling disabled the agent, REPLACING the marker `recoverable_race` classifies on. Never widen `_STALE_NODE_MARKERS` to catch it — the wording is Chrome's and not closed (the XPath path says `"DOM agent is not enabled"`); the lock removes the cause. Scope is the TAB OBJECT because the state is (measured: a second connection to the same target is unaffected — the table is per CDP session), keyed by `id(tab)` + `weakref.finalize` because `Connection` defines `__eq__` without `__hash__` and a `Tab` is unhashable. NOT re-entrant and nothing needs it to be — no function here calls another — which is also why the lock is not extended over the cloner's `asyncio.gather` of six aspects, where a parent-held lock would deadlock the styles aspect that resolves inside it. **`refresh_element` is the one home for `Element.update()`** and takes the same lock: `update` reads as "re-read this element" and is a `DOM.getDocument`, so `query_elements` calling it per element reset the table once per row while a sibling was mid-query — no caller may reach `element.update()` directly. It does NOT make a node id safe to HOLD across calls (the cloner keeps one across five further CDP calls; `audit/stage2/finding_F884_concurrent_dom_queries.md` §6) |
| `proxy_forwarder.py` | authenticated egress-proxy forwarding + `_free_port` |
| `proxy_utils.py` | proxy string parsing + Chrome launch-arg helpers |
| `window_sizing.py` | **the one home for a spawn's requested window size** — the `--window-size` launch arg, the CDP `setWindowBounds` apply, and the post-launch measurement that makes the reported size truthful (F-804). Since F-888 it also owns the RE-ATTACH read, `measure`: a browser this backend did not launch has no request to compare against, and calling `apply_and_measure` for one would SET the size first — resizing a human's open window to a default they never asked for. An unreadable window answers None and the caller reports `measured: false`, never the model's 1920x1080 default |
| `spawn_contention.py` | **THE one home for "did this spawn fail because it was racing sibling spawns"** (F-834) — the in-flight threshold and the paragraph `contention_hint` appends to a failed spawn's error, including the explicit disclaimer that nodriver's root/`no_sandbox` advice does not apply. **It names the COUNT, never the mechanism**: an integer is the only fact this module has, and since F-834's two layers hand concurrent spawns distinct reserved clone directories, "they contend for the same Chrome profile" is an assertion it cannot make — measured false on the coverage gate's macOS/ARM64 cell (run 35150887345 attempt 2), where a fleet that had already serialised its one master-taking lead spawn still lost a FOLLOWER with every follower on its own directory. So the paragraph offers both causes (the profile singleton, and what N simultaneous Chrome launches cost a small runner) and keeps the one remedy that serves either. F-834 appears exactly ONCE in it, because the disclaimer is pinned as the text after that mention. A sibling of `spawn_exhaustion`, not a fold-in: capacity and contention are different questions with different remedies. Both are appended at the ONE composition site in `browser_manager.spawn_browser`; the peak in-flight count they read is `BrowserManager._spawn_peak_in_flight` |
| `spawn_exhaustion.py` | **THE one home for "is this machine out of browser-process capacity, and what should the operator do about it"** (F-811) — the live Chromium-family count, the threshold (`_EXHAUSTION_PROCESS_THRESHOLD`), and the operator paragraph `exhaustion_hint` appends to a failed spawn's error. Its name matcher is deliberately narrower than `process_cleanup`'s ("how much of what WE spawn is running", not "may I kill this pid") — do not unify them. Never raises, never ships to Sentry |
| `browser_connect.py` | **THE one home for "how long does a freshly launched Chrome get to open its DevTools endpoint before this attempt is called failed"** (F-834 stage 2) — `install()`, `installed()`, `CONNECT_PATIENCE_SECONDS` (30.0) and nothing else. The budget it replaces is nodriver's and it is a LOOP COUNT: `Browser.start` sleeps 0.25 s then asks `/json/version` five times 0.5 s apart, which is 2.75 s with no `Config` field behind it in 0.47 — and the F-870 probe, one Chrome on an idle runner, measures `ms_to_json_version` at 3943.6 / 4786.0 / 5839.1 ms cold on macOS/ARM64 (gate runs 35304880367, 35316298288, 35454765486) and 4250.0 ms cold on Windows/X64, i.e. **two of the three cells have never once answered inside it**, with the warmest macOS reading (2138.7 ms) already 78 % of the window. Missing it costs a killed browser and a relaunch, not a failed spawn: no `Browser` comes back, so `spawn_leak` reaps the Chrome that was coming up and F-834 stage 1 launches another from cold — the F-860 WARNING that failed four of the last five macOS fleet runs. **The seam is `HTTPApi.get` and nothing else can be**: the decision is made strictly between "Chrome is spawned" (nodriver creates the process) and "`Browser.start` has already raised", and that one call is the only thing inside that interval — it also has exactly ONE caller in nodriver 0.47, pinned, though the wrapper is still keyed on the endpoint NAME so a future second caller does not inherit the patience silently. Deliberately NOT the `Browser.create(host=…, port=…)` recovery F-870 §6.1(b) sketched: that path works and leaves a `Browser` with no `_process`, i.e. one that cannot kill its own Chrome; nor `config.host`/`config.port` set by us, which makes the LAUNCH ours and is a second home for spawning Chrome. The patience is charged ONCE per launch (the deadline is stamped on the `HTTPApi` instance nodriver builds fresh in each `start`), so its four remaining attempts see it spent and add only their own 2 s. It is a CEILING, not a wait — an endpoint that opens in 300 ms costs 300 ms — and a launcher that EXITS, before the wait or during it, ends it at the next refusal (`returncode` re-read every pass), so the one case this could have slowed is faster than 2.1.9. **The ceiling is for LAUNCHES WE OWN**: `_launched_process` resolves the owning browser once and reads nodriver's `_process`, and `None` — the `connect_existing` door, which since F-888 is `cdp_attach`'s one home with its two consumers (`desktop_launch`'s delegated launch and `browser_reattach`'s adoption) — takes nodriver's window UNCHANGED, because an attach targets an endpoint that is already open and a live one answers in 0.78 ms median (measured, ten fetches), so patience buys nothing there while a stale recorded port would cost 30 s inside a user-facing call. There is deliberately no second constant for that door. An HTTP ERROR answer is likewise not a closed socket: `urllib.error.HTTPError` is excluded from the retry set, so a squatter answering 500 fails in nodriver's window. **The "2.75 s" is 2.75 s of WAITING, not wall clock** — add what five refusals cost, which is the platform's: near-instant on POSIX (F-870 §1.3), 2023-2060 ms each on Windows (measured locally), so ≈12.9 s there, which is why the Windows cell's 4250 ms cold start does not fail; a port that LISTENS and never answers costs `urlopen`'s own 10 s per attempt on both. `install()` is called from `browser_manager._launch_browser`, ahead of the F-810 branch, on `session_hygiene.install()`'s precedent of installing at the site that needs it rather than at import time; idempotent anyway, on `cdp_transport`'s. A leaf: `nodriver` + stdlib, `_now`/`_sleep` its single timing seam. **Delete it on a nodriver bump** — later versions expose `browser_connection_timeout`/`browser_connection_max_tries`, and passing the constant to those is the same answer without the patch |
| `spawn_leak.py` | **THE one home for "this spawn failed before nodriver handed a `Browser` back — WHICH process did it launch, and reap that one"** (F-860, F-919) — `Attempt`, `launched_pid` and `reap_launched_browsers`, composed from `process_cleanup`'s existing cmdline scan and escalating kill. **The fence is the launched PID, and that is a change of ANSWER, not of degree**: F-860 had only the attempt's `--user-data-dir` and killed everything on it that started within `_CLOCK_TOLERANCE_SECONDS` of the launch, whose safety argument was "a real Chrome already holding an explicit profile predates the attempt and is spared" — and concurrency falsifies it, because concurrent unnamed spawns all select the SHARED profile by design (F-834). Two of them stamp their launches **9.9-86.4 ms apart** (measured), so the loser's one-second window covered the WINNER's Chrome by 11x at worst, and terminated the operator's logged-in browser in exactly the profile-singleton case the reap promised to spare. Shrinking the window is not the fix and the constant's own premise says so: it existed to absorb the kernel's ~10-16 ms rounding of a start time, and sparing the sibling needs it under 9.9 ms — the two quantities are the same size, so no value does both. So `Attempt` is the orchestrator-owned handle passed INTO `_launch_browser` (never returned — the moment it is needed is the moment that call raised), stamped there with the `uc.Config` OBJECT before the fallible `uc.start`, and `launched_pid` reads the pid off the `Browser` nodriver registered for that exact object — **identity (`is`), never a field match**, because two concurrent spawns on one directory build configs equal in every field. Three reused answers make that pid safe to kill and none is re-spelled here: `process_exit.browser_pid` (is it the BROWSER and not a `--type=` child; and its `returncode` guard is the recycled-pid guard — and **this row has twice claimed more for it than is true**, first that it BEATS `browser_pid_registry`'s stored `(pid, create_time)` pair and then that such a pair is UNOBTAINABLE here; F-919 §4 refutes both and is the one home for the argument. The pair is ruled out on WORTH: it would differ only inside the two-loop-iteration band below, and only for a recycled pid already carrying our own `--user-data-dir`, which the second witness excludes — against a second responsibility in a seam that exists to time a launch, a blind spot before the first `/json/version`, and a second recycled-pid rule. On Windows the pid is pinned by the open PROCESS handle regardless of `returncode`; on POSIX the guard is open for two loop iterations and open DETERMINISTICALLY — `os.waitpid` frees the pid on the watcher thread while `returncode` lands two `call_soon_threadsafe` hops later, and `base_events._run_once` drains a fixed `ntodo`, so a reap with no suspension point in front of it reads None — and there the profile-directory witness is what stands), `_get_browser_pids_for_profile` (is it still on the directory we launched it on) and `_kill_process_by_pid`. **An answer that cannot be established resolves toward NOT killing** — uniformly with `profile_lock._browser_pids` and with what F-886 and F-888 chose — and the cost is named: a Chrome that genuinely leaked but whose launch we cannot name (a `Browser` missing from the registry, a collected handle, or the DELEGATED F-810 launch) is left RUNNING until the next backend start's orphan reap. **This row used to add "which kills its own Chrome anyway" of that last one, and that is not what `desktop_launch` does**: its kill is conditional (`launch_and_attach:560-561`) on a stamp taken at one place (`_run_task:466`), and F-919 §6 names the THREE paths that reach the `finally` with a live Chrome and no kill — the 20 s pid deadline expiring, `_process_create_time` answering None (its `except psutil.Error` swallows `AccessDenied` alongside the gone process it was written for), and `_kill_delegated` itself refusing on a create_time mismatch or a psutil error. The old time fence caught all three by guessing, at a WIDER aperture than the one F-919 closed — a delegated launch takes seconds, so every sibling Chrome in that span was inside its window — which is why the coverage is given up rather than restored. F-924 is the cheap successor: stamp `Attempt` from the pair `:466` already computes. Called from the ONE teardown site `browser_manager._teardown_failed_spawn` shares between the cancel and error handlers, only when no `Browser` handle exists. Takes the `ProcessCleanup` as an argument and imports no orchestrator; never raises. It kills pids and **deliberately leaves Chrome's singleton artefacts alone** — Chromium unlinks an orphaned `SingletonLock` itself on the next launch, and whether one is stale is `profile_lock`'s question (F-871). Its WARNING is unchanged by F-834 stage 2 and still fires for a spawn that genuinely left a Chrome behind — what changed is upstream, in `browser_connect`: a Chrome that had merely not finished opening its endpoint is no longer one of them |
| `profile_lock.py` | **THE one home for INTERPRETING Chrome's process-singleton artefacts** — "is this profile held by a live process, and who holds it" (F-871) — `profile_hold` and the `Hold(pid, reason)` it returns. Interpreting, not owning the names: `profile_copy.REGENERABLE_NAMES` also lists all three (plus `lockfile`) for the different question of what a clone must not copy and what a trim may delete — that list is about bytes on disk, this module is about what they MEAN. Two witnesses and no third: the process table (`process_cleanup`'s cmdline scan, handed in as an argument so this stays a leaf) and Chrome's own process singleton, read the way Chromium's `ParseProcessSingletonLock` reads it — `SingletonLock` is a SYMLINK whose target is the string `<hostname>-<pid>`, i.e. a claim about a PID, so a lock naming a dead pid is ORPHANED and holds nothing (which is exactly what Chrome concludes before unlinking it and starting). `SingletonSocket`/`SingletonCookie` are NOT consulted: they are written after the lock, so a live browser always has a lock, while the socket's `/tmp` target outlives a killed browser — a plain `.exists()` over all three (what `clone_storage` used to ask) read that residue as "busy" and silently walked a NAMED profile to `<name>-2`, and could never see the lock at all because a dangling symlink is `exists() == False`. Deliberately does NOT delete a stale lock (a second way to do what Chromium already does, from a failure handler that would race a dying browser), and deliberately skips Chromium's extra `IsChromeProcess` check — a recycled pid costs one walk, a live browser's profile called free costs the profile. **On Windows there is only ONE witness** (Chrome takes a delete-on-close `lockfile` there and writes no `Singleton*` at all), whose mere presence is deliberately not consulted — a presence test is the reasoning this finding condemns, and a power loss leaves a `lockfile` with no pid in it forever. What that costs is the DIRECTION of an unreadable answer: `_browser_pids` returns `None` for "could not be asked" (distinct from `()` for "asked, nothing running"), and with no second witness that resolves toward HELD, uniformly with `_pid_alive`. **And what that scan ANSWERS WITH is a whole process TREE, of which only the BROWSER member holds the profile — `_tree_hold` (F-931), the one thing this module imports `browser_cmdline` for.** `process_cleanup._get_browser_pids_for_profile` applies no `--type` filter and `profile_hold` reported `min(pids)`, so a renderer or a crashpad handler was named as the holder in F-914's refusal and in `walk_reason`; and because `close_instance` waits for and kills the BROWSER alone (`process_exit.browser_pid` answers None for any `--type=` child, deliberately — F-910), for a window after every close the survivors were exactly a dead browser's children and the shared session read HELD by a pid nothing drives, refusing the very next unnamed spawn (measured, release gate run 35689647688, `integration (Windows/X64)`). `browser_reattach.held_by` has asked `browser_cmdline.browser_process` since F-888 for the same reason; this is that rule reached from the other side, so the two witnesses can no longer disagree. THREE answers from two facts: a browser member (the LOWEST when Chrome's singleton has failed and there are two — a directory with two browsers is held whichever is named, which is also why `browser_process`'s ambiguity-`None` may NOT be reused here) holds it; every member readable and all children is an established NEGATIVE and falls through to the lock exactly as an empty scan does; any member whose argv could not be READ holds it and the reason SAYS SO rather than claiming a browser we never saw. A pid MISSING from the table is neither — the scan and this read are two moments, and a process that exited between them is an established negative. Waiting for the whole tree on CLOSE instead was rejected: it costs close latency, it contradicts F-910 at the same file, and it closes one door of several (a crash, `kill-orphans` or Task Manager leaves the same orphans). Fixing it here costs one thing, named: a profile whose browser is gone and whose children linger is now reported FREE, and Chrome's own singleton is the backstop either way. `tests/fakes.py`'s `write_singleton`/`held_profile` are the one way a test writes these artefacts. Never raises |

**Cloner subsystem** (one engine + thin adapters + disk storage)
| File | Owns |
|---|---|
| `cdp_element_cloner.py` | **THE cloner engine** (`CDPElementCloner`) — the complete-element **clone schema** (its shape lives here, **not** in `models.py`) + every aspect (`styles` via CDP; `structure`/`events`/`animations`/`assets`/`related_files` via JS-eval). An OPERATIONAL failure **raises** `ToolError` (F-858, convention 2), and reading a JS aspect's answer is delegated to `js_aspect_answer.parsed` (F-872), never re-implemented here. Two `{"error": ...}` dicts are EMBEDDED payload records, not tool returns — `extract_complete_element`'s per-aspect isolation and `_get_element_html`'s sub-field degradation, both named in `tests/test_cloner_error_convention.py` and gated there by AST. **A third, unfinished, shape remains**: the four non-animation JS scripts answer an unresolved selector with `{"error": "Element not found"}` and their aspects RETURN it, so that one *is* a tool return in the shape convention 2 bans. `extract_element_animations` alone raises it. F-872 deliberately left it — an error-convention decision, not a transport one — and it is the named follow-up in `audit/stage2/finding_F872_cloner_nested_bidi_nodes.md` §6 |
| `js_aspect_answer.py` | **THE one home for reading a cloner JS aspect's `tab.evaluate` answer** (F-872) — `parsed`, `js_error`, `unexpected_type`, `MAX_ERROR_CHARS`. Two decisions: (1) the only shape an aspect may answer with is a **string**, because `Tab.evaluate` sends `serialization="deep"` on every call and returns the deep-serialized value verbatim, so a returned OBJECT arrives as BiDi `RemoteValue` nodes at EVERY depth and `return_by_value` cannot undo it — there is deliberately **no** BiDi decoder here, and the partial `_convert_nodriver_result` unwrapper that preceded it (top level only, nested arrays left as raw nodes — corrupted, never crashed) is deleted; (2) a thrown script comes back as the `ExceptionDetails` record ITSELF (`if errors: return errors`), which has no `exception_details` attribute and whose `.text` is the literal `"Uncaught"` for every throw there is (measured), so the message is built from `.exception.description` and clamped to `MAX_ERROR_CHARS` — the text is Chrome's, its LENGTH is the page's. A leaf: `nodriver` + `tool_errors` only. Deliberately **not** shared with F-869's `page_storage` reader: same `JSON.stringify` + `json.loads` idiom, different failure policy, and an idiom is not a home |
| `file_based_element_cloner.py` | thin to-file adapter (`FileBasedElementCloner`, name KEPT) — owns `output_dir` only. A delegated extraction failure **propagates** (F-858): it used to be swallowed into a `{file_path, …}` answer with an empty summary, i.e. a file claiming to be a clone that failed |
| `progressive_element_cloner.py` | thin adapter (`ProgressiveElementCloner`) — `expand_*` slices from cached extraction; `_require_stored` is the one store-miss guard (raises, F-858) |
| `clone_storage.py` | on-disk **profile/clone quota + GC** (NOT extraction — see glossary "clone") + **the one profile-selection resolver** (`resolve_profile_selection`) + **the one re-selection a failed spawn attempt retries onto** (`_fallback_profile_selection`). Since F-834 stage 1 that fallback answers for **all three roles, and only a `clone` re-clones**: a `clone` gets a fresh per-attempt clone; an `explicit` (NAMED) profile retries **the SAME directory**, because the caller asked for that profile's cookies and a clone is a different identity, as is a walk to `<name>-2` — the one place either may happen is the resolver, where F-871 reports it; a `master` retries the same directory too **when `_profile_hold` says nobody took it**, and falls through to a reserved clone when a sibling did, because retrying a directory another Chrome owns fails the same way again. What makes the same directory available at all is the failed attempt's F-860 reap, which has just killed the Chrome this attempt started. Before that widening, `None` came back for every non-`clone` role and the three-attempt loop re-raised on the FIRST failure — the two shapes measured are a concurrent unnamed spawn that lost Chrome's profile singleton, and a NAMED follower alone on its own directory whose Chrome STARTED but missed nodriver's fixed ≈2.75 s connect deadline (macOS/ARM64, run 35150887345 attempt 2). **Master itself is never reserved** (the release would have to live on the close path, and a leaked reservation forces every later spawn to clone forever), and **the retry never waits for the sibling wave**: `BrowserManager._spawns_in_flight` is decremented in the `finally` around ONE attempt, so a spawn deciding its retry is not counted in it (pinned — the count reads 0 there), and every member of a failing wave would read a number excluding all the waiters and be released together. A wait gated on that counter cannot see the herd it exists to break up. Whether a directory is busy is NOT decided here: `_profile_hold` is a two-line adapter over `profile_lock.profile_hold`, and `_profile_has_running_browser` is that answer as a bool (the name is kept — every sweep, trim and selection site reads it). When a held NAMED profile forces the walk to `<name>-N`, the selection dict carries `requested_user_data_dir` / `walked_to` / `walk_reason` (F-871) — one field set, surfacing through `spawn_diagnostics.profile_selection` because `_public_profile_selection` copies the dict whole. **That walk is CONDITIONAL since F-915 and both held branches ask one rule, `profile_target.hand_over_or_refuse`** — see that row: what stays HERE is which directory is being asked about and what the copy is taken from. A held NAMED session is its own copy source (there is no closed form of it), while the held SHARED one keeps the seed, so its copy still comes from a directory nothing is writing to and only the jar comes live; the SHARED branch reads its `Hold` ONCE where a liveness bool used to do, because the refusal needs the REASON too and a second walk of the process table would be a second answer one line apart. `_fallback_profile_selection` takes the same `driven` witness and is the SECOND DOOR onto that rule (F-834 widened it to all three roles, so left without one it answers a held shared session with exactly the clone the resolver refuses, one spawn failure later); it also carries a hand-off the previous attempt was making onto the retry through `profile_target.still_driven_source`, whose `driven` re-ask is fail-closed insurance production cannot reach (one immutable witness per spawn; see that row) — whether a source is STILL ours is settled a whole browser launch later, at `_seed_cookies_over_cdp`. `_live_seed_fields` is the ONE stamping of `LIVE_SEED_KEY` + `HANDED_OVER_KEY`, because THREE branches now produce a live source and a second spelling is how one of them would come to name a source another does not. **What the SEED is, and what a request may name, is `profile_seed`'s** (F-892/F-894/F-895/F-896): this file keeps the three wrappers every other site calls by name (`clone_is_auto`, `clone_is_named`, `_snapshot_needs_refresh` — now a one-line binding of `profile_seed.needs_refresh` to OUR two directories) and it is **the only thing that knows WHERE those directories are**, which it hands over as a `profile_seed.Roots`. `require_allowed_user_data_dir` is THE gate and it does three things in one call (F-896): `profile_seed.profile_request` reads `session` + `user_data_dir` as ONE request, `profile_seed.require_allowed` refuses what may not be opened, and it ANSWERS the anchored DIRECTORY — which is load-bearing, because `adopt_held_profile` runs in front of selection and matches a directory, so a raw `"default"` matched a name nothing holds and fell through to a copy. **`resolve_profile_selection` calls that same gate** rather than re-composing `require_allowed` itself, so "where does this land" has one answer and not two agreeing ones; the shared profile — by PATH or by the name `default` — is then folded off the ANCHORED path to the `default` ROLE, which is what makes `close_instance` refresh the seed afterwards. The seed PATH is REFUSED. **`_copy_profile_tree` now REPORTS a refused copy** (`TARGET_IN_USE`) instead of returning as though it had copied, and `_refresh_master_snapshot_if_safe` turns that into `snapshot_refreshed: False` + `snapshot_error: SNAPSHOT_IN_USE` — a refresh whose target was held by a live browser shipped as a success, about a seed not one byte of which had moved (F-893). `_refresh_snapshot_if_stale` is the ONE pre-copy freshen both copy paths call; they had the same two lines each, which F-892 would have had to widen twice. `_require_copied` is the guard the other two copy sites pass the answer to — unreachable for them today (both copy into a directory they have just found free, with no `await` between check and copy) and pinned by AST so no THIRD site may discard it. `_public_profile_selection` is where F-895's `seeded_from` / `seeded_at` / `seed_changed_since` are stamped — one site, every role, read from the marker on disk — and since F-898 it is also **THE one line between the resolver and its caller**: it pops `LIVE_SEED_KEY` (`seed_live_source`), the internal key the resolver stamps with the SOURCE DIRECTORY when the copy it just made came from a session this backend drives. `spawn_browser` reads that key off the dict the resolver returned, hands the jar over, and reports only counts — so the path never reaches `spawn_diagnostics` and the dict stays copied whole everywhere else. **The `driven` witness is threaded, not fetched**: `require_allowed_seed_from`, `_seed_source`, `_seed_source_for_copy` and `resolve_profile_selection` each take it as a keyword defaulting to `profile_source.NOTHING_DRIVEN`, so this module still learns nothing about `BrowserManager` and an un-updated caller keeps 2.1.12's refusal. One async snapshot per spawn (`cookie_handoff.driven_profiles`) feeds the sync predicate every one of those sites asks, which is what keeps "is this source ours" a single answer across the pre-flight gate and the copy. **F-898 also paid its own LOC**: this file was at exactly 1000 of a budget that ratchets DOWN only, so the trim half (`_dir_size_bytes`, `_regenerable_dirs_in_profile`, `_regenerable_size`, `_trim_profile_regenerable`) moved to `profile_copy` — its true home beside `REGENERABLE_NAMES` — rather than a `GRANDFATHER` row being re-added to a file whose own history is F-897 DELETING one. `require_allowed_user_data_dir` is PUBLIC because `tool_sections/browser_management.spawn_browser` must ask it before the F-888 re-attach (see `profile_seed`'s row). **`require_allowed_seed_from` is its F-897 twin and is PUBLIC for a sharper version of the same reason**: it takes the ANSWER of the first gate (the directory the caller's session request MEANS, so "is this the shared session" and "does it already exist" are asked about a directory and not a string) and refuses a `seed_from` with no new session to apply to — and `spawn_browser` must ask it AHEAD of `adopt_held_profile`, because a session whose browser is still running is a session that EXISTS, so without it `--from` would be silently dropped by an adoption in exactly the case a caller most wants to be told about. **That gate also asks the SOURCE question and DISCARDS the answer** (F-897 review S1): the source-is-open, source-missing and source-reserved refusals are raised inside `profile_source.seed_source`, which runs under the resolver INSIDE `spawn_browser`'s `try`, so they reached the caller re-labelled `Failed to spawn browser: …` — a spawn that never started, reported as one that failed, which is verbatim what the comment above the pre-flight forbids. An inner `except ToolError: raise` does NOT fix it (it still unwinds into the enclosing `except Exception`), so the question is asked twice on `require_allowed`'s own precedent. The pre-flight ask is ADVISORY and the resolver's stays authoritative — "is it open" is a fact with a lifetime and the honest read is the statement before the copy with no `await` between; the pre-flight turns the common case into a clean refusal and narrows the window to microseconds rather than to zero. `_seed_source` is therefore SIDE-EFFECT-FREE, and it is THE one binding of `profile_source.seed_source` to our four directories and to `_profile_has_running_browser` (review N4 — `_seed_source_for_copy` calls IT rather than spelling the same binding a second time). **How OFTEN that question is asked is `check_source`'s** (memo review S): the gate takes it and the RESOLVER's own call passes False, because that ask already sits inside the try where a source refusal is re-labelled anyway and `_seed_source_for_copy` raises the same sentences one statement later with no `await` between — so a walk of the process table is paid exactly where its answer is USED, the pre-flight's and the read before the copy, and the third is deleted (review N2 measured three walks for one source against an unseeded named spawn's one, and `_profile_hold` is a full psutil cmdline scan). A 1 s memo in `profile_source` bought the identical saving and was REPLACED by the flag: same cost, no process-global state, no clock, no reset hook and **`_seed_source_for_copy` is the one the resolver calls**, adding `_refresh_snapshot_if_stale` for the shared session — the only source that HAS a seed to freshen. Taken twice that freshen would copy a whole profile for a spawn about to be refused; left in the shared function a gate whose job is asking questions would write to disk. The two may answer differently on a first run (no seed yet → the live shared dir; after the freshen → the seed), which is harmless precisely because the pre-flight's answer is thrown away. `resolve_profile_selection`'s `source_override`/`source_kind` pair is ONE `profile_source.SeedSource` argument now (`override`) — a path and the word recorded for it are decided together, and two parameters let a caller record a copy as having come from somewhere it did not. **HOW a profile is copied is `profile_copy`'s** (F-897) — `REGENERABLE_NAMES`, `ignore_names`, `copy_file`, `copy_delta`, `rmtree_robust`; what stays here is the POLICY around a copy, and that split is what took this file under the 1000-LOC default and DELETED its `GRANDFATHER` row. **F-914/F-915 paid the same way, twice**: the held-target RULE went to `profile_target.py` (it deserved its own home — a policy where `profile_lock` is a fact) and what an EVICTION means went to `clone_trash.py`, because the rule alone did not pay for itself against a budget that ratchets DOWN only. This file still decides WHICH clones exceed a cap and must go; what "go" means, and for how long it can be undone, is that leaf's. Its `PTH105`/`SIM105` per-file ignores SHRANK by exactly the two codes that left with the trash mechanism. **`_clone_needs_refresh` and `_profile_refresh_days` are DELETED** — no callers anywhere, and the first carried the marker filename's second spelling; `BROWSER_PROFILE_REFRESH_DAYS` keeps its `settings.py` field (`extra="forbid"`, so an existing `.env` naming it must still load) but nothing presents it as live — README calls it inert, `.env.example` comments it out, the two example configs no longer set it, and the field carries the reason |
| `profile_seed.py` | **THE one home for a profile's SEED — the marker that records it, the files that witness a login in it, the seeds' own names, and THE WORD A CALLER SAYS** (F-892, F-894, F-895, F-896; F-897 for the name rule two fields now share). Four questions, one subject. **F-896's half**: `DEFAULT_SESSION` (`"default"` — F-894 reserved the word as a refusal explicitly ahead of this vocabulary, and it now MEANS the shared profile; `RESERVED_NAMES` drops to the two MECHANISM names), `is_default_name` (the BARE form only — `sessions/default` is refused instead, because answering the shared profile for it would be F-894's substitution one separator away from the word we teach), `profile_request` (THE one reading of `session` + `user_data_dir`: the alias resolves TO the new spelling, both-with-different-values RAISES rather than applying an invisible precedence, and `session` refuses a path — drive through `PureWindowsPath` for `reserved_reason`'s measured reason, both separators literally, because a backslash is a separator on Windows and a filename character on POSIX), `Roots` (the four directories, one tuple, because they always travel together and `clone_storage` owns where they are) and `needs_refresh` (moved from `clone_storage`, where every term in it was already this module's). `seed_name` answers `"default"` for BOTH the shared profile and its seed — they are one session to a user, and a caller told `seeded from master-snapshot` learns a word they can neither type nor act on; staleness is unaffected because `provenance` reads the marker's recorded `source` PATH. (1) **`LOGIN_WITNESSES`** — the profile-relative files whose mtime moves when a login lands, led by `Default/Network/Cookies` because that is Chrome >= 96's cookie jar and **MEASURED 2026-09-20 on the live master and its snapshot, the pre-96 `Default/Cookies` this list used to name is ABSENT in both while the Network one is 524,288 B**: so the `pre-clone-stale` refresh could not see a pure COOKIE login at all — Google SSO, Amazon Seller Central, any SPA that never offers to save a password — and depended on `Login Data` (only when Chrome saves a password) or `Web Data` (autofill). The legacy path is KEPT beside the current one (a carried-over profile still has it; an absent file costs one stat). **It is spelled here and NOWHERE else, not even in a docstring** — `tests/test_profile_seed_truth.py` greps the package and fails on a second spelling, because a second copy is exactly how the shipped list came to name a file Chrome stopped writing four years ago. (2) **The marker** — `MARKER_NAME`, `read_marker` / `write_marker`, `is_auto` / `is_named` (F-201's fail-safe-on-a-legacy-marker ruling moved here with the code that applies it), and F-895's two new keys, `seeded_from` (the seed by NAME, the word a user can say) and `seeded_at`, written BESIDE `source`/`source_kind`/`created_at` so a 2.1.10 reader still finds what it looks for. `provenance` is what `spawn_diagnostics.profile_selection` and the `profiles` CLI verb both report, and `seed_changed_since` asks `LOGIN_WITNESSES` — the SAME list, never a second one. **A legacy marker reads as `unknown` with `seed_changed_since: None`, and `created_at` is deliberately NOT substituted for `seeded_at`**: when a directory was made is not a claim about which seed it was made from, and unknown provenance reported as fresh is the silence this finding closes. (3) **`anchor` + `RESERVED_NAMES` / `reserved_reason`** — where a `user_data_dir` request LANDS (anchoring moved here so the reservation can be asked about the directory a request MEANS, not the string it was written as) and whether it may. **`anchor`'s `inside(...)` call is a DISAMBIGUATION and never a guard** (F-901) — it asks "did the caller already write the `sessions/` prefix", and the branch it does not pick was returned unchecked, which is how `user_data_dir="."` opened the CLONE ROOT and `".."` the BROWSER-SESSION ROOT (measured; `Path("..").name` is `""`, so no name-keyed refusal could see either). Whether a landing is allowed is `reserved_reason`'s, asked of the LEXICALLY NORMALISED path `anchor` now returns for a relative request — `os.path.normpath`, deliberately not `resolve()`, because it must fold `..` for a directory that does not exist yet, must not follow a symlinked session root (F-888's re-attach matches that string) and must cost no stat on the spawn path; what it cannot see, a component the OS folds away (`...` on Windows), is caught by the ROOT refusal below, which compares through `same_dir` and resolves BOTH sides. An ABSOLUTE path is never normalised — F-896 promises it back byte-for-byte. Three refusals: a reserved NAME (`user_data_dir="master"` resolved to `<root>/sessions/master`, not `<root>/master` — MEASURED, that directory exists, 0.46 GB, marker `explicit-master-snapshot`, created 2026-09-11, i.e. someone asked for the master by its documented name and silently got a nine-day-old clone); the SNAPSHOT path (a browser driven there writes into the seed every later session copies from — F-893's precondition, closed at the door it came in through); and a DRIVE-QUALIFIED path that is not absolute (`C:foo`, what a Windows absolute path becomes once a lenient string layer has eaten its backslashes — `Path.is_absolute()` is False for it, so the resolver anchored it as a bare name: MEASURED, that is exactly and reproducibly how `sessions/stealth-mcp-browser-sessionssessionsstealth-chrome-devtools-mcp-f876e3d7f2ec` came to exist) — and that third test reads **TWO flavours on purpose**: the DRIVE through `PureWindowsPath`, because a drive is a Windows concept (`PurePosixPath("C:foo").drive` is `""`, measured) and reading the HOST's flavour made the refusal Windows-only and the pin RED on all six POSIX CI cells, and "would this be ANCHORED rather than opened" through plain `Path`, the flavour `anchor` actually uses — so `anchor` keeps its one home and `C:foo`, which is not absolute under either flavour, is refused identically everywhere. `default` is reserved ahead of the vocabulary that will use it, because a friendlier word for the same trap is still the trap. **F-901 adds the two that are about the LANDING rather than the name**: the clone root and the browser-session root are refused THEMSELVES, through either door (an absolute path reaches them exactly as `"."` did, and they are where profiles are KEPT — a browser opened on one writes its own profile files in among every session's) — two equalities and nothing wider, so a directory that merely lives near them stays the caller's own; and a RELATIVE request that lands outside the clone root at all is refused, which is what closes the `".."` walk. **Those two ask their question two different ways and it is not an inconsistency**: the ROOT pair compares through `same_dir`, which RESOLVES both sides, so a link pointing AT a root and a component Windows folds away (`...`) are both caught; the WALK is `_inside_lexically`, asked of the strings alone, because a session directory that is a symlink or a junction to storage elsewhere RESOLVES outside the clone root and a resolving test refused that working configuration through both spellings (review S1). Every escape F-901 closes is folded by `normpath` before either runs, so the lexical question costs the rule nothing; what it gives up is a link INSIDE the clone root pointing outside it, which is the caller's own business exactly as 2.1.11 said. `_landing_words` is why a refusal that reached a directory through a link names the one it really opens — the shipped message told an operator that `<clones>\self` "is a directory profiles are KEPT in", naming a path visibly inside the root. `sub/../acme` is ALLOWED and canonicalised to `sessions/acme`: after normalisation it is the directory it already meant, so one directory keeps one recorded answer rather than a second spelling being invented for it. The MASTER path is deliberately not refused and is not even a parameter of `reserved_reason` — the resolver settles it one line earlier. **`require_allowed` is the raise-shaped gate and it is asked TWICE on purpose** (review M1): `spawn_browser` asks it BEFORE `browser_reattach.adopt_held_profile`, which runs in front of profile selection and matches the requested directory against live browsers — so an absolute snapshot path WITH A BROWSER ON IT was re-attached to and the resolver, where the reservation used to live alone, never saw the request; that is the exact machine state F-893 measured, and the resolver-only pin passed because its fixture had no browser there. The resolver asks again because it is public with its own callers. It is a path decision plus one `Path.resolve` and one `exists()`, so twice is two stats; it ANSWERS the anchored path, so `anchor` still runs once per selection; and the master still reaches the re-attach in front of it, where being adopted is the right outcome. At the SPAWN it is asked AHEAD of the F-808 guard, because a caller-shaped refusal outranks a host-shaped one and a headless host answered F-808 for every reserved path. **The phrasing of where a session came from is still this module's** (F-897): `seed_sentence` (the ONE phrasing of `provenance`'s three fields, because `stealthy profiles` and `stealthy spawn` both say it; PHRASING only — whether the question arises is the caller's, and the two answer it differently on purpose. **`seed_changed_since` has THREE values and each gets its own ending** (review N1): True shouts `SEED CHANGED SINCE` because it is an ALERT, False says nothing because "up to date" on every spawn is noise, and None — no login witness readable in the recorded source, which a DELETED or renamed source guarantees — says `(source unreadable)`, lowercase and parenthetical because it is a caveat about what could be READ and one register for both would teach a reader to ignore both. It used to render byte-identically to False, so a vanished source read as a fresh one. It deliberately does not say "deleted": this function sees three fields and not a path, and "exists but holds no login" produces the same None). `require_name` is F-896's name rule with the FIELD as data, so `session` and `seed_from` share one rule and each still names the parameter the caller typed — a caller told about `session` when they wrote `seed_from` edits the wrong argument. **`_names_nothing` is asked by THREE fields, not two**: F-896's delta gave `session` and its alias one sentence for a value that names no profile, and `require_name`'s empty branch raises that same sentence rather than a hint each caller composes, so `seed_from` cannot become a third wording of it — its last clause is true of all three, since omitting `session` opens the shared session and omitting `seed_from` copies it. Only the PATH refusal still takes a per-field hint, because the ESCAPE genuinely differs. **WHICH SESSION a new one is copied from is `profile_source`'s** — see that row. A leaf: stdlib plus `tool_errors` (which imports nothing from `embedded`); the master and snapshot dirs, the profile, and the `inside` predicate (`clone_storage._is_relative_to`, its one home) all arrive as ARGUMENTS |
| `profile_source.py` | **THE one home for `seed_from` / `--from` — WHICH SESSION a new session is copied from, and whether there is a NEW session for that copy to apply to** (F-897) — `seed_request` (the one reading of `seed_from`/`--from` — a NAME through `require_name`, never a path, because the provenance it writes is a word the caller can pass back to `session=` and an arbitrary directory has no such word), `require_new_session` (FOUR refusals, one sentence read four ways: there has to be a session, it has to be the caller's own, it has to be a SESSION at all, and it must not already exist. The third is review M1's measured silent drop — the resolver only seeds a directory it is about to CREATE under the clone root, so a `user_data_dir` landing anywhere else passed every gate and was then never used: no copy, no marker, `seeded_from: unknown`, not one word to the caller; it is decided here because the rule is this function's, and `clone_storage` supplies `inside_root` exactly as it supplies `shared`, both REQUIRED so a second caller must decide rather than inherit a default. The fourth is a REFUSAL rather than a no-op, which lies about what the caller got, or a re-seed, which overwrites a login typed by hand, and it NAMES where the existing session actually came from. **Every sentence here names BOTH spellings of the remedy** — `session=<name>` and `--session NAME` — because the CLI prints the backend's message verbatim and a caller told to pass an argument they never typed goes looking for the wrong thing; keying the wording on who asked would need a second message home), `SeedSource`/`seed_source` (the directory and the word recorded for it, decided together so nothing can record a copy as having come from somewhere it did not; `None` and `"default"` are ONE path; every other source is refused while a live browser holds it, BY NAME, because `profile_copy.copy_file` answers a locked file by skipping it and the gap cannot be enumerated afterwards — the shared session is the exception only because the product maintains a separate closed copyable form of it, and per-session seeds were considered and declined at ~0.47 GB plus a second copy of F-892/F-893's whole lifecycle each. **Since F-898 a HELD source is two cases and this is where they part**, which is why `seed_source` takes a second witness, `driven`: a source THIS backend drives answers `LIVE_SESSION_KIND` and is copied exactly as before AND then has its jar handed over the CDP connection we already hold to it (`cookie_handoff`, `clone_storage.LIVE_SEED_KEY`, `tool_sections/browser_management._seed_cookies_over_cdp`), so the one thing the file copy provably loses is carried by the one mechanism that can carry it; a source held by a browser we do NOT drive keeps F-897's refusal to the letter and gains a sentence saying WHY — there is no CDP connection of ours to read it through — plus the three ways out (close it, spawn it through this backend and seed from it while it runs, or seed from `default`). The witness is a CALLABLE with `NOTHING_DRIVEN` as its default, so it FAILS CLOSED: a caller that does not pass one gets 2.1.12's behaviour and a new call site cannot inherit a live hand-off by accident. **`default` is a FOURTH shape and `_default_source` is its home** (F-898 review M1): the `default`/unset branch used to return ABOVE the held/driven split, so the one source a human actually logs in to — whose browser normally survives its backend since F-888, and whose seed is never refreshed while it runs — got no hand-off at all, and under it sat a silent copy of the LIVE shared directory whenever no seed existed yet, i.e. this finding's own zero-cookie harm in the default path reported as an ordinary success. It now answers three ways: driven → copy the SEED and hand the live jar over; held by a Chrome we do NOT drive → copy the seed and refuse NOTHING, because the closed copyable form exists and is exactly what F-897 promised (`seed_changed_since` already says it is behind, and refusing would break `--from default` on the machine state that is most normal); no seed AND running → REFUSED by name, nothing created, remedy one `close`. **That is also why `SeedSource` carries `live` as a third field rather than a flag on `kind`**: for a named source the copy source and the live source are one directory, for `default` they are two, and `clone_storage` must read `seed.live` — reading `kind` is literally what made this a no-op. Cost: one process-table walk, and ONLY for a spawn that could actually receive a hand-off — `_default_source` asks `driven(...) and held(...)` in that order, because `driven` is a lookup in a snapshot this spawn already took while `held` is a psutil walk of every process on the machine, so the free witness short-circuits and the conjunction's ANSWER is byte-identical either way. Asked the other way round, as it first shipped, every named spawn paid the walk to learn the shared session is not one of ours. Pinned from BOTH sides in `test_seed_from_session.py` — an unseeded named spawn still reads `["beta"]`, a driven one reads `["gamma", "master"]` — so neither the order nor a third ask can move unnoticed), and nothing at all about how OFTEN any of it is asked. A 1 s memo over the liveness witness lived here between the delta review's N2 and the memo review's S, and it is GONE: nothing in this module remembers an answer, so no walk of the process table can be served from a stale one. What that memo saved is saved by `clone_storage.require_allowed_seed_from`'s `check_source` instead — False for the resolver's own call and nowhere else — which is the same arithmetic with no process-global state, no clock and no reset hook. `seed_request` tests FALSY, so `seed_from=""` is "not given" and seeds from `default` exactly as an unset `--from` does (F-896's `2953e8d` reasoning: `""` for an optional string is what a language model sends, and saying nothing here already has a right answer); anything non-empty that strips to nothing still raises. It left `profile_seed` when the merge that brought F-901 in put that module at 1112 lines of a 1000-LOC budget that ratchets DOWN only: the cut is this question's surface rather than a raised cap (`dom_handler`→`text_entry`'s precedent, `cli_call`→`cli_render`'s), and the line between the two is WHEN the question is asked — `profile_seed` is consulted on every spawn, nothing here runs unless a caller wrote `seed_from`, and that module's own docstring still describes four questions without naming this one. Not `profile_copy` either: that one owns the MECHANICS of copying a directory and knows nothing about sessions, while everything here is policy about which session may be NAMED — the two joined by the one measured fact that `profile_copy.copy_file` answers a file Chrome holds open by skipping it, which is the whole reason `seed_source` refuses a live source BY NAME. They meet at exactly three points, so neither grows a second copy of the other: `require_name` (one name rule for `session` and `seed_from`), `require_allowed` (a source is anchored and reserved by the same gate a target is) and `seed_sentence`/`provenance` (the already-exists refusal names a seed in the words `stealthy profiles` uses). A leaf: stdlib plus `profile_seed` and `tool_errors`; the four directories arrive as a `profile_seed.Roots` and the containment predicate and the "is a browser holding this" witness as callables, so this module never learns where a session root is and `clone_storage` stays the only thing that knows |
| `cookie_handoff.py` | **THE one home for handing a RUNNING browser's cookie jar to a fresh one over CDP, and for knowing which profile directories THIS backend drives** (F-898) — `read_jar`/`write_jar` (`Storage.getCookies`/`Storage.setCookies`, on the BROWSER-level connection; measured, on a page session `getCookies` never answers at all, which is the first thing a plausible hand-off gets wrong because every other cookie surface here is tab-scoped), `as_param`/`params`/`CARRIED_FIELDS`, `hand_off`/`Handoff`, `HandoffError`/`failure`, and `Driven`/`driven_profiles`/`NOTHING_DRIVEN`. Its sibling is `profile_copy` and the boundary is one MEASURED fact: a file copy of a running source carries **zero** cookies (the SQLite jar is held open and skipped) and cannot say which file mattered, which is why `profile_source.seed_source` refused a live source at all. **The translation is DERIVED, not typed** — the shared field names of nodriver's own `Cookie` and `CookieParam` — minus exactly two, each for its own reason: `expires` needs a TRANSLATION (CDP reports a session cookie's expiry as `-1`, a marker and not a date, and OMITTING it is what makes the target treat the cookie as a session cookie too — the one shape a file copy can never carry, since it is never on disk), and `same_party` is F-902's fix read from the other side (Chrome 153 does not send it and `cdp_transport._RETIRED_COOKIE_FIELDS` SYNTHESISES it, so forwarding it writes our own invention back into a browser). What the derivation drops unprompted is also right: `size`/`session`/`partition_key_opaque` have no `CookieParam` counterpart, and `url` is `CookieParam`'s alone — a jar read carries `domain`/`path` and an invented `url` would change which host a cookie belongs to. It goes through `Connection.send` and never a socket of its own, because that is the one door `cdp_transport` protects. **PII: no cookie NAME and no cookie VALUE may leave this module** — counts, the CDP METHOD and an exception TYPE, `cdp_transport._report`'s discipline applied to the one subsystem whose whole payload is credentials; `HandoffError` is the ONLY exception whose text is ever repeated (this module wrote it), everything else is reported by type alone, and the caller must NOT pass a failure to `debug_logger.log_warning(error=…)` because `exc_info` would carry Chrome's answer to a command whose parameters WERE the jar. `_step` names the method that ACTUALLY failed and never one the caller stamped ahead of the call (a failed READ was reported as `Storage.setCookies`, caught by the pins), and `_translated` is its sync sibling for the one step that makes no CDP call at all — a jar entry `CookieParam` will not take was reported as a bare type with no step on it, so the translation reuses `_failed` rather than growing a second phrasing of "which step, which type". `driven_profiles` reads the ENTRY's `options`, **never `BrowserInstance.user_data_dir`, which does not exist** — that reader answered None for every real instance and the hand-off would never have fired, silently, while every hermetic pin passed because the double offered the attribute the product does not have (`ty` caught it; the absence is pinned). A leaf: `nodriver` + `profile_seed` for the one path comparison, both browsers and the manager as arguments |
| `profile_copy.py` | **THE one home for the filesystem mechanics of a Chrome profile directory — copying one, removing one, and TRIMMING one** (F-897, F-898). `REGENERABLE_NAMES` + `ignore_names` + since F-898 the deleting half (`regenerable_dirs`, `regenerable_size`, `trim_regenerable`, `dir_size_bytes`, moved here from `clone_storage`, because two paths read that list in OPPOSITE directions — a copy excludes these names, a trim under storage pressure deletes them — and they lived one import apart from drifting; WHEN a profile is trimmed and which are idle enough to trim stay `clone_storage`'s storage-cap question): what a copy leaves behind, ~98 % of a profile by size and all of it rebuilt on the next launch; ONE home because two paths read it in opposite directions (this copy excludes them, `trim_regenerable` one function below deletes them under storage pressure) and `profile_lock` names three of them for the DIFFERENT question of what Chrome's process singleton MEANS. `copy_file`/`COPY_ATTEMPTS` + `copy_delta` + `rmtree_robust`: what all three do about a file Chrome is holding open, which is RETRY briefly then make progress anyway — and **the cost of that answer is the whole reason `profile_source.seed_source` refuses a live SOURCE by name**: a skipped file is a login silently missing from the copy and this module cannot say which one mattered. Extracted from `clone_storage` by the finding that made the source of a copy a caller's choice, which is also what lets that module state its policy without the mechanics in the middle of it (`page_storage` leaving `browser_manager`, same shape). `ignore_names` takes the NAMES alone — the `directory` parameter was an unused `shutil.copytree(ignore=…)` vestige, and a parameter nothing reads claims the answer depends on where you ask. A leaf: stdlib + `debug_logger`; every path arrives as an argument, and it knows nothing about session roots, seeds or roles |
| `profile_target.py` | **THE one home for "the profile this spawn asked for is already OPEN"** (F-914, F-915) — `hand_over_or_refuse` and `still_driven_source`. The rule is the OWNER's, it has exactly two outcomes and it is not to be re-litigated: a browser THIS backend drives can be asked for its cookies over CDP (`cookie_handoff`), so the spawn goes ahead on a directory of its own and is seeded from the holder's LIVE jar; anything else — another backend's Chrome, or the human's own — is REFUSED by name, because there is no connection of ours to read that jar through and `profile_copy.copy_file` answers a file Chrome holds open by SKIPPING it, so the copy carries no cookies and cannot say which ones. "Substitute but report it" was offered and explicitly not chosen. What the substitution cost is measured in the findings: 17 walked directories on the owner's machine, one at `-22`, every one of them seeded from a snapshot that is refreshed only while the shared session is CLOSED (`clone_storage._refresh_master_snapshot_if_safe` answers `seed_error: "default-in-use"` otherwise) and was five and a half hours behind — "the master profile was erased / I have to set up the credentials again", from the user's chair. **The TARGET-side twin of `profile_source.seed_source`**: same verdicts through the same witness, about the directory a spawn is about to OPEN rather than the one it copies FROM, which is why it is its own module and not a fifth question in that one, whose whole surface is `seed_from`. A sibling of `profile_lock` and not part of it — that module answers *is this directory held, and by whom*, which is a FACT; this answers *what do we do about it*, which is a POLICY, and the two sat welded together inside `clone_storage` until F-914 needed the policy in two branches at once (`resolve_profile_selection` AND `_fallback_profile_selection`). The two witnesses are deliberately NOT asked in `profile_source`'s `driven and held` order and the saving that order buys does not exist here: both call sites already hold the `Hold`, because "it is held" is what sends a spawn down this path at all, so the psutil walk is paid either way. `still_driven_source` is the retry's half — the hand-off a failed attempt was making is CARRIED onto the re-selection, because otherwise the retry copies from the seed again and nothing asks for the jar that made the first attempt's session correct. **Its `driven` re-ask is fail-closed insurance that production cannot reach, and the row claimed otherwise until the F-914 review**: there is ONE `cookie_handoff.Driven` per spawn — an immutable snapshot taken in `spawn_browser` and handed unchanged to the first selection and to every fallback — and `clone_storage.LIVE_SEED_KEY` is only ever stamped on a path that same object has already approved, so the re-ask answers True for every value it can be handed and a source that CLOSED is not what it drops. That fact has a lifetime and is settled where it is OBSERVED, one whole browser launch later: `tool_sections.browser_management._seed_cookies_over_cdp` re-derives a FRESH snapshot at the moment of use and degrades to `seeded_via: "copy"` + `cookie_handoff_error` rather than failing the spawn. **The refusal names the SESSION and the HOLDER and never a PATH** (F-869/F-877: a profile path names the operating user, and the message reaches the client, the durable log and Sentry at once), and it names BOTH spellings of each remedy, `profile_source`'s discipline. **Its last clause is the one thing that DIFFERS between the two branches, and it is data rather than a second message** (`_fresh_session_holds`, on `profile_seed.require_name`'s precedent): every new session is a copy of the SEED and the seed is the shared session's own copyable form, so "holding none of that session's logins" is true of a NAMED holder and FALSE of the shared one, where the copy carries exactly those logins as of the last time that browser was closed. One sentence served both until the F-914 review, and in the machine state that is normal for the owner it told them the one remaining way forward loses the logins it in fact keeps. **What a WALK means changed with it**: F-871 made the walk to `<name>-N` reported and left it unconditional; the only walk that survives is one taken because we drive the holder, so the new directory is a COPY of that holder with its jar handed over — every sentence about a walk, the resolver's, the tool's `warning` and the CLI's line, has to be true of THAT walk and not of the seed copy that no longer happens. A leaf: `profile_lock` for the type it is handed, `profile_seed` for the one naming rule, `tool_errors` for the convention; the directories arrive as a `profile_seed.Roots` and the "do we drive it" witness as a callable, so it never learns where a session root is |
| `clone_trash.py` | **THE one home for what an EVICTION means** — `TRASH_DIRNAME`, `trash_dir`, `retention_seconds`, `trash`, `purge_expired`. An auto-clone the storage cap takes away is RENAMED into `.trash` inside the clone root — instant, same volume — and purged only after a retention window (`STEALTH_MCP_CLONE_TRASH_RETENTION_HOURS`, default 24 h; `<= 0` restores delete-immediately), because the worst incident this project has had was a storage sweep irreversibly deleting a logged-in profile, and a rename is what turns that class of bug from data loss into something an operator can carry back by hand. `TRASH_DIRNAME` is PUBLIC because every clone-root scan has to skip it by name: a scan that re-selected, re-sized or re-swept its contents would count evicted clones against the very cap their eviction was meant to satisfy and would evict them a second time. The eviction time is stamped (`os.utime`) because a rename preserves the clone's ORIGINAL mtime and retention is measured from eviction, not creation. It left `clone_storage` when F-914/F-915 put that file over a 1000-LOC budget that ratchets DOWN only (`dom_handler`→`text_entry`'s precedent, `clone_storage`→`profile_copy`'s twice over), and the line between the two is the question each answers: that module decides WHICH clones exceed a cap and must go, this one decides what going MEANS and for how long it can be undone — nothing here reads a marker, a role or a seed. Never raises: a refused move answers `None`, and a rename that fails (a Windows lock) falls back to a best-effort delete, because the cap still has to be honoured and that is strictly no worse than the behaviour this replaced. A leaf: `profile_copy` for the one robust delete and `settings` for the one knob; whether a directory is BUSY arrives as a CALLABLE, so F-871's answer stays in `profile_lock` and reaches here through `clone_storage` |
| `aspect_options.py` | **THE one home for "a caller passed an option this aspect no longer has"** (F-851) — the retired-option table (`RETIRED`), the signature filter (`accepted`) and the per-aspect warning (`note`). A NAMED tolerance, never a widened `except`: a stale kwarg used to raise `TypeError` at the kwarg-binding site, which is OUTSIDE `gather`'s per-aspect isolation, so one retired string failed the whole complete clone |
| `js/` | the 7 browser-side extraction scripts (`extract_styles.js`, `extract_structure.js`, `extract_events.js`, `extract_animations.js`, `extract_assets.js`, `extract_related_files.js`, `comprehensive_element_extractor.js`). Every script the ENGINE evaluates ends in `return JSON.stringify(…)` — success return AND not-found guard (F-872); `tests/test_cloner_schemas.py` pins it, deriving the set from the filenames in `cdp_element_cloner.py`'s own source, so a new aspect joins the pin the day it is wired up |

**Animations derivation** (six leaves downstream of the engine's animations
aspect; `cdp_element_cloner.extract_element_animations` -> `analyze()` is the ONE
call path in, so DOM extraction still has exactly one home). Reading order is the
dependency order — each imports only the ones above it, and none imports `server`.
| File | Owns |
|---|---|
| `animation_facts.py` | **THE one home for READING what the collector sent** — JSON field readers (`as_obj`/`as_rows`/`as_number`) and CSS token readers (comma lists + the list-cycling rule, `<time>`, keyframe selectors, easing classification, `specificity`, `prefers_reduced_motion`, `own_compound`). Also **THE one home for a derived value's confidence**: `Derived` welds a value to the confidence its own branch produced, and `claim`/`put` emit the pair or omit the field — a caller physically cannot supply a confidence it did not receive (F-850). Also **THE one home for the payload's shared value types** — `Facts`/`Record`, the `Caps` bounds with `caps_from`/`cap_message`, and `warn` (the one way a record grows a warning, so no site can hardcode `warnings: []`); the cap DEFAULTS are sized for a language model and justified from measurement in a comment there, never as `STEALTH_MCP_*` env knobs (F-853) |
| `animation_source.py` | **THE one home for WHERE a declaration lives** (F-849/F-857) — locating a rule's AUTHOR text in the sheet the collector captured (`rule_span`, `source_span`, `author_declaration`, `keyframe_span_for`; never Chrome's `cssText`, which is a re-serialization that matches nothing on disk), the `Span` that KEEPS the offset the slice was taken at plus `line_column`, the openable location (`open_location`: a linked sheet's href, else the DOCUMENT that contains the `<style>` — and nothing at all for a constructed sheet), and, when nothing is locatable, WHICH of the three causes it is (`missing_source_reason` / `pointer_reason` / `indirect_property` / `inline_declarations` / `blocked_stylesheet` — inline `style=""`, a stylesheet actually WITNESSED as cross-origin, or an adopted constructed sheet; never a guess). Turning a url into a path on disk stays with `extract_related_files`, the one URL→file answerer |
| `animation_edits.py` | **THE one home for EDIT RECIPES** (M10) — the per-knob cascade winner (`winning_rule`: `!important`, then specificity, then document order — degrading when a functional pseudo-class or a computed-value mismatch makes it undecidable, with `_mismatch_reason` deciding WHICH somewhere from the facts), and the token addressing (`token_verdict`, `_swap`, `_stamp_position`) that makes `find`/`replace` mechanically applicable without dropping the rest of the declaration. It composes recipes out of what `animation_source` returns; it never goes looking for a rule's text itself |
| `animation_advice.py` | trigger attribution, interaction/conflict warnings (closed code set), stagger grouping, and the `summary`/`overview` prose (templates over the payload, so they cannot contradict it) |
| `animation_waapi.py` | **THE one home for reading the live `Animation` objects** — timeline typing (`timeline_from_waapi`), live timing and keyframes, the records for everything the declared CSS does not describe (`build_waapi`: `element.animate()`, transitions, descendants, pseudo-elements), and the declared-vs-live reconciliation (`adopt_live_timelines`, which drops `duration_ms` for a scroll/view timeline). Declared CSS is `animation_analysis`; this is what is actually RUNNING |
| `animation_analysis.py` | **THE one home for the animations schema** — owns `analyze()`, the ONE composition site, plus per-animation records, keyframe resolution, derived timing, direction-aware checkpoints, the transition inventory, the `CLAIM_FIELDS`/`JUDGEMENT_BLOCKS` registry the confidence invariant test walks, the ONE editability verdict (`stamp_editable`: `editable` is "some recipe here applies", `not_editable` names the knobs that do not — a whole-record yes used to read as a promise about every knob, F-857), and the summary reduction for records the caller did not select (`summarize_detail`, which stamps a visible `detail_level` — never a silent shape difference between records) |

**Network, hooks, execution, storage, debug**
| File | Owns |
|---|---|
| `network_interceptor.py` | `NetworkInterceptor` — capture + the body caps (`_store_response`) |
| `dynamic_hook_system.py` | `DynamicHookSystem` — first-match-by-priority request hooks |
| `dynamic_hook_ai_interface.py` | AI-facing API for creating/managing hooks |
| `hook_learning_system.py` | hook examples/training surface |
| `cdp_function_executor.py` | direct JS function execution via CDP. Its sends always carried `await_promise=True`, but the two WRAPPERS the caller's code runs inside were synchronous, which is F-883's shape reached through the other door: `inject_and_execute_script`'s inner IIFE and `call_discovered_function`'s `func.apply` (behind `call_javascript_function` AND `execute_function_sequence`) both put an async caller's Promise OBJECT in `result`, which `returnByValue` serializes as `{}` and reports as `success: true`. Both wrappers are `async` and both inner calls awaited now — fixed in THIS home, never by routing them at `script_evaluation`, which is `execute_script`'s seam and carries a different error convention. What they still do not do is distinguish a rejection from a throw in the shape they return: both land in the same `catch` and come back as `success: false` with `error.message`, which is this subsystem's KEEP contract |
| `cdp_params.py` | **THE one home for "a caller's JSON, as the type a CDP wrapper declares"** (F-861) — `typed`, which builds each `execute_cdp_command` argument into the type nodriver's generated wrapper declares for it (`from_json` for the generated classes, through `Optional[..]` and `List[..]`), read from the wrapper's own type hints so nothing is typed by hand; primitives and already-typed values pass through untouched, and a value the type cannot take raises `ToolError` naming the param and the type. Called from the ONE site `cdp_function_executor.build_cdp_call`, after F-816's name folding. A leaf: imports only `tool_errors` |
| `response_handler.py` | large-response handling + file fallbacks; **the one home for "can this payload survive the transport"** — `json_safe` (serializable, F-822) and `surrogate_safe` (utf-8-encodable, F-823) |
| `in_memory_storage.py` | `InMemoryStorage` — deliberately non-durable instance cross-check |
| `debug_logger.py` | in-memory debug log ring/view; `log_tool_failure` is the ring entry point for a failed tool call (ring only — the durable/Sentry-bridged log line is deliberately NOT written, F-835/F-782). `log_warning` takes an optional `error=` (F-869) forwarded as `exc_info`, so a warning ABOUT a caught exception carries its traceback; the ring shape is unchanged either way, which is what keeps `get_debug_view`'s contract byte-stable |

### Tombstones — do NOT route a change to these (they were removed)

| Gone | Use instead |
|---|---|
| `embedded/element_cloner.py` | `embedded/cdp_element_cloner.py` (consolidated, M5b) |
| `embedded/comprehensive_element_cloner.py` | `embedded/cdp_element_cloner.py` (M5b) |
| `embedded/persistent_storage.py` | `embedded/in_memory_storage.py` (renamed, M15) |
| `embedded/response_stage_hooks.py` | removed (M12a) |
| `env_utils.py` | never existed — env home is `settings.py` |
| `hot_reload` / `reload_status` **tools** | removed (M2) — code edits apply via a **fresh backend** (source-fingerprint eviction), not a live reload |

---

## Glossary

One meaning per term. Where a term is irreducibly overloaded, each sense gets a
distinct qualified name; the bare word is retired from ambiguous surfaces.

| Term | THE one meaning | Not to be confused with |
|---|---|---|
| **backend** | the shared `python -m … --transport http` process running FastMCP + 94 tools, one per display context | the stdio proxy; "the server" (ambiguous — avoid); the pre-2.0.4 "exactly one process per machine" reading ([DESIGN §2.7](./DESIGN.md#27-display-context-where-a-window-launched-here-would-be-seen)) |
| **stdio proxy** | the short-lived per-Claude-Code-session process bridging stdio ↔ the backend's HTTP | the backend |
| **MCP session** | FastMCP's `mcp-session-id` handshake token (created by `initialize`, discarded by the liveness probe) | a browser session; a Claude Code session |
| **Claude Code session** | one client connection = one stdio proxy instance | an MCP session; a browser session |
| **browser session / named session** | a `spawn_browser(session=…)` profile-backed browser instance. **The parameter is `session` since F-896** and it takes a NAME, never a path; `user_data_dir` is the DEPRECATED alias that resolves to the same request through `profile_seed.profile_request` and is the only one that still accepts a path. This row said `session_name=` until F-894 and taught agents a keyword that raised | any of the above; a "session root" |
| **`default` session** | `profile_seed.DEFAULT_SESSION` — the shared profile an unnamed spawn lands on, the one a human logs in to, and the one every new session is copied from. `session="default"` opens it; it is `<root>/master` on disk and `profile_role: "default"` in the answer (F-896) | its **seed** (below), the copyable form; a session OF YOUR OWN called `default`, which is refused because one word may not name two profiles |
| **seed** | the profile a new session is COPIED from at creation — `<root>/master-snapshot` by default, reported as `seeded_from: "default"` because to a caller it IS the `default` session in copyable form (F-895, renamed F-896), or since F-897 any session the caller named with `seed_from` / `--from`, reported by that session's own name. Recorded per session as `seeded_from` / `seeded_at` in its clone marker | the `default` session itself (the seed's own source, refreshed into it); a *reserved name* (below), which is what a caller may not call a session |
| **`seed_from` / `--from`** | `spawn_browser(session=NEW, seed_from=SOURCE)`: create NEW as a copy of the SOURCE session instead of of `default` (F-897). A NAME, through the same gate `session` passes — including F-896's empty rules, shared rather than copied: `""` is NOT GIVEN and `"   "` raises with the one `_names_nothing` sentence. CREATION-only, so an existing target RAISES. Unset means `default`, which is today's behaviour on the same code path. **A RUNNING source is allowed since F-898 when THIS backend drives it** — see *cookie hand-off* below; a NAMED one held by a browser we do not drive is still REFUSED BY NAME, because a copy of a live profile silently drops whatever Chrome has locked and nothing can say afterwards what was lost. **`default` — and therefore an unset `seed_from` — has its OWN three outcomes** (F-898 review M1): the copy always comes from the SEED, the live jar is handed over when we drive the shared browser, a `default` held by a Chrome we do not drive is copied and never refused, and the one refusal is no-seed-yet + `default`-open | a *re-seed* or *promote*, which do not exist — a login done in one session still never reaches another unless the caller asks (study §3.D) |
| **cookie hand-off** | `cookie_handoff.hand_off`: after a new session is created from the file copy, the SOURCE browser's cookie jar is read with `Storage.getCookies` and written into the TARGET with `Storage.setCookies`, over the CDP connections this backend already holds to both (F-898). It is what makes `seed_from` work while the source is still open, and the answer says which happened — `seeded_via: "cdp-cookies"` with counts, or `"copy"` with a `cookie_handoff_error`. It carries **the WHOLE jar and cookies ONLY**: every cookie in the source browser for every site (there is no origin filter — `carry_origins=` is named as a future opt-in and deliberately not built), and no localStorage, sessionStorage, IndexedDB, service worker or Cache Storage, all measured | a *re-seed* (the source is untouched and keeps running); the FILE copy, which still runs and is what carries everything on disk — the hand-off adds the one thing a copy of a live profile provably cannot carry |
| **reserved profile name** | `profile_seed.RESERVED_NAMES` — `master` and `master-snapshot`: the MECHANISM's own directory names, refused outright because anchoring them under the clone root hands back a DIFFERENT profile under the name the caller used (F-894). `default` was a third until F-896 gave it a meaning; it is still reserved in the sense that it names exactly one directory, so any spelling that would CREATE a second one under that word (`sessions/default`) is refused | the `default` session, which is openable; the shared profile by absolute PATH, which is allowed and selects the `default` role |
| **browser-session root** | the on-disk `STEALTH_MCP_BROWSER_SESSION_ROOT` dir holding profiles/clones | a browser session (this is the *storage* for them) |
| **instance / instance_id** | one live browser managed by `BrowserManager`, keyed by `instance_id` | a browser session (an instance is the *runtime*; a session is the *named profile*) |
| **profile** | a Chrome user-data-dir (the `default` session's, or a per-session copy) | a session (which *selects* a profile) |
| **profile clone** | a copy-on-spawn profile derived from the seed | the **element clone** (DOM extraction) — always qualify |
| **persistent profile** | `browser_pid_registry.on_persistent_profile`: a profile the caller NAMED (`spawn_browser(session=…)`), so the directory outlives its browser AND, since F-888, the browser outlives its backend — never reaped at shutdown or startup recovery, re-attached instead | a **profile clone** (disposable, dies with its browser, still reaped); a *protected backend* (F-886's question, about a BACKEND's port); `kill-orphans --force`, which takes it anyway |
| **re-attach / adoption (of a browser)** | `browser_reattach.run`: a new backend connecting over CDP to a persistent-profile browser a dead backend left running, under its RECORDED instance id | **adoption (of a backend)** below — which recorded BACKEND a client reuses. Two different questions; always qualify |
| **in-memory storage** | the deliberately non-durable `InMemoryStorage` cross-check (M15 rename of `persistent_storage`) | durable disk state (there is none for instances) |
| **clone storage** | `clone_storage.py`: the on-disk profile/clone quota + GC subsystem | in-memory storage; the cloner *engine* |
| **cloner engine** | `CDPElementCloner`: the one canonical DOM-extraction engine (post-M5b) | clone storage (disk); a profile clone |
| **display context** | `display_context()`'s token for the desktop a window launched HERE would appear on ([DESIGN §2.7](./DESIGN.md#27-display-context-where-a-window-launched-here-would-be-seen)) | the `headless=` spawn argument (a caller's request); the `DISPLAY` env var (one Linux-only input) |
| **adoption** | which recorded backend a client reuses — `adoption_candidates`, asymmetric by design ([DESIGN §2.7](./DESIGN.md#27-display-context-where-a-window-launched-here-would-be-seen)) | reuse *identity* (`singleton._same_identity_backend_ready`) — adoption picks WHICH record to test, identity decides whether it passes |
| **backend registry** | `backend_registry.py` + `server.json`: **which backend to talk to**, one entry per (display context, identity) since F-886 | the **browser-pid registry** (below); `server.port` (`PORT_FILE`), write-only legacy; `singleton.lock` |
| **protected backend** | `backend_eviction.protected`: one of ours, running, of an identity we would NOT adopt, still owning a live browser — so never terminated and never bound over (F-886) | *adoptable* (identity matches, so we'd reuse it); *alive* (`backend_liveness`'s question); an idle stale backend, which IS evicted |
| **browser-pid registry** | `browser_pid_registry.py` + `browser_pids.json`: **which browsers are tracked and by whom** | the **backend registry** (above); in-memory storage; clone storage |

---

## Tool count = 94 (derived, never typed)

The authoritative count is the live registry:
`sum(len(v) for v in SECTION_TOOLS.values())` == **94** across 11 sections.
`SECTION_TOOLS` is filled by **`server.py`'s binding loop over `SECTION_MODULES`** — the
loop walks each section module's `TOOLS` tuple, applies `section_tool(SECTION)` and binds
the result into `server`'s namespace — so the count derives from what the eleven modules
export, not from anything typed. The CLI's `--list-sections` and description string derive
their numbers from `SECTION_TOOLS`, and a test asserts the printed total equals the
registry count — so no hand-maintained number can drift. If you add a function to a
section module's `TOOLS` tuple (or remove one), the count updates itself; update the `94`
in the docs to match (the count-assertion test will remind you).
