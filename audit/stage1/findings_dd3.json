{"id":"F-140","location":"src/stealth_chrome_devtools_mcp/embedded/cdp_element_cloner.py:2-16","quote":"Enhanced Element Cloner using proper CDP methods\n=================================================\n\nThis module provides comprehensive element extraction using the full power of\nChrome DevTools Protocol (CDP) through nodriver. It extracts:\n\n1. Complete computed styles using CDP CSS.getComputedStyleForNode\n2. Matched CSS rules using CDP CSS.getMatchedStylesForNode  \n3. Event listeners using CDP DOMDebugger.getEventListeners\n4. All stylesheet information via CDP CSS domain\n5. Complete DOM structure and attributes\n\nThis provides 100% accurate element cloning by using CDP's native capabilities\ninstead of limited JavaScript-based extraction.\n\"\"\"","what":"Three independently-implemented engines all answer 'extract complete data about this element': ElementCloner.clone_element_complete (asyncio.gather over 6 JS-file evaluations, element_cloner.py:464-539), ComprehensiveElementCloner.extract_complete_element (one 216-line monolithic inline JS string, comprehensive_element_cloner.py:62-278), and CDPElementCloner.extract_complete_element_cdp (raw CDP domain calls, cdp_element_cloner.py:33-93). All three are live, each wired to its own MCP tool, and each returns a structurally different result schema (flat multi-key vs. flat camelCase vs. nested snake_case under an 'element' key). CDPElementCloner's own module docstring explicitly claims it supersedes the JS-based approaches ('100% accurate ... instead of limited JavaScript-based extraction'), yet the other two were never removed or rerouted to call it.","why_it_matters":"Any bug fix, new field, or behavior change to 'how we extract element data' must be manually triplicated across 3 unrelated code paths using 3 different browser-automation techniques (JS-file injection, monolithic inline JS, raw CDP). This has already happened partially (see F-142): a hang-avoidance rewrite reached only 1 of the 5 sibling primitives. Consumers also get inconsistent response shapes depending on which of the near-synonymous tool names they picked, with no indication which is canonical.","severity":"High","confidence":0.92,"verified":false,"survived_adversarial_review":null,"fix_direction":"Pick one engine (CDP-native, per the code's own stated rationale) as canonical, reimplement the JS-file and monolithic-JS variants as thin adapters over it or delete them outright, and converge the 3 output schemas into 1. Keep the 16 tool names as thin call-throughs if backward compatibility for tool discovery matters; the response schema changes regardless.","effort":"L","category":"architecture"}
{"id":"F-141","location":"src/stealth_chrome_devtools_mcp/embedded/file_based_element_cloner.py:101-115","quote":"            # Extract styles using element_cloner\n            style_data = await element_cloner.extract_element_styles(\n                tab,\n                selector=selector,\n                include_computed=include_computed,\n                include_css_rules=include_css_rules,\n                include_pseudo=include_pseudo,\n                include_inheritance=include_inheritance\n            )\n            \n            # Generate filename and save\n            filename = self._generate_filename(\"styles\")\n            file_path = self._save_to_file(style_data, filename)\n            \n            # Create summary","what":"FileBasedElementCloner has 8 methods (extract_element_styles_to_file, extract_element_structure_to_file, extract_element_events_to_file, extract_element_animations_to_file, extract_element_assets_to_file, extract_related_files_to_file, extract_complete_element_to_file, clone_element_complete_to_file) that all repeat the identical 4-step shape: call an underlying extractor, call self._generate_filename(prefix), call self._save_to_file(data, filename), then hand-build a bespoke 'summary' dict of a few counted fields. The 2-line filename+save pair is copy-pasted verbatim (only the string literal changes) at lines 112-113, 180-181, 247-248, 308-309, 368-369, 428-429, 490-491, and 541-542.","why_it_matters":"647-line file where roughly 400 lines are the same wrapper pattern restated 8 times. A change to save semantics (e.g. atomic write, error handling on disk-full, adding a checksum) requires 8 edits instead of 1, and the summary dicts already show inconsistent key naming across the 8 copies (e.g. 'file_path' as str vs Path, differing summary field sets) because there is no shared contract forcing consistency.","severity":"Medium","confidence":0.9,"verified":false,"survived_adversarial_review":null,"fix_direction":"Extract a single _extract_and_save(prefix, extraction_coro, summary_fn) helper; each of the 8 current methods becomes a ~5-line call site supplying only the prefix and a small summary-field lambda/dict.","effort":"M","category":"code_health"}
{"id":"F-142","location":"src/stealth_chrome_devtools_mcp/embedded/element_cloner.py:552-565","quote":"        Extract complete styling information using direct CDP calls (no JavaScript evaluation).\n        This prevents hanging issues by using nodriver's native CDP methods.\n\n        Args:\n            tab (Any): Browser tab instance\n            element (Any): Element object or None to use selector\n            selector (str): CSS selector if element is None\n            include_computed (bool): Include computed styles\n            include_css_rules (bool): Include matching CSS rules\n            include_pseudo (bool): Include pseudo-element styles\n            include_inheritance (bool): Include style inheritance chain\n\n        Returns:\n            Dict[str, Any]: Dict with styling data","what":"extract_element_styles_cdp's docstring states its CDP-native rewrite exists specifically to prevent hangs, and extract_element_styles (element_cloner.py:28-65) now does nothing but delegate to it. But the other 4 extraction primitives in the same ElementCloner class - extract_element_structure, extract_element_events, extract_element_animations, extract_element_assets - still call self._load_js_file(...) followed by await tab.evaluate(js_code) (e.g. element_cloner.py:152-153), the exact JS-evaluation pattern the CDP rewrite was built to replace.","why_it_matters":"This is a fix that landed in 1 of 5 sibling methods and was never propagated to the other 4. If the underlying hang bug is real (the docstring states it as fact, not hypothesis), 4 of 5 extraction tools in ElementCloner - extract_element_structure, extract_element_events, extract_element_animations, extract_element_assets - remain exposed to it, with no tracking issue or TODO marking the gap.","severity":"High","confidence":0.8,"verified":false,"survived_adversarial_review":null,"fix_direction":"Port extract_element_structure/events/animations/assets to CDP-native calls following the extract_element_styles_cdp pattern (DOM domain for structure/children, DOMDebugger.getEventListeners for events), removing the _load_js_file/tab.evaluate path entirely once parity is confirmed.","effort":"M","category":"code_health"}
{"id":"F-143","location":"src/stealth_chrome_devtools_mcp/embedded/progressive_element_cloner.py:56-70","quote":"            base = {\n                \"tagName\": full_data.get(\"element\", {}).get(\"html\", {}).get(\"tagName\")\n                or full_data.get(\"tagName\", \"unknown\"),\n                \"attributes_count\": len(full_data.get(\"element\", {}).get(\"html\", {}).get(\"attributes\", [])),\n                \"children_count\": len(full_data.get(\"children\", [])),\n                \"summary\": {\n                    \"styles_count\": len(full_data.get(\"element\", {}).get(\"computed_styles\", {}))\n                    or len(full_data.get(\"styles\", {})),\n                    \"event_listeners_count\": len(full_data.get(\"element\", {}).get(\"event_listeners\", []))\n                    or len(full_data.get(\"eventListeners\", [])),\n                    \"css_rules_count\": len(full_data.get(\"element\", {}).get(\"matched_styles\", {}).get(\"matchedCSSRules\", []))\n                    if isinstance(full_data.get(\"element\", {}).get(\"matched_styles\"), dict)\n                    else len(full_data.get(\"cssRules\", [])),\n                },\n            }","what":"clone_element_progressive only ever calls comprehensive_element_cloner.extract_complete_element (progressive_element_cloner.py:40-42), whose output is flat (top-level 'styles', 'eventListeners', 'cssRules'). Yet this block, and the same .get('element',{}).get(X) or .get(Y) fallback pattern repeated in expand_styles (93-104), expand_events (141-146), and list_stored_elements (231-248), defends against the nested CDP-shaped schema ('element.computed_styles', 'element.event_listeners') that only CDPElementCloner produces - a producer that is never wired into ProgressiveElementCloner.","why_it_matters":"Dead defensive branches for a data source that is never actually connected are a fossil of an abandoned or planned engine swap. Beyond the maintenance confusion, the 'or'/ternary fallback silently returns 0 or {} on a genuine shape mismatch instead of surfacing an error, which would hide a real regression (e.g. if extract_complete_element's schema changes) behind a plausible-looking empty result.","severity":"Medium","confidence":0.85,"verified":false,"survived_adversarial_review":null,"fix_direction":"Once F-140's schema consolidation lands, delete the dual-schema .get(...) or .get(...) fallbacks throughout progressive_element_cloner.py and read the single canonical shape directly; let a genuine shape mismatch raise instead of silently returning empty.","effort":"S","category":"code_health"}
{"id":"F-144","location":"src/stealth_chrome_devtools_mcp/embedded/server.py:3334-3338","quote":"    tab = await browser_manager.get_tab(instance_id)\n    if not tab:\n        raise Exception(f\"Instance not found: {instance_id}\")\n    cdp_cloner = CDPElementCloner()\n    return await _with_cdp_timeout(cdp_cloner.extract_complete_element_cdp(tab, selector, include_children), instance_id=instance_id)","what":"element_cloner, comprehensive_element_cloner, file_based_element_cloner, and progressive_element_cloner are all module-level singletons constructed once at import time (server.py imports the pre-built instances directly). CDPElementCloner is the only one of the 5 imported as a class and instantiated fresh inside the tool function body on every single call. Separately, FileBasedElementCloner.__init__ (file_based_element_cloner.py:48) constructs its own private ComprehensiveElementCloner() instance instead of importing the shared comprehensive_element_cloner singleton that comprehensive_element_cloner.py already builds at module scope.","why_it_matters":"Currently harmless only because both classes' __init__ methods are no-ops (CDPElementCloner.__init__ is 'pass'; ComprehensiveElementCloner.__init__ is 'pass'). But it is an inconsistent construction pattern across otherwise-parallel classes: the next person to add caching, connection reuse, or any per-instance state to CDPElementCloner or ComprehensiveElementCloner will silently get per-call or duplicate-instance semantics instead of the singleton behavior every sibling class relies on.","severity":"Low","confidence":0.88,"verified":false,"survived_adversarial_review":null,"fix_direction":"Instantiate cdp_cloner = CDPElementCloner() once at module scope alongside the other 4 singletons and import it in server.py; have FileBasedElementCloner accept/import the shared comprehensive_element_cloner singleton instead of constructing its own.","effort":"S","category":"code_health"}
{"id":"F-145","location":"tests/test_clone_storage_cap.py:19-23","quote":"import json\nimport os\nimport server\nfrom server import _clone_is_auto, _enforce_clone_storage_cap_in","what":"No test in the repository exercises the extraction logic of any of the 5 cloner classes (ElementCloner.extract_*, ComprehensiveElementCloner.extract_complete_element, ProgressiveElementCloner.clone_element_progressive/expand_*, CDPElementCloner.extract_complete_element_cdp, or any FileBasedElementCloner method's actual extraction path). Only 3 files reference these classes/instances by name (test_element_cloner_output_dir.py, test_clone_output_dir.py, conftest.py) and all 3 test only FileBasedElementCloner's output-directory path resolution. The other 5 clone-named test files (test_clone_storage_cap.py, test_clone_sweep_race.py, test_clone_trash_recovery.py, test_clone_legacy_marker_classification.py, test_profile_clone_excludes_cache.py) import disk-hygiene helpers directly from server (_clone_is_auto, _enforce_clone_storage_cap_in, _profile_ignore_names, _copy_profile_delta) - a storage-eviction subsystem layered on top of the clone output directory, unrelated to whether extraction itself is correct.","why_it_matters":"The consolidation this deep-dive recommends (F-140/F-141/F-142) would be executed with zero automated signal on whether the unified extractor still returns equivalent data to the 3 engines it replaces. Given the priority order for this audit is maintainability first, the highest-leverage fix here (collapsing 3 extraction engines into 1) is also the riskiest to do blind.","severity":"High","confidence":0.9,"verified":false,"survived_adversarial_review":null,"fix_direction":"Before or alongside consolidation, add extraction-correctness tests against a mocked tab/CDP surface (mirroring the mocking style test_server_call_conventions.py already establishes) covering at minimum: element found/not-found, styles/events/children shape, and the schema each of the 3 engines currently produces, so a consolidation PR can diff old-vs-new output.","effort":"M","category":"testing"}
